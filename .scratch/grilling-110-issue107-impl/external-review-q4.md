Title: 看看这段聊天

URL Source: https://chatgpt.com/s/t_6a950b6597d48191a0ab54d5abf3e0af

Published Time: 2026-08-31T05:04:37.593Z

Markdown Content:
对，**出问题的不是“缺 role 必须做 Console 级回归”这个设计目标，而是 #106 Q5 把“坏数据注入方式”也锁死在了 `FindDataDir()` 的真实数据发现路径上**。

换句话说：

> **Q5 的测试语义没问题，但测试夹具/数据目录注入机制设计错了。**

### 问题本质

现在链路实际上是：

所以即使 `SnakeCaseEnumConverter.Read` 已经确认：

**Q5 仍然测不到这条路径。**

* * *

## 建议解决方案：保留 Q5，改造数据注入边界

我建议不要推翻 Q5，也不要把 `FindDataDir()` 改成专门服务测试。

应该把设计改成：

> **生产模式继续使用 `FindDataDir()`；Console 回归测试通过明确的测试数据目录入口注入坏数据。**

最干净的是给程序增加一个**显式 data-dir 覆盖机制**，例如：

Bash

或者如果项目已有参数体系，也可以采用环境变量：

Bash

我更推荐 **CLI 参数**，因为 Q5 本身就是 Console 进程级测试，测试意图会非常清楚：

而不是偷偷改变 `FindDataDir()` 的行为。

* * *

## 测试夹具只放两个文件

你已经确认了一个非常重要的事实：

> `LoadAll` 中 `tripartite_model.json` 是第二个加载文件。

所以 Q5 根本不需要复制完整 `data/`。

临时目录：

其中：

*   `brain_regions.json`：真实、合法

*   `tripartite_model.json`：仅把一个必需的 `role` 改成 `null`

这样执行：

实际链路就是：

后面的 `FileNotFound` 根本不会影响结果，因为第二个文件已经把进程终止路径触发了。

* * *

## 为什么不建议修改 `FindDataDir()` 的“向上 8 层”

**不要为了 Q5 把这个改成无限向上搜索或者增加搜索路径。**

那是在修测试，而不是修产品接口。

`FindDataDir()` 当前的职责应该仍然是：

> **在正常运行环境中找到程序的数据目录。**

而 Q5 的职责是：

> **证明一个指定的数据输入会导致 Console fail-fast。**

这两个职责最好分开。

否则以后测试可能变成：

这是典型的测试侵入生产路径。

* * *

## 如果不想新增 `--data-dir`

还有一个更小的方案：

**让 Q5 的临时目录伪装成 `FindDataDir()` 当前认可的目录结构/位置。**

但我不推荐。

因为你现在已经发现：

> FindDataDir 向上 8 层 + `/home/dog/game/data` 兜底

这意味着测试必须“猜”程序的数据发现规则。

这种测试的脆弱性很高：

这恰恰是你现在碰到的问题。

* * *

## 所以我建议把 #106 Q5 修正成这个协议

### Q5 测试契约

**目的不变：**

验证缺失/非法 `role` 在真实 Console 进程中：

**新增一个测试输入边界：**

### Fixture

### 断言

至少锁定：

并且建议再加一个**防假阳性断言**：

更关键的是，可以把 Q5 的验收语义写成：

> **必须由 `tripartite_model.json` 中的非法 `role` 触发 fail-fast，而不能因为测试夹具缺少后续数据文件导致 exit 1。**

这样就彻底解决你指出的问题。

* * *

## 最终判断

所以我的裁决是：

**不是 #106 Q5 的“Console 级缺 role 回归”设计错了。**

真正的问题是：

> **Q5 锁定了“进程级行为”，却没有同时锁定“可控输入数据源”。**

导致测试实际上测成了：

而不是：

### 建议改动优先级

**最佳方案：**

这属于**测试基础设施/输入边界修正**，不应该改变 `SnakeCaseEnumConverter` 本身，也不应该为了测试修改真实数据。

而且这和你前面已经实测锁死的 **Q1 `"role": null"` converter 路径**是完全一致的：Q1 证明“故障确实存在”，Q5 负责证明“真实 Console 入口能正确 fail-fast”。现在只是把 Q1 的坏输入真正送进 Q5。

* * *

If you want, I can:

*   继续详细说明如何实现--data-dir参数支持

*   编写Q5测试用例示范代码

*   解释为何不应修改FindDataDir函数及其测试侧影响
