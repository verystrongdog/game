## 话题

开题并裁决 E2 月相调制标定（#38 Q7 实验轨 E1→E2→E3→E4→E6→E5→E7 中 E1 之后的 P0 项）：把「E2 移动壳问题」从自然语言落成可执行的预注册数值协议并实现跑通。

**E2 与 E1 的本质区别**：E1 验证固定月相下的壳动力学；E2 验证**移动壳**——E_c 本身随 λ(t) 运动，公式链 `λ(t) → E_c(λ(t)), E_th(λ(t)) → E(t), q_i(t), q̂_i(t)`。

E2 正典定义（数学框架 §11.6）：扫描 f_c(λ;θ_c)/f_th(λ;θ_th)，验证 λ→E_c(λ)→q^base(λ) 产生合理连续可校准的月相响应。

## 待裁决项（来源：ChatGPT 分享页 2026-08-29）

1. **月相驱动 λ(t) 的时间尺度**——「慢调制」相对 τ_char 的定量定义（绝热参数/时间尺度比）
2. **移动壳跟踪判据**——r(t)=E(t)/E_c(λ(t)) 的跟踪滞后/偏差允许范围（E1 的 |r−1|≤δ_r 不能原样搬用，E_c 本身在动）
3. **职责分离**——λ(t)（外部月相参数）/ E_c(λ)（壳高度）/ E_th(λ)（阈值）/ q_i^base(λ)（节点归一基线）四函数角色分离与防重叠措施；含与 E6（q^base(λ) 独立标定）的边界裁决
4. **E2 验收判据**——E1 有 Q1-Q6 预注册 PASS 结构，E2 的 PASS 条件需等价的量化定义
5. **数值协议适配**——λ(t) 时变引入绝热误差与 ε_num 数值误差的分离；G_test 沿用 P₄ 或更换；占位参数延续/更换；0.675→0.900 先验参考非硬拟合的检查方式
6. **执行时序裁决**——E2 归属 #35 scope 伞（#100 Q7 已并入）；与 #101 并行关系维持；「相信被打败」暂缓维持

## 六维定位

- **维度**: 规则（月光场数值校准——公式/判据）+ 管线（sim 脚本验证——怎么造出来、怎么验证）
- **依赖**: Grilling #38 已闭合（§11.1-11.6 接口形态定死，数值全待校准）/ Grilling #100 E1 协议已 PASS（E2 前置基线）/ issue #35（scope 伞，E1-E7 已并入）/ 数学框架 §11.6 E2 定义
- **阻塞**: E3 局部注入响应 / E4 群体事件驱动 / E6 q^base(λ) 标定 / E5 边权敏感性 / E7 g(SAN,state)——E2 不 PASS 则后续实验轨全部阻塞

## 当前状态

- E2 定义（数学框架 §11.6 :366）：扫描 f_c(λ;θ_c)/f_th(λ;θ_th)，验证 λ→E_c(λ)→q^base(λ) 产生合理连续可校准的月相响应；E2 对照 0.675→0.900（旧 v2 g_0×lunar 表）为先验/验收参考，非理论输入、非硬拟合目标（决策树 #38 Q7 :2240）
- E1 前置基线（#100）：PASS，占位参数域 E_c=1 / E_th=0.5 / ε=0.1 / d_max=1 / w=1，G_test=P₄，h_E1(x)=tanh(x)·tanh(x−x_th)（标注「E1 实现选择，非理论新增公理」——E2 可更换形状不构成推翻理论结构）
- 待裁决：三个钉死项（时间尺度/跟踪判据/职责分离）+ 验收判据 + 数值协议 + 时序
- 事实发现：E6（q^base(λ) 独立标定，P2）与 E2 定义中的 q^base(λ) 响应验证存在职责重叠风险，需裁决边界；「慢调制」「合理连续可校准」「跟踪滞后」均为未量化表述，须先量化后定验收

## 来源

ChatGPT 分享页（t_6a93125600bc81918b48d4848de68de0，2026-08-29「裁决随机性供给」高亮消息，会话末尾 E1→E2 总结）+ 决策树 #38 Q7（:2240）/ #100（:3463-3517，E2 可开题 :3499、按序执行 :3517）+ 规则/时空结构数学框架.md §11.6 + term_registry（壳态/A1/E_c/E_th/q̂/τ_char 已入库）+ sim_moonlight_e1.py（占位参数注释指向 #35 E2 标定）
