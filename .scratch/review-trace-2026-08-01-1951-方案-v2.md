# Review Trace: 方案-v2.md

> 审查日期: 2026-08-01 19:51 | 审查范围: `.scratch/review-plan-design/方案-v2.md`
> 方法: 符号执行 + 限界验证 | 前置检查: 无

## 前置输入

- [x] 无 --pre-check（AI 全量 trace）
- [x] 被引用文件自动拉入: 无（H1 命中均为文献/尾注引用，非数据依赖，不拉入）

## Trace Table

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| 1 | §三 H1-H6 定义 | 无（定义层） | — | 定义 6 条启发式规则及其触发信号 | `枚举<H1\|H2\|H3\|H4\|H5\|H6>` | §四 H→9列映射（消费 H 类型选择填列策略）、§九 step 1（消费触发信号标注段落） | 无 | ✅ |
| 2 | §三 不触发 | H1-H6 触发条件（#1） | `枚举<H>` | 过滤：排除纯叙述/设计意图/已废弃/索引段落 | `List<段落>`（已过滤的规则候选） | §九 step 1（标注阶段——仅对过滤后段落做 H 标注） | "纯叙述"与"含数据流动"的边界模糊。例如"X 系统负责 Y 功能"可能是叙述，但如紧跟数据定义则含隐含数据流。误过滤（规则段落漏标）→ 后续 trace 静默缺失。当前无召回率检查机制 | ⚠️ |
| 3 | §四 H→9列映射 | H1-H6 触发类型（#1） | `枚举<H>` | 每个 H → 填列顺序 + 推导策略 + 交叉类型规则 | `Dict<H, 填列算法>` | §九 step 3（消费填列顺序执行 trace） | 1) H4 同时命中 H5 管道步骤→ 处理顺序 H3→H4→H5 已定义；2) 同一段落同时触发 H2+H3 时的行间排序未定义——依赖文本顺序，但排序规则只在文件级。极端：类型定义（H2）出现在变换（H3）之后→ trace 行顺序反直觉 | ⚠️ |
| 4 | §四 排序规则 | 被审查文件集 | `List<引用<md文件>>` | 按数据流依赖排序：父类→子类 > 被引用→引用方 > 数据文件→消费方 > 方案→实现 | `List<引用<md文件>>`（已排序） | §九 step 2（消费排序后文件列表执行 trace） | 1) 循环引用→保留原序✅；2) 多文件有相同优先级→ 稳定排序未定义（按文件名？）；3) 排序结果依赖 H1 正确提取所有引用，H1 漏标→ 排序错误 | ⚠️ |
| 5 | §四 叉乘上限 | 输入值域 × 分支条件 的 N×M 组合数 | `数值` | N×M>50 → 抽样：全边界（数值 min+max+null / 枚举全值+null）+ 随机 10-15 组，总计 ≤30 | 抽样组合集 + `⚠️ 抽样检查` 标注 | §九 step 3（消费抽样集做叉乘覆盖检查）+ §十一 输出（消费标注） | 1) 混合型输入（数值×枚举）的全边界笛卡尔积可能已超过 50，抽样逻辑嵌套调用？边界递归未定义；2) 随机抽样若恰好漏掉唯一出问题的组合→ 假阴性。抽样方法（均匀/分层）未指定；3) null 输入在枚举型中的语义：是枚举值之一还是额外输入？ | ⚠️ |
| 6 | §五 9列 Schema | 无（格式定义层） | — | 定义 trace table 的 9 列及其追问问题 | `Dict<列名, 追问>` | §九 step 3（消费列 schema 填 trace 行）+ §十一 输出模板（消费列名渲染表头） | "边界"列：描述为"空输入/极端值/类型不匹配"，但正常值域边界（如 m∈[0,1.4] 的 0 和 1.4）是否也在此列？当前措辞含"极端值"暗示含正常边界，但与 §四 的"全边界"定义有语义重叠 | ⚠️ |
| 7 | §六 基础类型表 | 无（类型系统定义层） | — | 定义 4 种基础类型标签及其"已定义"标准 | `枚举<枚举<T>\|数值\|引用<T>\|文本>` | 所有 trace 行的"输入·类型"/"输出·类型"列（消费类型标签标注） | 1) `文本` 类型的"无约束"判定的逆向风险：格式约束存在但未被标注为 tagged text → 审查无法发现格式违规；2) `枚举<T>` 的 T 是任意字符串，无命名空间管理——同一枚举名在不同文件中可能含义不同，但类型系统不区分 | ⚠️ |
| 8 | §六 判定规则 | 类型标签 + 已定义/未定义 | `枚举<枚举\|数值\|引用\|文本>` × `枚举<已定义\|未定义>` | 将类型×定义状态映射为判定结论 | `枚举<✅\|⚠️\|❌>` | §九 step 3（消费判定填充 trace 行的"判定"列） | 复杂类型的组合判定未定义。例如 `List<枚举<T>>`——枚举值域列全但 List 可空？此时判定是 ✅ 还是 ⚠️（List 空集合边界）？ | ⚠️ |
| 9 | §七 停止条件 | trace 行状态 | `Dict<列名, 填写状态>` | 3 正常停（项目边界/类型闭合/重复模式）+ 4 不能停信号 → 判定停或继续 | `枚举<stop\|continue>` | §九 trace 循环（消费停止判定决定是否继续 trace） | 1) "重复模式停"——"与之前 trace 同构"的比较粒度未定义：完全相同的输入输出类型？还是相同变换模式？2) "项目边界停"中 `tools/` 目录脚本默认为非标准——但如果项目新增其他目录的自写脚本，边界判断依赖审查者了解"哪些是自写的" | ⚠️ |
| 10 | §八 半径扩张 v1 | trace 行判定（#8 的输出） | `枚举<❌\|⚠️>` | 强制填写"输出·消费者"列 → 自动进入待复查清单 | `List<修复点：{修复内容, 受影响消费者, 复查状态}>` | §九 step 3（产生缺口时写入待复查）+ step 5（输出阶段渲染待复查表）+ §十一（输出模板的"半径扩张"段） | 1) 修复者修复缺口 A 后，A 的消费者列的修复可能引入新缺口 B → 新缺口是否自动追加到本次待复查清单还是等下次 review-plan？2) 消费者列为空（不可能搜到消费者）时→ 不能强制填写，但当前规则说"必须"填写 | ⚠️ |
| 11 | §八 变更追踪 | 两次 trace 输出的缺口汇总 | `List<缺口>` × `List<缺口>` | diff 缺口汇总：消失=已修复，新增=修复引入 | `List<{上次缺口, 本次状态, 备注}>` | §十一 输出模板（消费渲染变更追踪表）+ 人类读者 | 上次 trace 文件发现：glob 按时间戳，同名文件可能有多轮审查，取"最新非本次"——但如果两轮审查范围不同（传入文件不同），diff 语义不正确。需加审查范围一致性检查 | ⚠️ |
| 12 | §九 执行流程 Step 1 解析 | 用户命令行输入 | `List<文件路径>` + `Optional<文本>`（--pre-check） | 读取 md → H 标注 → H1 拉入 → 输出标注后段落集 | `{文件集, 标注段落, 已验证项集合}` | Step 2（消费文件集排序）+ Step 3（消费标注段落 trace） | 1) H1 拉入深度 1 级✅，但如果拉入文件自身也有被引用的数据文件（JSON/YAML），是否一并拉入？当前只说不递归 md→md，数据文件引用未覆盖；2) 拉入上限 5 文件，超限时哪些文件被拉入取决于遍历顺序（未指定） | ⚠️ |
| 13 | §九 执行流程 Step 3 Trace | 标注段落（#12 的输出）+ 类型系统（#7） | `List<段落>` × 类型标签集 | 逐条填 9 列 → 叉乘检查 → 空白格判 ❌/⚠️ → 缺口处填消费者列 | `Trace Table`（9列×N行）+ `待复查清单` | Step 3.5（交叉比对）+ Step 4（L1.5 抽查）+ Step 5（输出） | "逐条"顺序：同文件内按文本顺序，跨文件按排序结果。但 H4（扩展 H3 行）需"H3 行在前"——如果排序导致 H3 行不在 H4 之前（跨文件场景），扩展关系断裂 | ⚠️ |
| 14 | §九 执行流程 Step 3.5 交叉比对 | Layer 2 trace ❌/⚠️（#13 的输出）+ Layer 1 覆盖声明 | `List<缺口>` × `List<覆盖类别>` | 缺口与 L1 覆盖声明取交集：命中→ L1 可能漏检，未命中→ 正常标记 | `List<缺口>`（带交叉标记） | §十一 "前置输入"段（消费标记输出）+ 人类读者 | 1) 依赖 --pre-check，不传则整步跳过✅；2) L1 覆盖声明来源于推荐格式的"检查范围"段——但如果脚本输出用退化路径（非标准格式），无检查范围段，此时交叉比对做不了但 step 4 抽查也做不了→ 两个盲区叠加 | ⚠️ |
| 15 | §九 执行流程 Step 4 L1.5 抽查 | L1 覆盖声明（来自 --pre-check） | `List<覆盖类别>` | 随机抽 3 项独立验证→对比结果 | `枚举<一致\|不一致>` + 可能标记 | §十一 "前置输入"段 | 退化路径（无检查范围段）→ 跳过抽查，标注 ⚠️。但跳过抽查 + 交叉比对也可能退化 → L1 的"已验证"状态实际未被任何机制质疑。建议在退化叠加时升级警告为 ❌（"前置输入不可验证"） | ⚠️ |
| 16 | §十 分层模型 Layer 1 | — | `文本`（脚本输出） | 格式/引用/参数/结构检查 | 检查通过项列表 | Layer 1.5（抽查输入）+ Layer 2（跳过已验证项） | "确定性（同输入→同输出）"✅。但 Layer 1 脚本的版本管理未提及——脚本升级后输出格式变化可能破坏 --pre-check 解析 | ⚠️ |
| 17 | §十 --pre-check 推荐格式 | — | — | 定义推荐结构：脚本名/检查范围/结果/汇总 | `文本`（结构化的脚本输出） | §九 step 1（解析为已验证项）+ step 4（消费检查范围段做抽查） | 1) 推荐格式中的 check_id 无命名空间——不同脚本可能有相同 check_id；2) PASS/FAIL/WARN 三元 vs Trace Table 的 ✅/⚠️/❌ 三元不完全对应（WARN ≈ ⚠️ 但语义略有差异）；3) 汇总的 X/Y/Z 必须与结果段的计数一致——无自检机制 | ⚠️ |
| 18 | §十一 输出格式 前置输入段 | --pre-check 结果（如有） | `文本` | 渲染为前置输入 checklist | `{pre_check_label} — {pre_check_summary}` | 人类读者 | {pre_check_label} 和 {pre_check_summary} 是模板占位符✅。但如果 --pre-check 传入的是文件路径（vs 内联文本），引擎先读文件再解析——文件不存在 → 应报告 ⚠️ 而非静默跳过 | ⚠️ |
| 19 | §十三 审查范围策略 | 用户传入文件数 | `数值` | 按文件数分支：1-2=全trace / 3-5=聚焦H1+H5 / 6+=仅H1 | `枚举<全trace\|聚焦H1+H5\|仅H1>` | §九（在 Step 1 后、Step 3 前消费策略选择） | 1) 文件数=传入文件数✅；2) 行数>5000 软上限✅；3) 策略选择发生在 Step 1 解析后、Step 2 排序前——但如果此时 H1 拉入已完成，文件集已扩大，策略仍按传入数判定，可能导致"1 小文件+5 大拉入文件"全 trace→ 实际工作量远超预期 | ⚠️ |
| 20 | §十四 v0→v1→v2+ 路线 | 当前状态（v0 方案） | `文本` | v0 方案→ v1 纯 AI Skill→ v2+ 运行评估后固化脚本 | `枚举<v0\|v1\|v2+>` + 各阶段产出 | v1 实现者（消费 SKILL.md 规格）+ v2+ 评估者 | v2+ 的"运行 3-5 次后评估"——评估标准未定义。什么指标触发"需要固化脚本"？AI 漏检率？人工复查成本？ | ⚠️ |
| 21 | 附录A 自检信号表 | 运行中统计 | `数值`（连续计数） | 阈值触发 → 提示用户介入 | `枚举<信号类型>` + 提示文本 | Skill 运行循环（消费触发信号中断审查） | 1) "连续"计数——如果信号条件在行间交替出现（如第 3 行触发类型分类退化、第 4 行不触发、第 5 行又触发），"连续"中断→不触发信号，但实际退化仍在发生；2) 信号触发后用户介入——如果用户选择忽略，信号是否持续重复触发？冷却机制未定义 | ⚠️ |

## 缺口汇总

| # | 位置 | 严重度 | 描述 |
|----|------|--------|------|
| 1 | §三 不触发 | ⚠️ | "纯叙述"过滤无召回率检查——规则段落可能被误过滤导致静默缺失 |
| 2 | §四 H→9列映射 | ⚠️ | 同一段落同时 H2+H3 触发时行间排序未定义（仅依赖文本顺序） |
| 3 | §四 排序规则 | ⚠️ | 同优先级文件的稳定排序未定义；排序依赖 H1 正确提取 |
| 4 | §四 叉乘上限 | ⚠️ | 混合型输入边界笛卡尔积可能超 50→嵌套抽样逻辑未定义；抽样方法未指定 |
| 5 | §五 边界列 | ⚠️ | 正常值域边界 vs 异常边界的归属含混 |
| 6 | §六 基础类型 | ⚠️ | 文本无约束判定的逆向风险；枚举 T 无命名空间管理 |
| 7 | §六 判定规则 | ⚠️ | 复合类型（List/Dict/Optional）的边界判定未定义 |
| 8 | §七 停止条件 | ⚠️ | "重复模式同构"比较粒度未定义；tools/ 外自写脚本边界不明 |
| 9 | §八 半径扩张 | ⚠️ | 修复引入新缺口的追加时机未定义；消费者列为空时的强制填写矛盾 |
| 10 | §八 变更追踪 | ⚠️ | 审查范围不同时 diff 语义不正确 |
| 11 | §九 Step 1 | ⚠️ | H1 拉入不覆盖被引用文件的数据文件引用（JSON/YAML） |
| 12 | §九 Step 3 | ⚠️ | 排序后 H4 扩展可能找不到跨文件的 H3 行 |
| 13 | §九 Step 3.5+4 | ⚠️ | 退化叠加：交叉比对和抽查同时退化时 L1 无人质疑 |
| 14 | §十 Layer 1 | ⚠️ | 脚本版本管理与 --pre-check 解析兼容性 |
| 15 | §十 推荐格式 | ⚠️ | check_id 无命名空间；PASS/FAIL/WARN 与 ✅/⚠️/❌ 不完全对应 |
| 16 | §十一 前置输入 | ⚠️ | --pre-check 文件路径不存在时的处理未定义 |
| 17 | §十三 策略 | ⚠️ | 1 小文件+5 大拉入文件→全 trace 实际工作量失控 |
| 18 | §十四 v2+ | ⚠️ | "运行 3-5 次后评估"的固化触发标准未定义 |
| 19 | 附录A 信号 | ⚠️ | "连续"计数对交替退化模式不敏感；信号重复触发的冷却机制缺失 |

## 半径扩张 — 待复查

| 修复点 | 受影响消费者 | 复查状态 |
|--------|------------|---------|
| （待修复后填充） | | |

## 变更追踪

| 上次缺口 | 本次状态 | 备注 |
|---------|---------|------|
| — | — | 首次 trace（方案-v2.md 修订后 v1.0） |

## 统计

- 总 trace 行数: 21
- ✅ 类型闭合: 1
- ⚠️ 有疑点: 20
- ❌ 有缺口: 0
- 待复查: 0

---

*审查方法: review-plan v2（符号执行 + 限界验证）*
*启发式: H1-H6 | 类型系统: 枚举/数值/引用/文本*
