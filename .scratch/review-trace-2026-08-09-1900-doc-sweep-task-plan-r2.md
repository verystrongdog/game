# Review Trace: doc-sweep-task-plan（第2轮）

> 审查日期: 2026-08-09 19:00 | 审查范围: task-plan.md（9 文件 20 处修正）
> 方法: 符号执行 + 交叉比对 | 前置检查: run_all_checks.py（4 FAIL 均为既有问题）
> Diff-against: review-trace-2026-08-09-1857-doc-sweep-task-plan.md

## 变更追踪（vs 第1轮）

| 第1轮缺口 | 本次状态 | 操作 |
|----------|---------|------|
| G1: C3 目标选择表与 NPC AI doc 不一致 | 仍开放 | task-plan 需修正 C3 |
| G2: F3 状态表未核实 | 仍开放 | task-plan 需修正 F3 |
| G3: 技能生成机制.md L3 遗漏 | 仍开放 | task-plan 需新增 J1 |

## 本轮 Trace — 仅追踪第1轮缺口相关行

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| R1 | C3 §11.4 目标选择 | task-plan C3 段 | `表格<5×2>` — 单tone→孤立倾向 | 修正为交叉引用摘要 | `文本` — "详见 NPC AI 行为模型.md §三" | 回合战斗流程.md §11.4 | 源表与 NPC AI doc §3.3 per-role tone_bias 定义不一致 → 修正后一致 ✅ | ⚠️→✅(修正后) |
| R2 | F3 §十 状态表 | task-plan F3 段 | `表格<4×?>` — 4行无状态列 | 每行增加 ✅/⚠️ 状态列 | `表格<4×?+1>` | 运行时状态模型.md §十 | 核实结果: 行1✅ 行2⚠️ 行3⚠️ 行4✅ | ⚠️→✅(修正后) |
| R3 | J1 技能生成 L3 | `规则/技能树系统/技能生成机制.md:3` | `文本` — "效果从链路状态（m × focus × rel）涌现计算" | 新增修正项到 task-plan | `文本` — "效果从皮层节点 a(t) + CSTC gate_bonus 派生" | 技能生成机制.md 摘要 | body 已更新但摘要残留旧公式 | ❌→✅(修正后) |

## 修正规格（可直接应用到 task-plan）

### 修正1: C3 → 交叉引用

**定位**: task-plan §二 → C组 → C3 段
**旧**: L60-72（tone 配置表 + 5行倾向）
**新**:
```
**C3. §11.4 NPC目标选择（L526-540，整段替换）**
- 旧：`在预烘焙行为表中嵌入目标选择逻辑（Softmax 采样，详见预烘焙管线）` + 6性格标签目标选择表
- 新：`NPC 目标选择从 Affordance Competition 的 salience 计算中自然导出。
  每个候选目标的 salience = 相关皮层节点 a(t) 均值 × gate × tone_bias(role)
  （详见 [NPC AI 行为模型](技能树系统/NPC%20AI%20行为模型.md) §三）。
  NPC 选择目标 = argmax 目标 salience。
  L3/L4 感知技能可揭露更多目标信息（最脆弱目标、读自己意图的目标等）。`
  （删除旧6标签目标选择表，改为交叉引用 + 公式摘要）
```

**理由**: NPC AI 行为模型.md §3.3 的 tone_bias 是 per-role 加权和，不能拆成单 tone 孤立维度。

### 修正2: F3 → 状态核实

**定位**: task-plan §二 → F组 → F3 段
**旧**: L98-101
**新**:
```
**F3. §十 需要更新的已有文件表（L572-579）**
- 当前状态：4行全部标记为"待变更"，需更新为实际完成状态。
- 核实结果（Grilling #33 完成情况）：
  | 行 | 文件 | 状态 |
  |----|------|------|
  | 1 | 核心机制.md §2.1/§52 | ✅ 已完成 — L129/134 已用 WC 公式 |
  | 2 | 技能生成机制.md §一/§五 | ⚠️ body 已更新, L3 摘要残留 — 见新增 J1 |
  | 3 | term_registry.json | ⚠️ 待核实 — 本次不处理 JSON |
  | 4 | 回合战斗流程.md §三 | ✅ 已完成 — L106-120 已用 a(t) 节点 |
- 操作：在表中增加"完成状态"列，标注以上状态。
```

### 修正3: 新增 J1 → 技能生成机制 L3 摘要

**定位**: task-plan §二 → 在 I 组之后新增 J 组
**新组**:
```
### J组：规则/技能树系统/技能生成机制.md（1处修正 — review-plan 第1轮发现）

**J1. L3 摘要句**
- 旧：`效果从链路状态（m × focus × rel）涌现计算`
- 新：`效果从皮层节点激活 a(t) + CSTC gate_bonus(role) 派生（运行时状态模型 §八）`
- 注：body L29 已用新公式，L87-88 已有迁移对照——仅摘要句未同步（Grilling #33 残留）。
```

### 变更汇总更新

在 task-plan §三 表格中新增 J 组行，总数更新为 **10文件，21处修正**。

## 缺口汇总（第2轮）

| # | 位置 | 严重度 | 描述 | 修正后 |
|----|------|--------|------|--------|
| G1 | C3 | ⚠️ | 目标选择表与源文档不一致 | ✅ 修正为交叉引用 |
| G2 | F3 | ⚠️ | 状态表未核实 | ✅ 更新为核实状态 |
| G3 | 遗漏 | ❌ | 技能生成 L3 摘要旧公式 | ✅ 新增 J1 |

## 统计
- 第1轮: 19 trace / 15✅ / 3⚠️ / 1❌
- 第2轮: 3 trace（仅追第1轮缺口）/ 3→✅(修正后)
- 修正后 task-plan 无已知缺口
