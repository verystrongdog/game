# Review Trace: task-plan.md (v2 final)

> 审查日期: 2026-08-05 07:38 | 审查范围: `.scratch/moonlight-calibration/task-plan.md`
> 方法: 符号执行 + 限界验证 | 前置检查: 无
> 前次 trace: `review-trace-2026-08-05-0702-task-plan.md`（v1 中期草案，19行/8❌/6⚠️）

## 前置输入

- [x] 被引用文件: `事件/月光场-标量场模型.md` §7.3（实体类型表），`sim_moonlight_v2_sanity.py` §4（多观察者叠加）
- [x] Layer 1.5 抽查: 无前置脚本，跳过

## Trace Table

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| **R1** | §一 η公式 | g_0(R3), c(SAN)(R4), κ(R8) | `数值(g_0)`, `数值(c)`, `数值(κ)` | `η = g_0 × c × κ` | `数值(η)` ∈ [0, 0.9] | R5(φ叠加), R6(相变判定), R11(情境偏移), R12(NPC rel) | g_0∈[0.675,0.9], c∈[0.03,1.0], κ∈[0.6,4.5] → η∈[0.012, 4.05]。**上界 4.05 > g_0=0.9** — κ>1 时突破 | ⚠️ η上界 |
| **R2** | §一 消去 d_0 | d_0 | `数值(d_0)` ∈ {10..3000} | `η = g_0 × c × log(d_0) × κ / log(d_0) = g_0 × c × κ` | 化简后的三重积 | R1 | d_0=1→log(1)=0→除零。**d_0 最小值 10（投影造物），log(10)≈2.3≠0 → 安全。** 消去过程依赖 log(d_0)≠0 | ✅ |
| **R3** | §三 g_0月相 | lunar（§三枚举） | `枚举<0.75\|0.88\|1.00>` | `g_0 = 0.9 × lunar` | `枚举<0.675\|0.792\|0.900>` | R1(η) | 三档完备。0.88 为手动中点——不对称偏向满月。实际连续月相→3档的阈值未定义 | ⚠️ 月相粒度 |
| **R4** | §二 c(SAN) | SAN | `数值(SAN)` ∈ [0, 90+] | 分段线性4段, 锚点{0.03,0.08,0.50,1.00} | `数值(c)` ∈ [0.03, 1.0] | R1(η) | 4锚点已定义。SAN<0未处理（但SAN≥0保证）。SAN>90→c=c_min常数。分段点SAN=90/60/30/0处一阶连续（线性插值保证） | ✅ |
| **R5** | §六 φ叠加 | η_i(R1), r_i, R | `List<(η, 距离, 方向)>`, R∈[3,20] | `φ(point)=Σ η_i × e^(-r_i/R) / r_i` | `数值(φ)` | 游戏引擎(场感知), R6(η_crit空间判定) | r_i→0→1/r_i→∞。sim已有除零保护(r_i<1e-9→1e-9)。R 随实体类型变化 | ✅(浅) |
| **R6** | §五 η_crit | η(R1) | `数值(η)` | `η < 0.3 → 经典现实; η ≥ 0.3 → 月光现实` | `枚举<经典\|月光>` | 战斗系统(技能可用性), 遭遇系统(造物生成), 昼夜判定 | **临界点 η=0.3 精确值时属于哪边？** "≥"意味着 η=0.3→月光——需确认 | ⚠️ 边界 |
| **R7** | §七 V方向 | T(R13), D(R14) | `List<数值>` 长度6 | `V = T + D` | `List<数值>` 长度6 | R10(axis), R9(conc) | T_i∈[-0.5,0.5], D_i 值域未声明 | ⚠️ D_i值域 |
| **R8** | §四 κ查表 | entity_type | `枚举<12种实体类型>` | 查表获取 κ ∈ [0.6, 4.5] | `数值(κ)` | R1(η) | 12种类型的κ值已从η目标反推。**新实体类型创建时κ如何确定？** — 文档未说明 | ⚠️ 新类型 |
| **R9** | §七 conc | V(R7) | `List<数值>` 长度6 | `conc = |V_axis| / Σ|V_i|` | `数值(conc)` ∈ [0, 1] | R11(情境偏移), R12(NPC rel) | Σ|V_i|=0→除零。全零标签+无疾病→全零→需保护 | ❌ 除零 |
| **R10** | §七 axis | V(R7) | `List<数值>` 长度6 | `axis = argmax(|V_i|)` | `枚举<1..6>` | R15(叙事标签), R11(情境偏移方向) | 并列最大值→argmax取第一个。语义上多轴并列时取第一个可能丢失信息 | ⚠️ 并列 |
| **R11** | §七 情境偏移 | conc(R9), η(R1), Δ_situation_max[axis] | `数值(conc)`, `数值(η)`, `数值(Δ_max)` | `offset = conc × η × Δ_situation_max[axis]` | `数值(offset)` | 情境系统(situation_primitives.json) | **Δ_situation_max[axis] 的6个值未定义**——每个语义轴的情境维度最大偏移量未知 | ❌ 缺参数 |
| **R12** | §七 NPC rel | conc(R9), η(R1), direction_weight[axis] | `数值(conc)`, `数值(η)`, `数值(weight)` | `rel_new = baseline × (1 + conc × η × weight[axis])` | `数值(rel)` | 预烘焙管线(NPC AI) | **direction_weight[axis] 的6个值未定义**。rel 放大后是否需要 clamp？ | ❌ 缺参数 |
| **R13** | §七 T向量 | 性格标签偏移值 | `List<数值>` 长度6, 各∈[-0.5,0.5] | 直接使用 | `List<数值>` | R7(V) | 已有定义（NPC AI 行为模型 §4.2）。多标签叠加取最大值规则在此不适用——ρ_off 用完整向量 | ✅ |
| **R14** | §七 D向量 | 疾病文件链路m偏移 | `List<数值>` 长度6 | 从疾病链路m偏移模式提取 | `List<数值>` | R7(V) | **提取算法未定义**——"从疾病文件链路m偏移模式提取"不具可计算性 | ❌ 未定义 |
| **R15** | §七 叙事标签 | axis(R10) | `枚举<1..6>` | `label = semantic_label[axis]` — 固定映射表 | `文本` ∈ {"威胁","渴求","冲动","自否定","过去创伤","疏离"} | 设计师(内容配置) | 映射表已定义。叙事素材选择依赖设计师，不进入自动化 | ✅ |
| **R16** | §八 SAN | SAN_base, eq_SAN, comp_SAN | `数值(SAN_base)` +2× `数值(?)` | `SAN = SAN_base + eq_SAN + comp_SAN` | `数值(SAN)` | R4(c(SAN)) | **eq_SAN/comp_SAN 的值域和来源未定义**。SAN_base 实体类型表目前仅含SAN/d_0/η，不含HP | ⚠️ 子项未定义 |
| **R17** | §九 旧表替换 | 4个文件中的旧值 | 旧格式(倍率 1.0-2.0×) | 机械替换为新η值 | 新格式(g_0 lunar / η_crit) | 4个引用文件的对应段落 | 替换清单已完备（5处×4文件）。**"月光强度驱动空间复用"→"η和场相变驱动"不是纯替换——空间复用规则的实质变化未展开** | ⚠️ 语义替换 |

## 缺口汇总

| # | 位置 | 严重度 | 描述 | 与v1对比 |
|----|------|--------|------|---------|
| G1 | §七 | ❌ | **D向量提取算法未定义**。"从疾病文件链路m偏移模式提取"不具可计算性 | 新发现 |
| G2 | §七 | ❌ | **Δ_situation_max[axis] 6个值全空** | 替代v1 G7(映射表空白) |
| G3 | §七 | ❌ | **direction_weight[axis] 6个值全空** | 新发现 |
| G4 | §七 | ❌ | **conc 除零** — Σ|V_i|=0 时（全零标签+无疾病）公式崩溃 | 新发现 |
| G5 | §一 | ⚠️ | **η上界** — κ>1时 η = g_0 × c × κ 可 > g_0=0.9。零号病人 η=0.86 未超，但 κ=4.3 × c(SAN=50)=0.22 → 0.9×0.22×4.3=0.85。极限情况 clmap？ | 新发现 |
| G6 | §五 | ⚠️ | **η=η_crit 精确边界** — η=0.3 被分到月光侧（≥），需要确认 | 新发现 |
| G7 | §三 | ⚠️ | **月相三档粒度** — 实际连续月相→3档的映射阈值未定义 | 同v1 G10 |
| G8 | §七 | ⚠️ | **argmax并列** — 多轴并列最大值时语义丢失 | 新发现 |
| G9 | §四 | ⚠️ | **新实体类型的κ** — 12类型的κ已有，但新类型创建规则未定义 | 新发现 |
| G10 | §八 | ⚠️ | **eq_SAN/comp_SAN 值域和来源** — 类型签名缺失 | 同v1 G12 |
| G11 | §九 | ⚠️ | **"η和场相变驱动空间复用"** — 不是纯替换，实质规则变化未展开 | 新发现 |

### v1→v2 对比

| v1状态 | v2状态 | 说明 |
|--------|--------|------|
| v1 G1 c锚点 ❌ | ✅ 闭合 | c_min/c_60/c_30/c_0 已锚定 |
| v1 G2/G3 κ子因子 ❌ | ⚠️→降级 | κ 改为查表，动态公式留给独立grilling |
| v1 G4 d_eff/Q_projection ❌ | ✅ 消去 | d_0 不进入η计算 |
| v1 G5 旧表换算 ❌ | ⚠️→降级 | 机械替换表已完备，语义替换未展开 |
| v1 G6 η_environment ❌ | ✅ 消去 | 难度缩放不在本次范围 |
| v1 G14 SAN阈值×浮动 ⚠️ | ⚠️ | 保留为设计特性 |

## 统计

- 总 trace 行数: 17
- ✅: 5 (R2, R4, R5, R13, R15)
- ⚠️: 8 (R1, R3, R6, R7, R8, R10, R16, R17)
- ❌: 4 (R9, R11, R12, R14)
- v1→v2: 8❌→4❌, 6⚠️→8⚠️

> ❌ 集中在 §七 ρ_off 三路输出的具体参数值（G1-G4），这些是内容填充性质的——6个语义方向的权重值需要设计师逐一定义。核心公式链（R1-R6）已闭合，仅剩边界条件微调。
