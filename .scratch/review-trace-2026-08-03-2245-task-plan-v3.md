# Review Trace: task-plan.md v2（第三轮 — 增量）

> 审查日期: 2026-08-03 22:45 | 审查范围: `.scratch/grilling-skill-v3/task-plan.md`（v2修订版）
> 方法: 符号执行 + 限界验证 | 前置检查: 无 | 对比基线: 第二轮 trace（12行/4❌/8⚠️/8缺口）

## 前置输入
- [x] 无 --pre-check
- [x] Layer 1.5 抽查: 跳过
- [x] 增量审查: 仅追踪第二轮修复涉及的新增/变更规则（6 行），不对全文件重 trace
- [x] 变更追踪: 对比第二轮 8 缺口 → 逐项验证修复状态

---

## 变更追踪（第二轮缺口 → 本轮验证）

| 上次缺口 | 严重度 | 修复位置 | 验证结果 |
|---------|--------|---------|---------|
| H1: 准入覆盖 | ❌ | §2.0.3 新增第4条件 | ✅ 闭合——4条件覆盖了"偏离现实""术语表""独创""高频使用"四类 |
| H3: 定义同步标准 | ❌ | §2.4 Layer 1 改用文件修改日期 | ✅ 闭合——不比较内容，避免了假阳性和自举 |
| H6: numerical_locations 初始填充 | ❌ | §4.0 新增 `build_term_registry.py` + 人类确认两步 | ✅ 闭合——方法明确：脚本 grep → AI 行号→段引用 → 人类确认 |
| H8: 去重时效性 | ❌ | §2.2 D5 新增三条前提条件 | ⚠️→✅ 闭合——三条条件明确。见下方新增 trace 行 #3 的边界分析 |
| H7: 日期格式 | ⚠️ | §2.0.2 ISO 8601 YYYY-MM-DD | ✅ 闭合 |
| H4: 入库责任人 | ⚠️ | §2.2 D7 AI Step 5 前列候选 → 用户确认 → Step 4 批次写入 | ✅ 闭合 |
| H5: 过渡期做法 | ⚠️ | §六 手动 git log + 仅结构检查 | ✅ 闭合 |

**第二轮 8 缺口全部闭合。本轮发现 1 个新 ⚠️。**

---

## Trace Table（仅本轮修复引入的新规则）

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|---|------|----------|----------|------|----------|------------|------|------|
| 1 | §2.0.3 准入条件#4: 高频术语 | 项目所有活跃 .md 文件的术语出现频率统计 | `{术语:文本, 出现次数:数值, 是否含数值/公式:枚举<是\|否>}` | grep 术语全项目 → 计数 + 判断是否 ≥2 文档且含机制语义 | `枚举<应入库\|不满足条件>` | D3（新假设评估）; D7（入库触发）; Step 4.7c（清扫发现） | "≥2 个活跃设计文档"——"活跃"的判定：排除决策树/垃圾桶/.scratch/废弃段落后仍有 ≥2 → 应入库。标准已隐式定义在 numerical_locations 的 grep 排除规则中。⚠️ 但"具有机制语义"需要判定——含数值/公式是充分条件，但"纯文本描述但承载机制定义"的术语（如"敌我同构"在多个文档中出现但未必有公式）是否被覆盖？边缘情况 | ⚠️ |
| 2 | §2.4 Layer 1 步骤 4: 文件修改日期检查 | `term_registry.json` updated 字段 + `git log` 源文件最后修改日期 | `{注册表日期:日期(ISO 8601), 源文件日期:日期}` | `git log -1 --format=%ad -- <file>` → 比较日期 → if 注册表日期 < 源文件日期 → WARN | `枚举<PASS\|WARN>` | Layer 2 AI辅助（WARN 项进入差异列表）; 过渡期手动模式 | **日期精度**: git log 返回 timestamp，注册表只有 date。如果同一天内源文件修改了但注册表也是同一天更新 → 无法区分先后。标注 WARN 是保守策略（宁可误报不漏报），可接受。**文件 touch 假阳性**: 源文件被 `git touch` 或格式化工具修改但内容实质不变 → WARN 误报。在过渡期手动模式 AI 可二次确认；脚本模式下需配合内容 hash——但当前规格未要求 hash。标注为已知限制 | ⚠️ |
| 3 | §2.2 D5 时效条件#2: 中间轮次未修改术语 | grilling 会话中每轮的决策记录 | `List<{轮次:数值, 修改的术语:List<术语ID>}>` | 当前轮去重引用前：扫描当前轮与前轮之间的所有轮次 → 检查是否有任何术语被修改 | `枚举<可复用\|不可复用（术语已变更）>` | AI（自检）; 用户（如果复用过期基线 → 后续提案基于错误约束） | **状态追踪的可行性**: AI 在 grilling 中是否有能力追踪"哪些术语在中间轮次被修改"？这只在 AI 显式记录了每轮的修改术语时可行。方案未要求 AI 维护这个追踪列表 → 自检#2 依赖 AI 元认知（"我记得中间修改过X"）→ 同 §2.3 自检的 v1 实现方式（AI 元认知判断）。**建议**: D5 时效条件#2 标注 `⚠️(AI)`——依赖 AI 记忆，v2+ 可改为形式化追踪（每轮基线输出时同时追加到会话级"已修改术语"列表） | ⚠️(AI) |

---

## 缺口汇总

| # | 位置 | 严重度 | 描述 |
|----|------|--------|------|
| I1 | §2.0.3 条件#4 | ⚠️ | **"具有机制语义"的判定边界模糊。** 含数值/公式 → 明确满足。但纯文本描述但承载核心机制的术语（如"敌我同构"在多文档中出现但无公式）的边缘情况不明确。建议：将"含数值/公式"作为充分条件而非必要条件——AI 在 grep 过滤时保留"含数值/公式 OR 在 CLAUDE.md 术语表中 OR 在 ≥3 个文档中出现"的术语 |
| I2 | §2.4 Layer 1 步骤 4 | ⚠️ | **文件修改日期检查的已知限制。** 同日修改无法区分先后 + touch 假阳性。二者均为低概率事件，当前"宁可 WARN 不漏报"的保守策略可接受。标注为已知限制——不阻塞实施，但后续 `validate_term_registry.py` 实现时考虑加文件内容 hash 做精确比较 |
| I3 | §2.2 D5 时效条件#2 | ⚠️(AI) | **"中间轮次未修改术语"依赖 AI 元认知追踪。** 与 §2.3 自检的 v1 实现方式同源——当前阶段 AI 自我监控，v2+ 形式化。不阻塞 v1 实施但需标注覆盖深度 |

---

## 统计

- 总 trace 行数: 3（本轮增量）
- ✅: 0
- ⚠️: 3（I1/I2/I3——全部为已知限制或边缘情况，非阻塞）
- ❌: 0

### 三轮趋势

| 轮次 | ❌ | ⚠️ | 缺口总数 | 缺口性质 |
|------|-----|-----|---------|---------|
| 第一轮 | 3 | 4 | 7 | 架构层——机制设计有漏洞 |
| 第二轮 | 4 | 4 | 8 | 实现层——规格不够细 |
| 第三轮 | 0 | 3 | 3 | 边缘层——已知限制，不阻塞实施 |

**结论**: 方案已达到可实施状态。3 个 ⚠️ 全部为已知限制，可在实施中自然解决或标注 `⚠️(AI)` 留待后续形式化。

---

*审查结束: 2026-08-03 22:45*
*关联: [task-plan.md](../.scratch/grilling-skill-v3/task-plan.md), [第二轮 trace](review-trace-2026-08-03-2230-task-plan-v2.md)*
