# Review Trace: 缩写术语防御方案

> 审查日期: 2026-08-05 23:15 | 审查范围: `.scratch/abbreviation-defense/修改方案.md`
> 方法: 符号执行 + 限界验证 | 前置检查: 无

## 前置输入
- 无 pre-check 输入
- H1 自动拉入: `term_registry.json`, `基础行动设计.md`, `CLAUDE.md`, `.claude/skills/grilling/SKILL.md`

---

## Trace Table

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| H5-1 | §一 立即修复 — 基础行动设计.md 表头 AP→动作 | 原表头 `AP` | `文本` (缩写) | 替换为完整词 `动作` | 表格列头 `动作` | 基础行动设计.md 所有读者, 参数速查表读者 | 原文中 "AP 1" 的 "1" 值不变——只是列名改。"动作 1" 语义等价于"消耗 1 个动作"——但"动作"本身可能触发歧义（"动作"=action? move?）| ⚠️ "动作"是否最优命名？备选: "通道消耗"/"动作数"。但与回合战斗流程中"M1/Broca 各 1 个动作"的表述一致——保留"动作"即可 |
| H1-1 | §一 1.2 term_registry.json 追加 AP | `term_registry.json` | `Dict<termID, TermEntry>` | 新增一条 deprecated 条目 | TermEntry {status: deprecated, old_model: "RPG Action Points"} | grilling §3.0 基线查询, §3.0b 缩写检查, 所有使用 term_registry 的流程 | AP 条目的 numerical_locations 字段指向当前文件中"AP"缩写出现的位置——但修复后该位置不再存在。numerical_locations 应指向"历史位置"还是"修复后为空"? | ⚠️ numerical_locations 语义: 记录的是"该术语在项目中以旧义出现的位置"→对于 deprecated 术语, 这些位置可能是已经修复的历史残留。建议在 AP 条目中标注"⚠️ 已修复——以下位置为修复前的历史记录" |
| H5-2 | §一 1.3 全项目扫描危险缩写 | 全项目 .md 文件 | `List<文件路径>` | grep 表格列头 ≤4 字符的缩写 → 逐个排查 | `List<缩写候选>` | §三 3.2 候选清单 | "≤4 字符"的阈值是否合理? "HP"/"SAN"/"Δm"是 2-3 字符——但已在注册表中。"m""β""η"是 1 字符——已在注册表。真正危险的缩写恰好是 2-3 字符 (AP/CD/HP)。阈值可以放宽到 ≤4 覆盖 HSP (不存在) 和 "角色" (4 个中文字但非缩写)。| ⚠️ 阈值应区分中英文。"角色"=2 个中文字≠缩写。规则应写为"≤4 个 ASCII 字符" |
| H3-1 | §二 §3.0b — 缩写展开检查的触发 | grilling 开始 + 当前话题涉及的文件 | `List<文件>` | 读取文件 → 提取 ≤4 字符缩写 → 逐项查 term_registry | `List<{缩写, 完整含义, 注册表命中, 冲突风险, 操作}>` | 同一次 grilling 内的每个追问的 AI 决策生成 | 如果文件非常多 (>5 个 md)，提取全部缩写可能很耗时。是否分级: 优先提取"本次讨论直接引用的表格/参数"中的缩写？还是严格要求全部文件? | ⚠️ 范围过大时降级策略未定义 |
| H4-1 | §二 §3.0b 分支: 缩写与 RPG 默认模型冲突 → 去缩写+入库 | 冲突判定 = 展开后的完整含义 ∩ RPG 默认模型表 ≠ ∅ | `布尔` | 分支: 冲突=TRUE → 改源文件 + 入库 deprecated; 冲突=FALSE → 可保留，入库 active | 修改后的源文件 + term_registry.json | 同 H3-1 | "RPG 默认模型表"是什么? 目前 term_registry.json 的 old_model 字段就是逐个术语记录。但**缩写展开后**是什么意思——"AP"→"动作消耗"→"动作消耗"是否在 RPG 默认模型中有对应? **默认模型表不在注册表里——只在 AI 的训练数据里**。这构成了一个循环: AI 来判断一个缩写是否触发旧模型——但 AI 就是会被旧模型劫持的那个。| ❌ 冲突判定依赖 AI 自身的"RPG 默认模型知识"——而 AI 就是被这个知识劫持的对象。人类必须参与最终裁决 |
| H2-1 | §三 3.2 候选缩写清单 | 项目全量扫描 + AI 预判 | `枚举<表名>` → 冲突风险标签 | AI 预分类: 冲突风险高/中/低 | `List<{缩写, 含义, 冲突风险}>` | §三 3.3 执行: 写入 term_registry_candidates.md → 用户确认 → 入库 | 清单中 "HP" 标记为"无——与传统 RPG 一致"。但这正确吗? 项目中的 HP 确实与传统 RPG 一致——但这本身就是一个需要确认的设计决策。**与默认模型一致 ≠ 不需要注册**——因为未来可能改变。| ⚠️ "无冲突"的缩写是否入库? 当前规则只强制入库"有冲突的"。但注册表也承担"确认无偏离"的语义——即使无偏离也应注册 |
| H5-3 | §四 CLAUDE.md 规则追加 | CLAUDE.md 当前内容 | `文本` | 在 §术语注册表即时更新 后追加新段 | `文本` (约 5 行 Markdown) | AI 每次会话的 CLAUDE.md 加载 | "任何项目文档表格列头或参数名中出现的缩写（≤4 字符）视为术语——必须出现在 term_registry.json 中"——这是一个**很强**的约束。当前有多少缩写不在注册表中? 十? 五十? 如果数量大——这条规则会导致每次新缩写被引入时都必须同步写 JSON——这是合理的渐进负荷还是过度约束? | ⚠️ 规则强度与实际差距未评估。建议先全量扫描确认缺口大小 |
| H5-4 | §五 regenerate-prompt | CLAUDE.md + grilling SKILL.md 修改后 | 修改后的文件 | 用户手动 /regenerate-prompt | 新的 AI 行为 | 本次 grilling #21 继续 | "需要重新生成 prompt"——但用户当前不执行的话, 本次会话中 §3.0b 缩写检查**不会自动生效**。这产生了窗口期: 方案被批准后到 regenerate 之间——AI 行为不变。| ⚠️ 窗口期风险: §五 仅提示了"需要", 未说明 regenerate 之前 AI 应如何自检(如: 手动遵循 §3.0b 步骤, 不依赖 prompt 加载) |
| H1-2 | 全文 H1 引用检查 | `基础行动设计.md`, `CLAUDE.md`, `grilling/SKILL.md`, `term_registry.json` | 四个被引用文件 | 确认四个文件均存在 + 被引用的段/节存在 | `List<引用<File>>` | 方案执行者 | 引用均指向现有文件——目标存在。但 `基础行动设计.md` 的表头修改后——引用该表头的参数速查是否需要同步? 方案 §1.1 声明了"参数速查表同步修改"——已覆盖 | ✅(浅) |
| H3-2 | §三 3.3 — 扫描→写入 candidates→用户确认→入库 | 全项目 .md 扫描 | `List<缩写候选>` | 写入 `term_registry_candidates.md` → 用户审查 → 批量追加到 `term_registry.json` | 修改后的 term_registry.json | 所有使用 term_registry 的流程 | `term_registry_candidates.md` 文件当前是否存在? 方案未显式声明其路径。建议路径: `data/term_registry_candidates.md` | ⚠️ 文件名已明确但路径缺失 |

---

## 缺口汇总

| # | 严重度 | 位置 | 描述 |
|----|--------|------|------|
| G1 | ❌ | §二 H4-1 冲突判定 | 缩写-RPG冲突的判定依赖 AI 自身的"RPG 默认模型知识"——而 AI 就是被这个知识劫持的对象。方案没有给出**人类在何时介入**的明确流程。建议: 缩写检查表输出后→用户逐项确认"冲突"判断→AI 仅执行机械操作（入库/去缩写） |
| G2 | ⚠️ | §二 H3-1 降级策略 | §3.0b 扫描范围: 当涉及文件 >5 时, 是否降级为"仅提取本次直接引用的表格/参数"中的缩写? 未定义 |
| G3 | ⚠️ | §三 §3.3 | `term_registry_candidates.md` 路径缺失——应显式写为 `data/term_registry_candidates.md` |
| G4 | ⚠️ | §五 | regenerate 前窗口期: AI 应手动遵循 §3.0b 步骤(不依赖 prompt 加载)。方案未说明 |
| G5 | ⚠️ | §三 H2-1 | "无冲突"缩写是否入库? "HP 无冲突→可保留"但"可保留"≠"可不在注册表中"。注册表同时承担"确认无偏离"功能——即使无冲突也应注册 |
| G6 | ⚠️ | §四 H5-3 | "任何缩写必须出现在注册表中"的强度——与当前实际缺口大小未评估。建议先扫描再定规则措辞 |

---

## 统计
- 总 trace 行数: 10
- ✅(浅): 1 / ⚠️: 5 / ❌: 1

## 建议
- **G1** (冲突判定依赖 AI) 是最严重的——需要人类介入环节。修复: 在 §3.0b 流程中追加"缩写检查表输出→用户逐项确认冲突→AI 仅执行入库/去缩写"
- G2-G6 为 ⚠️ 不阻塞——可在执行时自然解决
