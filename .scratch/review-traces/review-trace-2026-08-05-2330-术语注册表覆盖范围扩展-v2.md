# Review Trace: 术语注册表覆盖范围扩展 — v2

> 审查日期: 2026-08-05 23:30 | 审查范围: `.scratch/abbreviation-defense/修改方案-v2.md`
> 方法: 符号执行 + 限界验证 | 前置检查: 无

## 前置输入
- 无 pre-check 输入
- H1 自动拉入: `term_registry.json`, `基础行动设计.md`, `CLAUDE.md`

---

## Trace Table

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| H2-1 | §一 1.1 扫描范围 | 六个目录 `规则/` `实体/` `事件/` `空间/` `呈现/` `管线/` | `List<目录>` | 遍历目录下 .md 文件的**表格列头 + 参数速查表参数名列** → 提取 ≤4 ASCII 字符标识符 | `List<{标识符, 来源文件, 来源位置}>` | §一 1.4 入库执行 | 空白格/null 表头? 参数表可能不含参数名列(纯中文行)。"参数名列"的判定——表头中哪一列是"参数名"? 需要扫描时按语义判断("参数"/"名称"/"变量"等列头) | ⚠️ "参数名列"判定规则需在执行时具体化 |
| H2-2 | §一 1.2 入库标准 — 四种情形 | H2-1 产出的 `List<标识符>` | `枚举<标识符>` | 每个标识符归类: 有冲突/一致/独创/已废弃 | `枚举<冲突\|一致\|独创\|废弃>` → 确定 old_model 字段填充策略 | term_registry.json 条目生成 | "有 RPG 默认模型冲突"的判定——**谁来判定**? v1 方案中这是核心缺口 (G1)。v2 方案将此判定推迟到"扫描后用户确认"(§一 1.4)→**用户承担冲突判定**。正确——AI 不单独判定任何缩写的冲突状态 | ✅ 冲突判定推给用户——AI 仅做机械提取 |
| H4-1 | §一 1.2 四种情形的 old_model 字段 | 标识符归类结果 | `枚举<冲突\|一致\|独创\|废弃>` | 分支: 冲突→记录 old_model+deviation; 一致→`—（无偏离）`; 独创→`—（项目独创）`; 废弃→记录 old_model+废弃原因 | `文本` (old_model 字段值) | term_registry.json 对应条目 | "一致"情形——如 HP 与 RPG 定义一致——old_model 写"无偏离"。但项目 HP 真的与 RPG 完全一致吗? 项目中 HP=50(±10), 战斗长度 4-20 回合, HP↔SAN 可互转——这些都不是"传统 RPG HP"的用法。**"HP 无偏离"可能是一个错误的判定**——需要仔细审查 | ⚠️ "一致"判定也可能需要用户介入——不仅是冲突需要审查 |
| H5-1 | §一 1.4 入库执行 | H2-1 + H2-2 产出: `Dict<标识符, 注册表条目>` + 用户确认 | `List<TermEntry>` (新增条目) | 批量追加到 `term_registry.json` 的 `terms` 字段 | 修改后的 term_registry.json | grilling §3.0 基线查询, 所有其他读取 term_registry 的流程 | term_registry.json 已是 909 行。新增条目数量预估? 如果扫描后新增 20-30 条——合理。如果新增 100+ 条——文件大小是否可控? | ⚠️ 批量追加后文件大小未预估 |
| H1-1 | §二 基础行动设计.md 表头修改 | `基础行动设计.md` 表头行 + 参数速查表 | `文本` ("AP") | AP → 动作 | 表头列名 "动作" | 基础行动设计.md 所有读者, 参数引用方(核心机制.md §2.5/§4.1) | 参数速查表中"AP"出现多次——需全部替换。核心机制.md §十一 参数速查表是否也用了"AP"? | ⚠️ 跨文件一致性: 核心机制.md 参数速查表需同步检查 |
| H1-2 | §三 CLAUDE.md 规则追加 | CLAUDE.md 当前内容 | `文本` | 在"术语注册表即时更新"段后追加覆盖范围声明 | CLAUDE.md 新段 | 每次会话加载 CLAUDE.md 时的 AI 行为 | 新规则"不在注册表中的短标识符→默认视为未定义"——**此规则强度是否需要豁免**? 例如: 工具脚本中的变量名/grep 输出中的缩写/用户自然语言中的缩写——这些不应被此规则约束。规则范围需限定为"设计文档的表格列头和参数名" | ⚠️ 规则范围需限缩——仅限"设计文档表格列头和参数名列"，不含脚本变量/对话文本/用户提问 |
| H3-1 | §四 不需要修改的部分 — 排除项 | grilling SKILL.md, review-plan SKILL.md | `引用<文件>` | **不**修改——声明排除 | — | — | 确认这两个文件确实不需要修改: grilling SKILL.md 的 §3.0 **已经**会查询 term_registry.json——扩展注册表后自动生效。review-plan SKILL.md 不涉及术语查询——也正确。 | ✅(浅) |

---

## 缺口汇总

| # | 严重度 | 位置 | 描述 |
|----|--------|------|------|
| G1 | ⚠️ | §一 H4-1 | "无偏离"判定可能错误——HP 在项目中有 HP↔SAN 互转、基线 50±10 等非传统用法。建议: **所有入库缩写均标注 old_model**（即使"一致"也写明传统 RPG 用法），不自动判定"无偏离" |
| G2 | ⚠️ | §一 H5-1 | 批量追加后 term_registry.json 大小未预估。建议先 dry-run 扫描确认新增条目数 |
| G3 | ⚠️ | §二 H1-1 | 核心机制.md 参数速查表是否也用了"AP"缩写——需同步检查 |
| G4 | ⚠️ | §三 H1-2 | 规则范围需限缩——"不在注册表中的短标识符→默认视为未定义"的适用范围应限定为"设计文档的表格列头和参数名列" |

---

## 统计
- 总 trace 行数: 7
- ✅(浅): 1 / ⚠️: 4 / ❌: 0
- **零硬缺口** — 全部为 ⚠️ 软化，可在执行时自然消解
- v1 方案的核心 ❌ (G1 冲突判定依赖 AI) 已消解——v2 将冲突判定推给用户确认

## 对比 v1

| v1 缺口 | v2 状态 |
|---------|--------|
| ❌ G1 冲突判定依赖 AI | ✅ 已消解——用户确认环节 |
| ⚠️ G2 降级策略 | ✅ 已消解——v2 无 §3.0b 流程 |
| ⚠️ G3 candidates 路径 | ✅ 已消解——v2 无 candidates 文件 |
| ⚠️ G4 regenerate 窗口期 | ✅ 已消解——v2 不需要新增流程 |

## 建议
- 方案可立即执行——0 ❌
- 执行时注意 G1 (不自动判定"无偏离")和 G4 (规则范围限缩)
