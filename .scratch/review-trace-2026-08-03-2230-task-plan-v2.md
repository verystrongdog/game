# Review Trace: task-plan.md v2（第二轮）

> 审查日期: 2026-08-03 22:30 | 审查范围: `.scratch/grilling-skill-v3/task-plan.md`（v2 — 术语注册表驱动方案）
> 方法: 符号执行 + 限界验证 | 前置检查: 无 | 对比基线: `review-trace-2026-08-03-2215-task-plan.md`（第一轮）

## 前置输入
- [x] 无 --pre-check
- [x] Layer 1.5 抽查: 跳过（无前置脚本）
- [x] 被引用文件自动拉入: `.claude/skills/grilling/SKILL.md`（当前结构已确认）, `CLAUDE.md`（术语表段已确认）
- [x] 变更追踪: 对比第一轮 trace（12 行 / 3 ❌ / 8 ⚠️ / 7 缺口）

---

## 变更追踪（第一轮缺口 → 本轮状态）

| 上次缺口 | 严重度 | 本次状态 | 备注 |
|---------|--------|---------|------|
| G1: 关键术语标准 | ❌ | ✅ 已解决 | 术语注册表替代 AI 识别——术语预注册，查询即可。无需 AI 定义"关键" |
| G2: 旧模型不可验证 | ⚠️ | ✅ 已解决 | old_model + deviation_reason 由人类入库时填写一次，所有会话复用 |
| G3: 独创术语边界 | ❌ | ✅ 已解决 | old_model 字段标注 `—（与项目定义一致，无偏离）`，人类决定一次 |
| G4: 新假设流程分歧 | ⚠️ | ⚠️→✅ 已解决 | D3：grilling 结束后评估入库，三标准明确（§2.0.3） |
| G5: 兼容性判定自举 | ❌ | ✅ 已解决 | 改为双层：Layer 1 脚本（结构层，确定性）+ Layer 2 AI辅助（默认标注"不确定"）。AI 不单独判定 |
| G6: 重复执行效率 | ⚠️ | ⚠️→✅ 已解决 | D5：允许去重引用。但引入新缺口 H8（见下） |
| G7: 编号矛盾 | ⚠️ | ✅ 已修复 | D6 明确：§3.0 插入在 §3.1 之前 |

**第一轮 7 缺口全部解决。本轮发现 8 个新缺口（H1-H8），其中 3 ❌ 5 ⚠️。**

---

## Trace Table（仅追踪新增/变更规则）

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|---|------|----------|----------|------|----------|------------|------|------|
| 1 | §2.0.1 注册表结构: terms 对象 | 设计者的术语定义 | `Dict<术语ID:文本, 条目:Obj>` | 将项目核心术语整理为结构化 JSON | `Dict<术语ID, {definition:文本, source:文本, old_model:文本, deviation_reason:文本, status:枚举<active\|deprecated\|under_revision>, numerical_locations:List<引用<文档§>>, added:日期, updated:日期}>` | AI (§3.0 查询); Layer 1 脚本 (§2.4); review-plan H1 (§2.5); 校验脚本 | **numerical_locations 的日期格式**未指定（"2026-08-03"）——ISO 8601？有无时区？值域未声明 | ⚠️ |
| 2 | §2.0.2 status 字段值域 | —（类型定义行） | — | 定义 status 三个合法值 | `枚举<active\|deprecated\|under_revision>` | 注册表条目 (§2.0.1); §4.0 首批清单; Step 4.7c 兼容性检查 | `deprecated` 术语的 numerical_locations 如何处理？废弃术语可能仍被旧文档引用 → 这些引用是否需要检查？方案未说明（见 H2） | ⚠️ |
| 3 | §2.0.3 准入标准: 三条件 | —（类型定义行） | — | 判定术语是否应加入注册表 | `枚举<是\|否>`（应入库/不应入库） | D3（新假设评估）; D7（入库触发）; §4.0（首批填充） | **缺口 H1**: 三条件的覆盖完备性——"在现有设计文档中高频使用但不在 CLI 术语表也不在风险表的术语"是否被覆盖？（如"激活点"→现在叫聚焦容量，但"激活点"这个词仍在文档中大量存在） | ❌ |
| 4 | §2.1 3.0 工作流: 扫描问题→查询注册表→填表 | 当前 grilling 提问 + `term_registry.json` | `输入: 文本(用户问题) + Dict(注册表)` | 1. AI 扫描问题涉及的概念; 2. 逐概念查询注册表; 3. 命中→填入基线; 4. 未命中→标记新假设 | `List<{术语:文本, 定义:文本, 来源:文本, 旧模型:Optional<文本>, 状态:枚举<注册表中\|新假设>}>` | §3.2 事实锚定（在基线约束集内引用）; §3.3 自检; 用户审阅 | "扫描问题涉及的概念"仍是 AI 操作——虽然范围被注册表圈定（只在注册表内查找），但如果问题涉及的概念在注册表中但没有被 AI 匹配到（漏查），仍然可能漏掉有旧模型风险的术语。注册表解决了"不知道定义"但未完全解决"不知道这个词需要查" | ⚠️ |
| 5 | §2.1 3.0 格式模板: 检查行 | 基线表 (#4) | `List<{术语, 状态:枚举<注册表中\|新假设>}>` | 逐术语判定来源 → 输出检查清单 | `List<{术语, 检查结果:枚举<✅\|⚠️新假设>}>` | 用户审阅; D3（入库评估触发） | 检查行中的"新假设"标注后——谁负责在 grilling 结束后评估是否入库？AI 提醒？用户主动？方案 D3 说"grilling 结束后评估"但未指定谁发起、何时发起（见 H4） | ⚠️ |
| 6 | §2.2 D5: 去重引用 | 当前轮基线表 + 前轮基线表 | `两个 List<基线条目>`集合 | 比较术语集 → 完全相同 → 简写为"复用第N题基线" | `文本`（简写声明 + 新增术语列表）或完整表格（术语集不同） | 用户（减少阅读重复）; AI（约束集仍有效） | **缺口 H8**: 第 N 题可能发生在多轮之前——如果中间轮次修改了某术语的定义（grilling 中的临时共识），复用第 N 题的基线可能过期。需要"同一次 grilling 会话内，该术语在注册表中的 status 未变更为 under_revision"的前提条件 | ❌ |
| 7 | §2.2 D7: 入库触发三条件 | grilling 中产生的新信息 | `枚举<新核心术语\|定义被修改\|清扫发现未注册>` | IF 任一触发 → 术语必须加入/更新注册表 | `引用<term_registry.json>`（需更新的条目） | Step 4 一致性清扫; 后续 grilling 会话 | "新核心术语"的"核心"由谁判定？Grilling 结束时 AI 建议列表 → 用户确认？方案未指定（见 H4） | ⚠️ |
| 8 | §2.4 Layer 1 脚本: 结构检查 | `term_registry.json`（含 numerical_locations） + 项目所有活跃文档 | `Dict<术语ID, List<引用<文档§>>>` | 1. 遍历被修改术语的 numerical_locations; 2. 检查每个引用指向的文档+段落是否存在; 3. 检查注册表 definition 是否与源文件最新段落一致 | `List<{术语, 位置, 检查类型:枚举<引用有效\|定义同步>, 结果:枚举<PASS\|FAIL\|WARN>, 详情:文本}>` | Layer 2 AI辅助 (#9); 校验脚本体系 | **缺口 H3**: "检查 definition 与源文件段落是否一致"——"一致"的标准未定义。逐字匹配 → 源文件微小编修注册表立即 FAIL（假阳性）。语义匹配 → 回到 AI 不可靠问题。需要定义具体的检查粒度（如"注册表 definition 必须包含源文件段落的关键短语"？） | ❌ |
| 9 | §2.4 Layer 2 AI辅助: 差异审查 | Layer 1 脚本输出 (#8) | `List<{位置, 旧数值, 检查结果}>` | 对每个差异: 列出旧数值 + 新定义 + 可能冲突 → 标注兼容性 | `List<{位置, 旧数值, 新定义, 兼容性:枚举<兼容\|需修订\|不确定>, 操作建议:Optional<文本>}>` | 人类复查（对"不确定"项）; Step 4 一致性清扫报告 | Layer 1 脚本尚未实现 → 在当前阶段，Layer 2 的输入从哪来？方案 §六将 Layer 1 实现标记为延迟。但 Step 4.7c 在 Layer 1 就绪前如何运作？回退到手动 grep？需要显式写替代路径（见 H5） | ⚠️ |
| 10 | §2.4 numerical_locations 维护: 注册时首次填充 | 新术语 + 项目所有活跃文档的 grep 结果 | `{术语ID:文本} + List<文档路径>` | grep 术语全项目 → 过滤出含数值/公式的位置 → 填入 numerical_locations | `List<引用<文档§>>` | term_registry.json（作为新条目的字段） | **缺口 H6**: 首批 24 条术语的 numerical_locations 初始填充——谁来执行？是一次性批量操作（脚本）还是逐术语手工填充？§4.0 列出了术语名但未给出 numerical_locations，也未指定填充方法 | ❌ |
| 11 | §2.4 numerical_locations 维护: 定义变更时遍历复查 | 注册表中某术语的新 definition + 该术语的 numerical_locations | `{新定义:文本, old_locations:List<引用>}` | 遍历每个 location → 检查该处的数值/公式是否与新定义兼容 → 更新/标记 | `List<引用>`（仍有效的）+ `List<{引用, 操作}>`（需修订的） | term_registry.json（updated 字段 + 修订后的 numerical_locations） | 这需要 AI 做语义兼容性判断——"此处数值是否与新定义兼容"。与 Layer 2 有相同的自举风险。但与 Layer 2 不同——这里可以回退到"全部标记为需人类复查"（定义变更 = 所有 consumers 都可能受影响 = 保守策略） | ⚠️ |
| 12 | §2.0.2 numerical_locations 类型 | —（类型定义行） | — | 定义单个 location 的格式 | `引用<文档路径 §段落ID>`（段引用格式，如 `规则/核心机制.md §6.3`） | 注册表条目 (#1); Layer 1 脚本 (#8); grep 结果 (#10) | 段引用（§N）随文件编辑漂移——§6.3 可能在编辑后变成 §6.4。注册表需要定期重新验证段引用的有效性。方案未说明失效检测机制 | ⚠️ |

---

## 缺口汇总

| # | 位置 | 严重度 | 描述 |
|----|------|--------|------|
| H1 | §2.0.3 | ❌ | **准入标准的覆盖完备性。** 三条件（现实偏离/术语表/独创核心）未覆盖"在现有文档中高频使用但不在以上三类的术语"。例如"激活点"已改名为"聚焦容量"，但旧词仍在文档中被引用——是否需要注册来追踪这个迁移？建议增加第四条件：**"术语在 ≥2 个活跃设计文档中被引用且具有机制语义"** |
| H2 | §2.0.2 | ⚠️ | **deprecated 术语的 numerical_locations 语义。** status=deprecated 的术语（态度/PAD/7驱动/5模块/CPM）仍有 numerical_locations。这些位置应该标记为"已知废弃引用"还是从 numerical_locations 中清空？方案未区分 |
| H3 | §2.4 Layer 1 | ❌ | **"定义同步"检查的一致标准缺失。** Layer 1 脚本第 3 步检查"注册表 definition 是否与源文件段落一致"。逐字匹配 = 假阳性洪水。语义匹配 = 回到 AI 不可靠。需定义中间方案——如"检查注册表 definition 中的关键词是否在源文件段落的最近一次编辑后仍然出现"或"检查源文件段落的最近修改日期是否晚于注册表 updated 日期" |
| H4 | §2.2 D3/D7 | ⚠️ | **"grilling 结束后评估入库"的责任人不明确。** D3 说"grilling 结束后评估"，D7 说"入库触发"。谁发起评估？AI 在 Step 5 关 issue 前列出候选清单？用户主动？如果都不做，新术语就漂在注册表外，3.0 的"查询注册表"会反复标记为"新假设" |
| H5 | §2.4 Layer 2 | ⚠️ | **Layer 1 延迟期间的替代路径未定义。** §六将 Layer 1 脚本实现标记为延迟。但在脚本就绪前，Step 4.7c 如何运作？是回退到手动 grep → AI 辅助（原方案）→ 还是完全跳过 Layer 1？当前方案写了两层但只有 Layer 2 可用 → 需写清楚过渡期做法 |
| H6 | §2.4 维护 + §4.0 | ❌ | **首批 24 条术语的 numerical_locations 初始填充方法缺失。** §4.0 列出了术语名和 status，但没有给出任何一条术语的 numerical_locations 值，也没有指定填充方法（是脚本批量生成还是手工逐条填写）。**这是阻塞实施的最直接缺口**——没有 numerical_locations，Layer 1 脚本和 Step 4.7c 无法运作 |
| H7 | §2.0.1 | ⚠️ | **日期格式未标准化。** `added` 和 `updated` 字段示例为 `"2026-08-03"`，但未声明格式标准（ISO 8601? YYYY-MM-DD? 是否有时区？）。管线数据规范应明确 |
| H8 | §2.2 D5 | ❌ | **去重引用的时效性。** `术语集复用第N题基线` 隐含假设：第 N 题的基线在当下仍然有效。但如果中间轮次的讨论修改了某术语的临时共识（如 grilling 中途 user 推翻了某个假设），复用过期基线会导致错误的约束集。需加前提条件："同一次 grilling 会话内，该术语在注册表中的 status 未变更为 under_revision，且未被本轮 grilling 的决策修改" |

---

## 统计

- 总 trace 行数: 12（本轮增量，覆盖所有新增/变更规则）
- ✅: 0（无完全闭合的行——每行都有边界条件可追问）
- ⚠️: 8（#1, #2, #4, #5, #7, #9, #11, #12）
- ❌: 4（#3, #6, #8, #10）
- 缺口: 8 项（H1-H8），其中 4 项 🔴 阻塞（H1/H3/H6/H8），4 项 🟡 警告（H2/H4/H5/H7）

### 与第一轮对比

| 指标 | 第一轮 | 第二轮 | 变化 |
|------|--------|--------|------|
| Trace 行数 | 12 | 12 | — |
| ✅ | 1 | 0 | -1（新规则的边界条件更严格） |
| ⚠️ | 8 | 8 | — |
| ❌ | 3 | 4 | +1（注册表引入新复杂度） |
| 缺口 | 7（全为原方案缺陷） | 8（全为新方案的实现细节缺口） | +1 |

**关键发现**：第一轮的 7 个缺口是**架构层**缺陷（机制设计本身有漏洞）。第二轮的 8 个缺口是**实现层**缺陷（机制设计正确，但规格不够细：谁做、什么时候做、格式标准、过渡期做法）。这是进展——解决了"对不对"的问题，现在面对的是"怎么做到"的问题。

---

*审查结束: 2026-08-03 22:30*
*关联: [task-plan.md](../.scratch/grilling-skill-v3/task-plan.md), [第一轮 trace](review-trace-2026-08-03-2215-task-plan.md)*
