Title: 看看这段聊天

URL Source: https://chatgpt.com/s/t_6a8f23e1005c81919b662d257de2d8d5

Published Time: 2026-08-26T17:35:29.001Z

Markdown Content:
三点全部确认。不过我建议把第 ① 点的表述稍微钉死：**M/A 是 benchmark 的规范输入，不是从系统自动推断出来的“事实语义”**。否则不同类别之间容易产生“研究者怎么选都可以”的自由度。

### D10 定案

**① 三类分治规则：确认。**

冻结为：

| 类别 | M/A 规则 |
| --- | --- |
| 负类 #1–#4 | `M=∅, A=∅` |
| 正类 #5–#7 | 按构造指南翻译得到的规范 `M/A` |
| 判别组 #8–#10 | 按预先声明的结构规范选择 `M/A`，不声称具有自然语义 |

这里负类的 `SM′=0` 确实是**规范选择 + 退化条款**产生的结果，因此不能把它解释成“证明随机/前馈/电路/通信系统不存在任何自模型结构”。它只能支持 benchmark 中定义的这个测试命题。

* * *

### ② CA / RL 的具体选择：确认

**CA：**

并且 Rule30 / Rule0 使用完全相同的 M/A。

这样 CA 的测试重点是：

而不是人为构造一个“外显动作”。

**RL：**

这个选择保留 RL 本身天然存在的状态→动作结构，因此比把 RL 强行处理成 `A=∅` 更有信息量。

* * *

### ③ paired M/A 一致 + 预期非验收：确认

这两条应该正式成为 D10 的硬约束。

**Paired：**

如果目标机制本身导致 `I` 的成员变化，例如：

那么这属于**被测机制的结构变化**，不是 M/A 选择漂移。

同样：

不能因为 Rule0 最终可能判 0，就给它换一套 M/A。

* * *

### 最重要的一点：预期 ≠ 验收

我同意你特别强调这一点，而且建议写成 benchmark 的总原则：

> **系统类别、representative/control 标签以及预期 CS′ 结果均不得参与判定计算；判定器只读取系统的 `step_fn` 与预先冻结的 M/A 规范输入。**

因此：

但实际：

不能修改 B_cpg，也不能修改阈值或 M/A 来“救回来”。

它就是一个正类中的失败观测；只有**三个正类全部为 0**时，才触发 `F−`。

* * *

## D10 最终冻结

这样脚本 3 就不会出现一个很隐蔽的问题：**为了让某个系统得到预期结果而反过来选择 M/A。**

到这里，D1–D10 已经把脚本 3 的判定框架、时间窗口、核、局部化、正例、paired 和 failure line 基本锁完了。

* * *

If you want, I can:

*   Summarize D10 policy key points in TL;DR bullets

*   Explain the significance of paired M/A consistency and expectation vs acceptance

*   Transform D10 frozen rules into an editable checklist for implementation
