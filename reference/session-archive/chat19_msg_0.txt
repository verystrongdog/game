Q8：**①确认；②确认。Q8 PASS。**

这轮可以作为 #103 的**职责边界与收口条款**。Q1–Q7 已经把“怎么走时间”和“谁消费月相”定义清楚，Q8 再把“谁不能碰什么”锁住，接口层就基本闭合了。

### ① 职责划分：PASS

我确认你列出的所有权边界。

尤其有三条需要特别保留：

- **#103 不拥有具体校准数值**：只规定 schema 和消费契约，`E_c/E_th/q̂` 的数值由 #35 产出。
- **#103 不拥有 E3/E4 瞬态动力学**：`F_local/F_collective`、`ΔE`、`q̂` 增强都不进入 `moonState`。
- **#103 不拥有最终图定义**：`G_H` / `graph_identity` 由 #1/#70 管线提供，#103 只要求校准产物与运行时图身份一致。

这样职责关系可以压缩成：

```text id=\"n2c7vz\"
                 #103 接口层
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
     时间轴        moonState       消费规则
        │             │             │
        └─────────────┴─────────────┘
                      │
             只消费外部已定义产物
                      │
       ┌──────────────┼──────────────┐
       ▼              ▼              ▼
      #35            #101           #1/#70
    校准/实验       神经落点       图/管线
```

而不是让 #103 最后变成一个“所有月光相关机制的总控制器”。

### ② 暂停 / 加速：PASS

**暂停不推进时间**完全符合 Q1/Q4。

特别是“加速”这一条，我赞成直接作为**禁止性不变量**写进去：

> **所有游戏时间推进必须通过已定义的休息推进路径发生；任何绕过休息机制直接修改 D 的时间加速机制均违反接口契约。**

因此：

```text id=\"1o8y3v\"
UI 暂停       → D 不变
战斗暂停      → D 不变
存档/退出     → D 不变
读档继续      → D 不变
战斗回合      → D 不变
死亡/复活     → D 不变

夜晚第三次休息
             → D + 1
             → λ(D) 更新
```

如果未来真的要加入“快速旅行/时间加速/跳过若干天”等机制，不能直接改 `D`；应该先单独开 grilling，定义它究竟等价于多少次合法的游戏日推进，以及期间哪些事件需要被消费。

---

# Q8 最终裁决

> **Q8 PASS：#103 的职责限定为游戏日/时段/月相时间轴契约、运行时消费规则、`moonState(D)` 查询契约及 calibration schema，以及基于 #102 结果的绝热性有效性声明。具体校准数值与 E3/E4 动力学归 #35；R_i 神经落点归 #101；g(SAN,state) 机制调制归 E7/#35；空间月光分布与夜晚危险性机制归空间维度；难度权重归 #35；呈现层时段月光表观归呈现层；w_ij 归 E5；最终 G_H/graph_identity 归 #1/#70 管线。**
>
> **暂停、存档/退出、读档、战斗及死亡/复活均不推进 D；任何绕过休息推进路径直接修改 D 的“时间加速”机制均违反 Q4 的 λ=f(D) 不变量，在未来若需要该机制必须另行 grilling。**

**Q8 PASS。**

## #103 到这里可以收口

Q1–Q8 已经形成完整的接口契约：

**游戏日定义 → 时间分层 → 刷新点 → D/相位锚定 → 消费 API → 校准安全 → schema/图兼容 → 职责与暂停/加速边界。**

因此我建议 **不要继续为了“完整”而人为增加 Q9**。如果没有新的冲突或未决自由度，下一步应该是把 Q1–Q8 **落盘到 #103 + §11.6 + term_registry，并做实现侧验收**，而不是继续 grilling。

---

If you want, I can:

- 继续讨论Q9及潜在自由度问题
- 详细论述非休息时间加速机制设计
- 安排Q1–Q8到#103和相关文档的落盘计划