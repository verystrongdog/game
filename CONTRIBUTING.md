# 协作指南

> 这个仓库有两位贡献者：**你（人类设计师）** 和 **AI（设计助手指令由 Claude Code 执行）**。本文档定义二者如何通过 GitHub 协作推进游戏设计。

## 角色分工

| | 人类 | AI |
|------|------|-----|
| 设计决策 | ✅ 最终决定权 | 追问、建议、呈现选项 |
| 写设计文档 | 审核和批准 | ✅ 起草和修改 |
| 写模拟脚本 | 定义验证目标 | ✅ 实现和运行 |
| Git 操作 | `git push`（见下方说明） | `git add/commit`、创建 PR |
| Issue 管理 | 创建 Grilling Issue | 在 Issue 中逐条追问 |
| PR Review | ✅ 最终审核 | 自检清单 |

## 工作流程

```
1. 人类创建 Grilling Issue（或 AI 在对话中发起）
2. AI 读取六维状态 → 确认话题边界 → 逐条追问
3. 达成共识后 AI 写设计文档 → 创建 PR
4. 人类 Review PR → Merge
5. AI 更新路由表和 memory
```

## AI 提交规范

AI 在以下情况会自主 `git add` + `git commit`：
- 完成一个独立设计文档
- 完成一个可运行的模拟脚本
- 完成一批关联修改
- 完成项目结构清理

AI **不会** 自主执行：
- `git push`（需要人类确认远程同步）
- `git reset --hard` / `git rebase` / `git push --force`（危险操作）
- 在 `main` 分支上做实验性改动（应先开分支）

## 分支策略

- `main` — 唯一真相源，永远可发布
- `feat/<功能名>` — 新子系统或大规模重构
- `exp/<实验名>` — 不确定方向的实验

工作完成后合并回 `main`。

## Issue 使用

| 模板 | 用途 | 谁来创建 |
|------|------|---------|
| Grilling | 设计评审 | 人类或 AI |
| 设计矛盾 | 发现不一致 | 任何人 |
| 子系统提案 | 新系统提议 | 人类 |

AI 会在 Issue 评论中逐条追问，每次一条，等待人类回复。

## PR 使用

每个 PR 必须勾选 PR 模板中的所有一致性检查项，包括：
- grep 废弃术语清理残留引用
- 交叉引用路径验证
- 路由表和项目总览同步检查

## 你的核心责任

1. **在 Grilling Issue 中回应 AI 的追问**——这决定了设计质量
2. **Review 每个 PR**——AI 会起草文档，但只有你能判断"这是对的"
3. **定期 `git push`**——AI 提交后需要你把 commit 推到 GitHub
4. **保持 Issue 列表干净**——关闭已完成的、标记 `wontfix` 的

## AI 的核心责任

1. **Grilling 不做假设**——设计决策必须逐一追问，不等用户说"好"就不继续
2. **写入后必须清扫**——每次修改设计文档后 grep 废弃术语，更新交叉引用
3. **不绕过禁止事项**——CLAUDE.md 的禁止事项是硬约束
4. **提交小步清晰**——每批提交一个主题，commit message 用中文
