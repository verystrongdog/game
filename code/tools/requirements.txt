# 校验器依赖（P4a 环境锁定）
#
# 13 个 validate_*.py 里只有两个需要第三方库：
#   · validate_disease.py            → PyYAML（读疾病目录的 YAML frontmatter）
#   · validate_ceiling_generator.py  → numpy（它要真的跑一次 gen_modulation_ceiling.py，#134）
# 其余 11 个 + run_all_checks.py + md_utils.py 纯标准库。
PyYAML==6.0.3
numpy==2.2.6          # 与 code/sim/requirements.txt 同 pin
