# Review Trace: task-plan.md

> 审查日期: 2026-08-01 20:05 | 审查范围: `.scratch/enemy-stat-pipeline/task-plan.md`
> 方法: 符号执行 + 限界验证 | 前置检查: 无

## 前置输入
- [x] 无 --pre-check
- [x] H1 被引用文件自动拉入:
  - [x] `实体/敌人与事件.md` §4.1 §4.2（HP/SAN 范围 + Stat Block 结构）— 已读，验证通过
  - [x] `data/connectivity/personality_tag_links.json` — 6 标签 × 15 核心链路，验证通过
  - [x] `实体/疾病目录/GAD.md` `MDD.md` `偏执型精神分裂症.md` — 功能域配置表格式一致，验证通过
  - [x] `规则/核心机制.md` §2.1 — 激活点公式 `4 + floor(SAN/30)`，验证通过
  - [x] `data/connectivity/link_registry.json` — 364 条目，验证通过
  - [x] `规则/技能树系统/NPC AI 行为模型.md` §4.2 — 多标签叠加"取最大值（不累加）"，验证通过

## Trace Table

| # | 规则 | 输入·来源 | 输入·类型 | 变换 | 输出·类型 | 输出·消费者 | 边界 | 判定 |
|----|------|----------|----------|------|----------|------------|------|------|
| 1 | §3.1 病情等级映射 | 病历 `诊断印象.严重度: CGI-S N` | `枚举<3\|4\|5\|6\|7>` | CGI-S 3-4→轻度, 5→中度, 6-7→重度 | `枚举<轻度\|中度\|重度>` | §3.2 m倍率选择; §3.3 HP/SAN查表; Stat Block identity层 | CGI-S <3 或 >7 无处理。CGI-S 3 vs 4 同为"轻度"无区分 | ✅ |
| 2 | §3.2 m偏移公式 | (1) 疾病文件 `功能域配置.m偏移(轻/中/重)` (2) 疾病文件 `CGI-S 域扩展.m倍率(参考)` (3) `personality_tag_links.json` 标签偏移 | (1) `Dict<链路ID, 数值>` (2) `数值` (3) `Dict<链路ID, 数值>` | `final_m = 0.0 + Δm_disease × CGI-S倍率 + Δm_tag` | `Dict<364链路ID, 数值[-1.0, 1.0]>` | §4 父类行为模板（条件触发）; §5.2 step 4; prebake管线 | **⚠️ 疾病m偏移值是否已含CGI-S倍率？** GAD.md `功能域配置` 给出三级具体值(+0.1/+0.3/+0.5)，`CGI-S域扩展`又给出 m倍率(×0.5/×1.0/×1.5)。若功能域配置值已是最终值→不应再乘倍率。若功能域配置是"基数"→需乘倍率，但 轻×0.5 后 VTA→NAcc = -0.05 极低。**两个表的关系未定义** | ⚠️ |
| 3 | §3.2 多标签叠加 | `personality_tag_links.json` 标签偏移 × 标签数(1-2) | `List<Dict<链路ID, 数值>>` (长度 1-2) | 取绝对值最大者，不累加 | `Dict<链路ID, 数值>` | §3.2 m偏移公式 `Δm_tag` 项 | 标签数=0（病历中标签字段为空）→ 无处理。标签数>2 → 病历格式约束为1-2个，不应出现 | ✅(浅) |
| 4 | §3.2 m天花板 | 最终 m 值（全部链路） | `Dict<链路ID, 数值>` | 拮抗对链 m总和 ≤ 1.4。超过时如何处理？截断？拒绝？等比缩放？ | `Dict<链路ID, 数值>` | prebake管线 采样校验 | **变换方法未定义。** "≤1.4"是约束条件，但超限时的修正算法未说明 | ❌ |
| 5 | §3.2 行为表组装 (杂兵) | (1) 父类默认模板 (§四) (2) 疾病文件 §行为覆盖 | (1) 行动偏好权重 + 情境响应优先级 (2) 条件→行为覆盖规则 | 父类模板 → 疾病覆盖层叠。冲突时疾病覆盖替换父类对应项 | 行为表 | §5.2 step 5-6; prebake管线 | 父类模板和疾病覆盖的同字段冲突——文档说"覆盖"（替换），但未说明是整行替换还是字段级merge | ✅(浅) |
| 6 | §3.2 行为表组装 (精英) | (1) 父类模板 (2) 疾病覆盖 (3) 病历 MSE/风险评估特殊字段 | 同上 + 病历文本字段 | 杂兵行为表 → 病历级微调。微调方法：§3.4.1 表 + §3.5 表 | 行为表 | §5.2 step 6; prebake管线 | **病历级微调规则不完整。** §3.4.1 只有 3 条反射级映射（惊跳/幻觉/惊恐发作），§3.5 只有 4 条特殊规则。MSE其余10个字段（言语/思维过程/思维内容/认知/自知力…）均无映射规则 → 大量病历信息在精英层被丢弃 | ⚠️ |
| 7 | §3.2 恐慌行为表 | (1) 父类恐慌模板 (2) 疾病非线性跳变 (3) 风险评估字段 | (1) `Dict<行为, 概率%>` (2) 条件→行为覆盖 (3) `枚举<否认\|被动意念\|主动意念\|有计划\|有企图>` | 父类%基线 → 疾病覆盖替换 → 病历覆盖调整概率 | `Dict<行为, 概率%>` (must sum ≈100%) | prebake管线 SAN<30分支 | **风险评估→概率调整量未量化。** "自伤概率↑""攻击概率↑"——↑多少？增加 10%？×1.5？调整后概率总和≠100%时归一化方法未定义 | ⚠️ |
| 8 | §3.3 HP精确值 | (1) HP范围表 (2) 病历偏移因子 | (1) `数值[min, max]` (2) 多个 -0.1 偏移项 | `HP = floor(lower + (upper-lower) × offset_factor)` | `数值 ≥ lower` | Stat Block 数值基线; §5.2 step 7 | **offset_factor 默认值缺失。** 公式用 offset_factor 乘范围差，但偏移因子表格只列出"→ -0.1"等减法。offset_factor 的初值是多少？0.5（中位）？0.0（下限）？如无默认值，无偏移触发时 HP=lower 偏保守 | ❌ |
| 9 | §3.3 SAN精确值 | (1) SAN范围表 (2) 病历偏移因子 | (1) `数值[min, max]` (2) "SAN -5" "SAN -3" | 从范围中位数或上限减去偏移量？ | `数值 ≥ lower` | Stat Block 数值基线; §5.2 step 7 | **HP用乘性公式，SAN用绝对减法——两个资源用不同的偏移逻辑。** HP偏移因子不适用于SAN，SAN偏移量不适用于HP。但"病历偏移因子"下混列两种。——格式混淆但不一定是错误。SAN默认值未定义（取上限？中位？） | ⚠️ |
| 10 | §3.3 行动偏好权重调整 | (1) 父类基线权重 (2) 病历特征调整因子 | (1) `Dict<行动类型, 数值[0,1]>` (sum=1.0) (2) 乘性因子 (×1.5, ×0.5, ×1.3, ×0.7) | 对特定行动的权重乘因子 → 生成新权重分布 | `Dict<行动类型, 数值[0,1]>` | prebake管线 Softmax 采样; NPC行动决策 | **乘因子后概率和≠1.0。** 例如奖赏系统基线 P(物攻)=0.15, P(防)=0.45。若"伤人=是"→P(物攻)×1.5=0.225，P(防)不变=0.45。总和从1.0变为(0.225+0.20+0.45+0.05+0.15)=1.075。**未定义归一化方法** | ❌ |
| 11 | §3.3 槽位预算 | SAN值 | `数值` | `4 + floor(SAN/30)` | `数值[4, 7]` | 激活点分配; prebake管线槽位约束 | §七 #1: 杂兵是否随战斗内SAN波动重算——文档自标❓。公式本身完整 | ✅(含已知待定) |
| 12 | §3.3 髓鞘化基线 (杂兵) | m偏移公式输出 | `Dict<链路ID, 数值>` | 静态——不变化 | `Dict<链路ID, 数值>` (静态) | prebake管线 | m值范围 [-1.0,1.0]，但极端叠加可能超限。§3.2 天花板约束处理超限情况（但变换未定义，见 #4） | ⚠️ |
| 13 | §3.3 髓鞘化基线 (精英) | m偏移公式输出 | `Dict<链路ID, 数值>` | 初始值 = 杂兵同公式。跨战斗成长 = 玩家Δm公式（仅战斗内增长，无巩固窗口） | `Dict<链路ID, 数值>` (动态) | prebake管线（Boss 多阶段独立烘焙） | "仅战斗内增长"——精英在战斗外SAN恢复后m是否回退到初始值？还是持有增长？文档说"仅应用战斗内增长，无巩固窗口"→ 暗示战斗外重置。但敌人重遇时（刷新后）m应当是初始值还是上次值？ | ⚠️ |
| 14 | §3.4 情境响应 (反射级) | MSE.行为 / MSE.感知 / 现病史 | `文本` → 子字符串匹配（"惊跳反应""幻觉""惊恐发作"） | 文本关键词匹配 → 情境响应规则激活 | 情境响应优先级偏移 | prebake管线 情境匹配 rel值 | **文本匹配规则脆弱。** "惊跳反应"只是MSE.行为字段的可能值之一。如果病历写了"对突然声音有明显惊跳"（含"惊跳"但不完全等于"惊跳反应"），是否匹配？需定义是子串匹配还是枚举匹配 | ⚠️ |
| 15 | §4.1-4.6 各父类恐慌行为表 | 父类枚举值 | `枚举<奖赏系统\|焦虑唤醒\|认知控制\|社会认知\|解离性\|进食>` | 查父类 → 返回恐慌行为%表 | `Dict<行为描述, 概率%>` (6 tables, each sum=100%) | §3.2.3 恐慌行为表组装; prebake管线 | 各表%已校验总和=100。但行为描述是自然语言（"僵住""回避""防御""精神攻击"…），未映射到游戏内可执行的动作枚举。prebake管线需要的是链路激活配置，不是行为标签 | ❌ |
| 16 | §4.5 Alter切换 | 恐慌行为表中 "Alter 切换" 条目 | `概率 30%` | "随机切换到另一套行为参数——重新骰行动偏好权重" | 新的行动偏好权重分布 | prebake管线 | **"另一套行为参数"未定义。** 是从同一父类的行动偏好基线重新随机采样？（那可能骰出相同分布）还是从预定义的Alter池中选择？（池在哪里？）DID疾病文件是否定义了Alter配置？ | ❌ |
| 17 | §5.2 管线 9步 | 病历 YAML + 疾病文件 + 标签JSON + 父类模板 | 混合类型（见 #1-#16 各行的输出·类型） | 9步串联: parse→lookup→compute→apply→output | `data/enemies/stat_blocks/*.json` | `bake_npc_behavior.py` (§5.3) | **步骤间类型传递未逐一定义。** Step 4 `compute_m_offset` 输出 Dict<364链路, 数值> → Step 5 `lookup_parent_template` 消费的是 父类枚举 → Step 6 `apply_override` 需要同时消费 Step 4 的 m值 + Step 5 的行为表 + 病历文本字段。这三个步骤的输入/输出类型衔接只在自然语言中隐含，未显式定义 | ⚠️ |
| 18 | §3.1 叙事来源映射 | 病历 `诊断印象.轴I` | `文本` (诊断名) | 固定返回 `病人` | `枚举<病人>` | Stat Block 身份层 | **全部病历→病人，无其他来源。** 任务目标说覆盖 19 种疾病 × 5-6 实例 ≈ 100 敌人——但如果叙事来源只有病人，造物/医疗人员/拜月教徒/动物的 stat block 用哪个模板？病历格式只适用于病人 | ❌ |
| 19 | §4.6 进食障碍 HP -20% | (1) HP精确值 (2) "HP -20%"修正 | (1) `数值` (2) 百分比修正 | `HP_final = HP × 0.8` | `数值` | Stat Block 数值基线 | **修正方式不明确。** "HP -20%"是 `HP = HP - 20%（×0.8）`还是 `HP = HP - 20% × 基础范围`（减固定量）？与 HP 精确值公式（#8）的执行顺序——先算HP精确值再-20%，还是-20%作为偏移因子的一部分？文档自标为 §七 #6 ❓ | ⚠️(已知) |
| 20 | §一 管线阶段 衔接 | 阶段1输出（病历YAML）→阶段2输入 | 病历YAML → Stat Block JSON → prebake输入 | 4阶段管线 | 最终: NPC行为概率分布 | 阶段3: `bake_npc_behavior.py` (待实现); 阶段4: `validate_enemy_stats.py` (待实现) | **阶段3依赖的脚本不存在。** `bake_npc_behavior.py` 规格已定但未实现——阻塞整条管线。阶段2的 `build_enemy_stats.py` 也未实现。两条管线脚本都是"待实现"——目前全部靠手工 | ⚠️ |

## 缺口汇总

| # | 位置 | 严重度 | 描述 |
|----|------|--------|------|
| G1 | §3.2 m偏移公式 | 🔴 阻塞 | **疾病m偏移值与CGI-S倍率的关系未定义。** 疾病文件 `功能域配置` 给出三级具体值（如 +0.1/+0.3/+0.5），`CGI-S域扩展` 又给出 m倍率(×0.5/×1.0/×1.5)。两者是选择关系（取其一）还是乘法关系？当前公式写为 `Δm_disease × CGI-S倍率`，但这可能重复乘算 |
| G2 | §3.2 m天花板 | 🔴 阻塞 | **超限处理算法缺失。** "拮抗对链 m总和 ≤ 1.4"是约束，但超限时如何修正？（截断/拒绝/等比缩放）——无定义 |
| G3 | §3.3 HP offset_factor | 🔴 阻塞 | **默认值缺失。** 公式 `HP = floor(lower + (upper-lower) × offset_factor)` 中 offset_factor 的初值未定义。无触发条件时 HP 取何值？ |
| G4 | §3.3 行动偏好归一化 | 🔴 阻塞 | **乘因子后概率和不归一。** 调整 P(物攻)×1.5 或 P(防)×0.5 后权重总和不等于 1.0。归一化方法未定义 |
| G5 | §4.1-4.6 恐慌行为→动作 | 🔴 阻塞 | **行为标签到游戏动作的映射缺失。** 恐慌表中写的是自然语言（"僵住""回避""随机低层反射"），但 prebake 管线需要具体链路激活配置。无翻译层 |
| G6 | §4.5 Alter切换 | 🔴 阻塞 | **Alter 池未定义。** "随机切换到另一套行为参数"——参数集从哪里来？DID 疾病文件是否定义了多套 Alter 配置？ |
| G7 | §3.1 叙事来源 | 🔴 阻塞 | **病历模板只覆盖病人来源。** 治疗方案目标是 19 疾病 × 5-6 实例 ≈ 100 敌人，但造物/医疗人员/拜月教徒/动物无人设模板 |
| G8 | §3.2 精英病历级微调 | 🟡 警告 | **MSE 大量字段无映射。** MSE 有 10 个域，§3.4.1 只映射了 3 个。其余 7 个域（言语、思维过程、思维内容、认知、自知力等）的精英级行为影响未定义 |
| G9 | §3.2 恐慌概率调整量 | 🟡 警告 | **风险评估→概率调整未量化。** "自伤概率↑""攻击概率↑"——增量多少？调整后不归一怎么处理？ |
| G10 | §3.3 SAN默认值 | 🟡 警告 | **SAN 精确值算法不明确。** HP 有乘性公式，SAN 只有减法偏移量但无默认值起点 |
| G11 | §3.4 反射级匹配 | 🟡 警告 | **文本匹配规则脆弱。** MSE.行为/感知字段是自由文本，关键词匹配（"惊跳反应"）需要定义匹配算法 |
| G12 | §3.3 精英m值持久性 | 🟡 警告 | **精英跨战斗m值是否重置？** "仅应用战斗内增长，无巩固窗口"→暗示战斗外重置，但重遇同一精英时期望m值是什么？ |
| G13 | §5.2 步骤间类型 | 🟡 警告 | **管线步骤输入/输出类型未显式定义。** Step 4→5→6 的类型传递只在自然语言中隐含 |

## 半径扩张 — 待复查

| 修复点 | 受影响消费者 | 复查状态 |
|--------|------------|---------|
| G1 (m偏移×倍率) | §3.2 全部链路m值; §5.2 step 4; prebake 情境匹配 | 待修复后复查 |
| G2 (天花板算法) | §3.2 m值输出; prebake 采样; 伤害公式 | 待修复后复查 |
| G3 (offset_factor默认值) | §3.3 全部敌人HP; §5.2 step 7; 战斗平衡 | 待修复后复查 |
| G4 (归一化) | §3.3 行动偏好; §5.2 step 6; prebake Softmax | 待修复后复查 |
| G5 (恐慌行为→动作) | §4.1-4.6 全部恐慌表; prebake SAN<30分支 | 待修复后复查 |
| G6 (Alter池) | §4.5 解离性恐慌; DID敌人设计 | 待修复后复查 |
| G7 (非病人模板) | 约 40-50% 的敌人实例（造物/医疗人员/拜月教徒/动物） | 待修复后复查 |

## 统计

- 总 trace 行数: 20
- ✅: 3 (含 1 已知待定)
- ✅(浅): 2
- ⚠️: 8
- ❌: 7
- 待复查: 7

---

*审查: 2026-08-01 20:05 | 方法: 符号执行 trace 20 行 | 关联: [#18](https://github.com/verystrongdog/game/issues/18)*
