# CLAUDE.md — 面具 (The Mask)

## 项目本质

这是一个**游戏设计项目**，不是纯代码项目。设计文档是 primary artifact，代码（Python 模拟）是验证设计的工具。最终目标是 **Unity 引擎**实装，但当前阶段是**纸面设计 + 数值模拟验证**。

**核心原则：设计驱动代码，代码服务于设计验证。不要在没有设计文档的情况下写代码。**

---

## 核心术语表（AI 必须准确使用）

| 术语 | 定义 | 禁止混淆 |
|------|------|----------|
| 态度 | 认知 + 情绪 + 行为 的组合结算结果，不是独立卡牌 | 不要说"态度卡" |
| PAD | Pleasure-Arousal-Dominance 三维情绪空间 | 情绪用 PAD，不要用离散标签 |
| 7 驱动 | 反射/惩罚/奖励/道德/文化/价值/照护 | 行为型技能必须归属一个驱动 |
| 5 模块 | 知觉与注意/记忆/知识表征/表象与语言/推理与决策 | 认知型技能必须归属一个模块 |
| CPM | Cognitive-Psychological Mechanism，4 步评估 | SEC 闭合是情绪信号触发点 |
| SAN | 精神护盾，不是血条 | SAN 与 HP 可互相转化但有上限 |
| 意志力 | 消耗性精神能量，用于硬解锁灰色行为型技能 | 意志力 ≠ SAN |
| 观察者效应 | 世界观核心——月光导致的量子叠加态 | 不是比喻，是 game mechanic |
| 镜子房间 | 开局/构筑/单局收束空间 | 不是普通房间 |

---

## 架构约束（强制）

### 设计层面
- 态度公式 `认知 + 情绪 + 行为 = 态度` 不可变。态度不是独立实体。
- 颜色系统已废弃，不要在任何新设计中使用颜色表示情绪/态度。用 PAD 向量。
- 离散 ±1/0 态度晶体已废弃，用连续 7 驱动 × PAD 空间（90 种态度，15 情绪 × 7 驱动，105 理论组合中 90 有效）。
- 治疗模拟框架已废弃，世界观是超自然观察者效应。
- 敌我同构——双方共用同一套底层体系。
- **当前分界**：玩家交互 = 3D 技能树 + 快捷槽，技能直接走数值结算（伤害/buff/debuff）；NPC AI = 态度引擎（认知+情绪+行为=态度）决定 NPC 的行为选择，使其脱离固定程序。态度不算伤害。

### 代码层面
- 当前阶段代码是 **Python 3 模拟脚本**（数值验证用），最终目标是 **Unity + C#**。
- 模拟脚本风格：单文件、函数式为主、中文注释和变量名可接受。
- 最终 Unity 实装时：C# 命名遵循 Unity 惯例（PascalCase 方法名、camelCase 字段）。
- 所有数值参数必须有来源注释（文献出处或设计文档章节引用）。
- 新模拟脚本应复用现有脚本中的常量和函数（如 EMOTIONS、DRIVES、sigmoid()），不要重复定义。

### 文档层面
- 每个子系统有独立目录和 README 级别的 .md 文件。
- "待解决问题"用独立文件记录（如 `行为系统/待解决问题.md`）。
- 废弃内容标记 `⚠️ 已废弃` 并说明废弃原因，移入 `垃圾桶/` 或 `.trash/`。
- 设计文档使用中文，代码注释中英皆可。
- **自动发现机制**：触发规则按目录匹配（如 `情绪系统/*.md`），新增 md 文件放入对应子系统目录后会被自动读取，无需手动修改 CLAUDE.md。新增子系统目录时，在"触发-响应规则"中加一条目录级规则即可。
- **⚠️ 已废弃标记**：被替代的子系统文件标记 `⚠️ 已废弃` 但保留完整内容（可能仍作为底层引擎或数据源的参考）。

### md 文件格式规范（AI 写/改文档时必须遵守）

**文件头部：**
```markdown
# 文档标题

> 一两句话摘要——本文档的核心内容和定位。
```

**文件尾部：**
```markdown
---
*创建: YYYY-MM-DD | 更新: YYYY-MM-DD*
*关联: [文档名](相对路径), ...*
```

**长文件必须加目录：** 超过 200 行的 md 文件，在头部摘要之后加 `## 目录`（有序列表链接到各 `##` 小节）。

**交叉引用：** 统一用 `[显示名](相对路径)` 格式，不要只用裸文件名。

**废弃标记：** `> **⚠️ 已废弃**：废弃原因。`

**参数速查表：** 涉及数值参数的文档，文末加参数速查表，列名统一为：`| 参数 | 符号 | 默认值 | 位置 |`。

**一个文件一个主题：** 不要在一个 md 文件中放多个 H1。内容独立的主题拆成独立文件。

**小节编号：** 用 `## 一、标题` 中文数字（长文档便于交叉引用，如"见 §二"）。

### 修改 CLAUDE.md 的铁律（自指约束）
- **只增不删**：修改 CLAUDE.md 时，只能新增规则，不能删除或削弱任何已有规则。
- 如果确实需要删除某条规则，必须先向用户说明理由，得到明确同意后才能删除。
- 这条规则本身也不能被删除。

---

## Git 工作流规范

### 提交策略
- **小步提交**：每完成一个独立的设计文档、一个可运行的模拟脚本、或一批关联修改 → 立即提交。不要等"全部做完"再提交。
- **新文件创建后立即 add**：写完一个 md 文件 → `git add` + `git commit`。不攒一堆 untracked 文件。
- **提交信息用中文**，格式：`类型: 描述`。类型包括：
  - `feat:` — 新功能/新系统（如 `feat: 技能树系统架构文档`）
  - `docs:` — 文档修改/新增（如 `docs: 态度结算链路 §④ 修正公式`）
  - `sim:` — 模拟脚本（如 `sim: CTRNN 进化模拟器`）
  - `fix:` — 修正错误（如 `fix: C_control 查表逻辑错误`）
- **按主题分批**：不同子系统的改动分开提交，不要混在一个 commit 里。

### 分支策略
- **大改动先开分支**：新增子系统、替换旧体系、大规模重构 → 从 `main` 开 feature 分支。
- **实验性方向用分支**：不确定的设计方向在分支上做，确认后再合入 `main`。
- **分支命名**：`feat/<功能名>` 或 `exp/<实验名>`（如 `feat/skill-tree`）。

### 文件管理
- 设计文档（.md）、模拟脚本（.py）、原型（.html）、数据文件（.json）、工具脚本（tools/）→ 全部进版本控制。
- `data/` 和 `tools/` 是设计的数据源和工具，不是生成物，必须提交。
- 外部库文件（如 `lib/three.module.js`）应优先用 CDN，如需本地副本则必须提交。
- 不该进 git 的已在 `.gitignore` 中排除（Python cache、IDE 配置、OS 杂文件等）。

### AI 行为
- 用户说"保存一下"/"提交" → AI 执行 `git add` + `git commit`，按主题分批提交。
- 每批提交前简要列出文件组，提交后展示 commit hash 和摘要。
- 危险操作（`git reset --hard`、`git rebase`、`git push --force`）必须先确认。

---

## 触发-响应规则（Strict）

### 当用户提出新设计/新功能时
1. **IF** 涉及玩家技能/技能树/技能设计/3D 可视化 → **THEN** 先读取 `技能树系统/README.md`（目录索引），再按需读取 `技能树系统/操作层/`、`技能树系统/激活系统/`、`技能树系统/调制参数/` 下的文件
2. **IF** 涉及 NPC AI/态度引擎/态度结算 → **THEN** 先提醒用户态度引擎已废弃（2026-07-26 grilling 决定），NPC AI 现用脑区链路模型（见 `技能树系统/脑功能层级模型.md`）；同时 Glob `态度系统/*.md` 确认是否有后续更新
3. **IF** 涉及认知 → **THEN** 先 Glob `认知系统/*.md`，读取该目录下所有 .md 文件
4. **IF** 涉及情绪 → **THEN** 先 Glob `情绪系统/*.md`，读取该目录下所有 .md 文件
5. **IF** 涉及行为 → **THEN** 先 Glob `行为系统/*.md`，读取该目录下所有 .md 文件
6. **IF** 涉及角色/面具 → **THEN** 先读取 `02-角色与面具.md`
7. **IF** 涉及世界观/叙事 → **THEN** 先读取 `04-世界观与叙事.md`
8. **IF** 涉及空间/关卡/场景设计/战斗场地 → **THEN** 先读取 `05-空间与关卡设计.md`
9. **IF** 涉及卡牌设计/卡牌内容 → **THEN** 先提醒用户卡牌体系已废弃（见 `⚠️ 已废弃`），然后 Glob `技能树系统/*.md`。旧卡牌参数保留在 `data/` 下作为技能设计的参考数据源。
10. **IF** 涉及战斗机制（跑位/距离/AoE/回合流程）→ **THEN** 先读取 `01-核心机制.md` 和 `技能树系统/操作层/基础行动设计.md`、`技能树系统/激活系统/链路槽位与激活系统.md`

### 当用户要求修改/扩展设计时
1. **IF** 提出的方向在 `98-被枪毙的想法.md` 中 → **THEN** 立即警告用户该方向已被废弃，并说明废弃原因
2. **IF** 提出的方向在 `行为系统/待解决问题.md` 或 `认知系统/待解决问题.md` 中标记为"仍在讨论中" → **THEN** 提醒用户该问题尚未有定论
3. **IF** 涉及数值参数 → **THEN** 搜索现有文档确认参数是否已有定义，不要凭空创造

### 当写代码时
1. **先**搜索项目中是否已有相关函数/常量，不要重复定义
2. **先**确认设计文档中对该功能是否有明确规定
3. 所有数学公式必须与设计文档中的公式一致
4. 模拟结果要有输出格式说明

### 一般规则
- 用户中文提问 → 中文回答。用户英文提问 → 英文回答。
- 涉及 Unity 操作 → 中英双语标注按钮/菜单路径。
- 涉及 `sudo` 或系统包安装 → 给出命令让用户自己执行，不要代替执行。
- 超过 3 个文件的改动 → 先进 Plan Mode 出计划，用户确认后再写代码。

---

## 项目文件地图

```
00-项目总览.md              ← 入口，设计抓手和待解决问题
01-核心机制.md              ← 技能交互 + 态度引擎 + SAN/敌我同构
02-角色与面具.md            ← 角色设定
03-敌人与事件.md            ← 敌人设计
04-世界观与叙事.md          ← 超自然观察者效应设定
05-空间与关卡设计.md        ← 四层空间模型（物理医院/扭曲空间/患者世界/轮回）
98-被枪毙的想法.md          ← ⚠️ 绝对不要重复这些方向
99-灵感收件箱.md            ← 未成形的想法

态度系统/                   ← ⚠️ 已废弃（2026-07-26），保留为参考数据源；NPC AI → 脑区链路模型
情绪系统/                   ← PAD 情绪空间（NRC VAD 数据）
行为系统/                   ← 7 驱动 + 意志力 + 硬解锁
认知系统/                   ← 5 模块 + SDT + 双加工 + CHC
技能树系统/                  ← 脑区链路技能树（当前玩家交互方式）
  ├── 脑功能层级模型.md         ← 正典：89脑区 L0-L6 七层功能层级
  ├── 操作层/                  ← L0-L6 技能操作 + 基础行动
  ├── 激活系统/                 ← 链路槽位、情境自动激活、SAN恢复、休息
  ├── 调制参数/                 ← 三因子调制模型
  ├── 3D可视化/                ← 可视化规范、Blender管线
  └── 已废弃/                  ← 旧三分支框架、态度引擎（保留为考古参考）
卡牌系统/                   ← ⚠️ 已废弃：卡牌设计（保留为技能参数参考数据源）
治疗中心建模/               ← 治疗中心空间建模

sim_battle.py               ← 冲突战蒙特卡洛模拟
sim_battle_v4.py            ← 卡牌版战斗模拟（保留供底层公式验证）
sim_cog_evo.py              ← 认知进化模拟
sim_evo_ctrnn.py            ← CTRNN 进化模拟
```

---

## 禁止事项

- ❌ 重新引入颜色系统（情绪/态度用颜色表示）
- ❌ 重新引入离散态度晶体（±1/0 三维向量）
- ❌ 将"态度"当作独立卡牌类型
- ❌ 重新引入卡牌交互方式（已迁移至 3D 技能树）
- ❌ 将战斗简化为"削血"逻辑
- ❌ 在仿真代码中使用与设计文档不一致的公式
- ❌ 凭空添加设计文档中没有的机制
- ❌ 用英文回答中文提问

## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
