# CLAUDE.md — 面具 (The Mask)

## 项目本质

这是一个**游戏设计项目**，不是纯代码项目。设计文档是 primary artifact，代码（Python 模拟）是验证设计的工具。最终目标是 **Unity 引擎**实装，但当前阶段是**纸面设计 + 数值模拟验证**。

**核心原则：设计驱动代码，代码服务于设计验证。不要在没有设计文档的情况下写代码。**

---

## 核心术语表（AI 必须准确使用）

| 术语 | 定义 | 禁止混淆 |
|------|------|----------|
| 态度 | 认知 + 情绪 + 行为 的组合结算结果，不是独立卡牌 | 不要说"态度卡" |
| PAD | Pleasure-Arousal-Dominance 三维情绪空间 | 情绪用 PAD，不要用离散标签 |
| 7 驱动 | 反射/惩罚/奖励/道德/文化/价值/照护 | 行为型技能必须归属一个驱动 |
| 5 模块 | 知觉与注意/记忆/知识表征/表象与语言/推理与决策 | 认知型技能必须归属一个模块 |
| CPM | Cognitive-Psychological Mechanism，4 步评估 | SEC 闭合是情绪信号触发点 |
| SAN | 精神护盾，不是血条 | SAN 与 HP 可互相转化但有上限 |
| 意志力 | 消耗性精神能量，用于硬解锁灰色行为型技能 | 意志力 ≠ SAN |
| 观察者效应 | 世界观核心——月光导致的量子叠加态 | 不是比喻，是 game mechanic |
| 随身镜子 | 自我观察 UI 界面，非物理房间 | 已从固定空间改为随身物品（2026-07-28） |

---

## 架构约束（强制）

### 设计层面
- 态度公式 `认知 + 情绪 + 行为 = 态度` 不可变。态度不是独立实体。
- 颜色系统已废弃，不要在任何新设计中使用颜色表示情绪/态度。用 PAD 向量。
- 离散 ±1/0 态度晶体已废弃，用连续 7 驱动 × PAD 空间（90 种态度，15 情绪 × 7 驱动，105 理论组合中 90 有效）。
- 治疗模拟框架已废弃，世界观是超自然观察者效应。
- 敌我同构——双方共用同一套底层体系。
- **当前分界**：玩家交互 = 3D 技能树 + 快捷槽，技能直接走数值结算（伤害/buff/debuff）；NPC AI = 脑区链路预烘焙查表（6 性格标签 → 髓鞘化偏移 → 27 情境 × N SAN 分段行为概率分布），使其脱离固定程序。态度不算伤害。

### 代码层面
- 当前阶段代码是 **Python 3 模拟脚本**（数值验证用），最终目标是 **Unity + C#**。
- 模拟脚本风格：单文件、函数式为主、中文注释和变量名可接受。
- 最终 Unity 实装时：C# 命名遵循 Unity 惯例（PascalCase 方法名、camelCase 字段）。
- 所有数值参数必须有来源注释（文献出处或设计文档章节引用）。
- 新模拟脚本应复用现有脚本中的常量和函数（如 EMOTIONS、DRIVES、sigmoid()），不要重复定义。

### 文档层面
- 每个子系统有独立目录和 README 级别的 .md 文件。
- "待解决问题"用独立文件记录（如 `行为系统/待解决问题.md`）。
- 废弃内容标记 `⚠️ 已废弃` 并说明废弃原因，移入 `垃圾桶/` 或 `.trash/`。
- **垃圾桶隔离原则（2026-08-03 新增）**：内容移入垃圾桶后，**项目中任何活跃文档（含索引、README、规格文档、维度状态）不得引用垃圾桶中的文件**。移动文件的同时必须 grep 全项目所有引用该文件路径的地方，一并更新或删除。废弃的数字/术语（如"37个技能""激活点预算"）同理——活跃文档中不得出现，只允许出现在决策树历史记录和标注了 `⚠️ 已废弃` 的段落中。
- 设计文档使用中文，代码注释中英皆可。
- **自动发现机制**：触发规则按目录匹配（如 `情绪系统/*.md`），新增 md 文件放入对应子系统目录后会被自动读取，无需手动修改 CLAUDE.md。新增子系统目录时，在"触发-响应规则"中加一条目录级规则即可。
- **⚠️ 已废弃标记**：被替代的子系统文件标记 `⚠️ 已废弃` 但保留完整内容（可能仍作为底层引擎或数据源的参考）。此标记仅在决策树历史记录和学术参考文献中可被引用。

### md 文件格式规范（AI 写/改文档时必须遵守）

**文件头部：**
```markdown
# 文档标题

> 一两句话摘要——本文档的核心内容和定位。
```

**文件尾部：**
```markdown
---
*创建: YYYY-MM-DD | 更新: YYYY-MM-DD*
*关联: [文档名](相对路径), ...*
```

**长文件必须加目录：** 超过 200 行的 md 文件，在头部摘要之后加 `## 目录`（有序列表链接到各 `##` 小节）。

**交叉引用：** 统一用 `[显示名](相对路径)` 格式，不要只用裸文件名。

**废弃标记：** `> **⚠️ 已废弃**：废弃原因。`

**参数速查表：** 涉及数值参数的文档，文末加参数速查表，列名统一为：`| 参数 | 符号 | 默认值 | 位置 |`。

**一个文件一个主题：** 不要在一个 md 文件中放多个 H1。内容独立的主题拆成独立文件。

**小节编号：** 用 `## 一、标题` 中文数字（长文档便于交叉引用，如"见 §二"）。

### 修改 CLAUDE.md 的铁律（自指约束）
- **只增不删**：修改 CLAUDE.md 时，只能新增规则，不能删除或削弱任何已有规则。
- 如果确实需要删除某条规则，必须先向用户说明理由，得到明确同意后才能删除。
- 这条规则本身也不能被删除。

---

## Git 工作流规范

### 提交策略
- **小步提交**：每完成一个独立的设计文档、一个可运行的模拟脚本、或一批关联修改 → 立即提交。不要等"全部做完"再提交。
- **新文件创建后立即 add**：写完一个 md 文件 → `git add` + `git commit`。不攒一堆 untracked 文件。
- **提交信息用中文**，格式：`类型: 描述`。类型包括：
  - `feat:` — 新功能/新系统（如 `feat: 技能树系统架构文档`）
  - `docs:` — 文档修改/新增（如 `docs: 修正回合战斗流程 §五 响应窗口规则`）
  - `sim:` — 模拟脚本（如 `sim: CTRNN 进化模拟器`）
  - `fix:` — 修正错误（如 `fix: C_control 查表逻辑错误`）
- **按主题分批**：不同子系统的改动分开提交，不要混在一个 commit 里。

### 分支策略
- **大改动先开分支**：新增子系统、替换旧体系、大规模重构 → 从 `main` 开 feature 分支。
- **实验性方向用分支**：不确定的设计方向在分支上做，确认后再合入 `main`。
- **分支命名**：`feat/<功能名>` 或 `exp/<实验名>`（如 `feat/skill-tree`）。

### 文件管理
- 设计文档（.md）、模拟脚本（.py）、原型（.html）、数据文件（.json）、工具脚本（tools/）→ 全部进版本控制。
- `data/` 和 `tools/` 是设计的数据源和工具，不是生成物，必须提交。
- 外部库文件（如 `lib/three.module.js`）应优先用 CDN，如需本地副本则必须提交。
- 不该进 git 的已在 `.gitignore` 中排除（Python cache、IDE 配置、OS 杂文件等）。

### AI 行为
- 用户说"保存一下"/"提交" → AI 执行 `git add` + `git commit`，按主题分批提交。
- 每批提交前简要列出文件组，提交后展示 commit hash 和摘要。
- 危险操作（`git reset --hard`、`git rebase`、`git push --force`）必须先确认。

---

## 触发-响应规则（Strict）

### 六维框架（所有设计工作的导航基础）

游戏 = 规则 × 实体 × 空间 × 事件 → 通过呈现交付，由管线生产。

**任何设计讨论必须先定位维度**。路由表：[docs/设计框架-六维状态.md](docs/设计框架-六维状态.md)。

### 当用户提出新设计/新功能时（按六维分拣）

1. **IF** 涉及"能发生什么、怎么发生"（战斗结算/资源流动/AI决策/回合结构/数值公式）→ **THEN** 先读取 `docs/维度/规则.md`（索引），再按需读取具体文档
2. **IF** 涉及"谁是参与者"（角色/敌人/NPC/物品/装备）→ **THEN** 先读取 `docs/维度/实体.md`
3. **IF** 涉及"在哪里发生"（房间/区域/关卡/空间连接）→ **THEN** 先读取 `docs/维度/空间.md`
4. **IF** 涉及"什么时候触发"（遭遇/对话/叙事节拍/游戏循环/任务）→ **THEN** 先读取 `docs/维度/事件.md`
5. **IF** 涉及"玩家怎么感知和操作"（UI/输入/反馈/视觉风格/声音）→ **THEN** 先读取 `docs/维度/呈现.md`
6. **IF** 涉及"怎么造出来"（数据格式/构建脚本/原型/测试）→ **THEN** 先读取 `docs/维度/管线.md`

### Grilling 专用规则

> **AI 注意**：以下每一步必须**逐条执行**，不可跳过任何一步。每次 grilling 开始时，将此清单作为任务列表创建并逐项打勾。

#### Grilling 启动 — 无话题时自动检索（不可跳过）

**IF** 用户调用 `/grilling` 但未指定具体话题 **THEN** 在进入 Step 0 之前，必须先执行以下检索：

- [ ] 读取 `docs/设计框架-六维状态.md` 全部内容（项目全局检查点）
- [ ] 读取全部六个维度索引：`docs/维度/规则.md` `实体.md` `空间.md` `事件.md` `呈现.md` `管线.md`
- [ ] 读取 `docs/决策树.md` 了解已完成 grilling 的轮次和决策数
- [ ] 按六维状态中的「全局 Grilling 优先级」顺序，列出当前所有未完成话题
- [ ] 给出推荐话题和建议理由
- [ ] 等待用户确认话题后，再进入 Step 0

#### Step 0 — 定位（不可跳过）
- [ ] 读取 `docs/设计框架-六维状态.md`，确认话题在全局优先级中的位置
- [ ] 读取 `docs/维度/<维度名>.md`，确认该维度已有设计和空缺
- [ ] **【v2 新增】前置资料包**：读取所有直接相关的设计文档和决策树历史记录。如相关文件 > 5 个，优先读取索引文件，具体子系统文件在追问涉及时按需读取
- [ ] **【v2 新增】输出资料包完整性声明**：列出"预读"清单（已完整读入上下文）+"待按需读取"清单，声明未在清单中的事实性引用视为违规
- [ ] 向用户确认话题边界："本次 grilling 聚焦 [维度] 的 [话题]，依赖 [X]，阻塞 [Y]"

#### Step 1 — 话题验证（不可跳过，不通过则不创建 issue）
- [ ] `grep` `docs/决策树.md` 中是否已有同名话题的「决策」或「被否决」→ 有则告知用户
- [ ] `gh issue list --label grilling --state open` 是否有重复 grilling issue → 有则链接已有 issue

#### Step 2 — 创建 Issue（验证通过后、追问开始前，不可跳过）
- [ ] `gh issue create`，标题 `[Grilling] <话题>`，标签 `grilling, needs-triage, 维度:<维度名>`
- [ ] Issue body 写清话题 + 六维定位 + 当前状态

#### Step 3 — 追问（逐题进行）
- **【v3 新增】Step 3.0 定义基线声明**：每轮追问前，AI 必须先查询 `data/term_registry.json`，列出所有关键术语的项目内定义和"已否决的默认模型"，再提出建议。旧模型未被识别 → 建议无效。详见 `.claude/skills/grilling/SKILL.md` §3.0
- 每轮一个问题，等待用户回答后再问下一题
- **【v2 新增】事实锚定**：每个回答如包含事实性主张 → 必须标注来源文件+段引用（优先）或行号。新参数/公式 → 显式声明"此为本次新增"。不得凭记忆引用项目内的参数、公式、设计决策、文件路径（见 §引用即读取铁律）
- **【v2 新增】决策深度自检**：涉及新数值参数（追问来源/依据/范围）、新机制交互（追问冲突处理）、新实体类型（追问已有实体关系）、跨维度影响（逐维检查）、用户单字确认（追问具体理解）→ 以上任一触发则必须继续追问，不得草率共识。全部数值有来源 + 机制冲突已检查 + 跨维已确认 + 用户非单字确认 → 方可关闭话题
- **【v3 新增】注册表入库**：基线表中出现不在注册表且现实有常用含义的术语 → 追问是否入库。Grilling 结束后 AI 列出候选清单，用户确认后写入 `data/term_registry.json`

#### Step 4 — 写入（达成共识后，不可跳过）
- [ ] 将决策写入受影响的 md 设计文档
- [ ] 将决策树追加到 `docs/决策树.md`
- [ ] 追加前 `grep` 决策树中与本次相关的「延迟」项 → 新记录标注来源，旧记录加注「→ 见」
- [ ] 更新 `docs/设计框架-六维状态.md` 对应维度的状态（✅/⚠️/❌）
- [ ] 检查 `项目总览.md` 是否需要同步
- [ ] 将关键决策摘要写入 memory 文件
- [ ] **【v2 新增】写入验证表**：逐条对照 grilling 决策 → 写入内容，输出验证表（决策编号/摘要/写入文件/位置/验证状态），每项标记 ✅已验证 或 ⚠️需人类复查
- [ ] **🔥 一致性清扫（强制，v2 强化）**：`grep` 本次废弃/修改的关键词（术语+参数名+文件名+废弃概念名+**废弃文件路径片段**）。命中 ≤10 → 全部 Read；>10 → 按优先级（设计文档 > 索引 > 数据JSON > 工具脚本）分批 Read。**必须额外检查**：(a) 移动进垃圾桶的文件是否还有活跃文档引用它（路径断裂），(b) 废弃的数字/术语是否仍在活跃规格文档中以非废弃标注的方式出现，(c) **【v3 新增】术语定义变更时对照 `term_registry.json` 的 numerical_locations 做兼容性复查**

#### Step 5 — 关闭 Issue（不可跳过）
- [ ] `gh issue close` 关 issue
- [ ] 在 issue 下发总结评论（决策表 + 受影响文件列表 + 推迟清单）
- **【v2 新增】质量门禁**：运行自动化校验脚本（阶段 2 实现后启用；当前阶段手动等效检查：cross-ref grep + grilling 产物完整性目视核查）。输出关闭总结（决策表 + 受影响文件 + 写入验证 + 校验结果）

#### Step 6 — 闭合后修正（grilling issue 关闭后，对其决策的修改）
- **触发条件**：已关闭的 grilling issue 中的决策被实质性修改（公式增删项、阈值变更、参数重定义、术语定义修正），而非纯文本修正（错字、格式）。
- **必须执行**：
  - [ ] 在对应的 GitHub issue 下发修正评论，格式：`## 🔧 闭合后修正 (YYYY-MM-DD)` + 修正项表格（原决策 / 修正后 / 根因）
  - [ ] 在 `docs/决策树.md` 对应 grilling 条目下追加 `**🔧 修正 (YYYY-MM-DD):**` 行，简述修正内容和 commit hash
  - [ ] 修正涉及多个 issue → 每个 issue 分别评论
- **不需要**：重新打开 issue、创建新 issue、或重新执行 grilling Step 0-5。

### 旧子系统触发规则（保留兼容）

以下旧子系统已废弃但保留为数据源，讨论时先提醒已废弃：
- 态度引擎/态度结算 → 已废弃，NPC AI 改用脑区链路模型。Glob `态度系统/*.md` 确认是否有后续更新
- 卡牌设计/卡牌内容 → 已废弃。旧卡牌参数在 `data/` 下作为参考
- 认知/情绪/行为独立系统 → 已废弃，查看 `docs/维度/规则.md` 了解当前正典

### 当用户要求修改/扩展设计时
1. **IF** 提出的方向在 `docs/决策树.md` 中被否决 → **THEN** 立即警告用户该方向已被废弃，并说明废弃原因
2. **IF** 涉及数值参数 → **THEN** 搜索现有文档确认参数是否已有定义，不要凭空创造
3. **IF** 改动影响多个维度 → **THEN** 列出受影响维度清单，确认依赖关系后再动手

### 当写代码时
1. **先**搜索项目中是否已有相关函数/常量，不要重复定义
2. **先**确认设计文档中对该功能是否有明确规定
3. 所有数学公式必须与设计文档中的公式一致
4. 模拟结果要有输出格式说明

### 一般规则
- 用户中文提问 → 中文回答。用户英文提问 → 英文回答。
- 涉及 Unity 操作 → 中英双语标注按钮/菜单路径。
- 涉及 `sudo` 或系统包安装 → 给出命令让用户自己执行，不要代替执行。
- 超过 3 个文件的改动 → 先进 Plan Mode 出计划，用户确认后再写代码。

### 术语注册表即时更新（2026-08-04 新增）

> **设计讨论中引入新术语、或在已有术语上叠加新含义层时，讨论结束后必须即时更新 `data/term_registry.json`——不等 grilling。**

**触发条件**：
(a) 新术语不在注册表中且有项目内自定义含义
(b) 已有术语的 `definition`/`old_model`/`deviation_reason` 被实质修改
(c) 两个已有术语的边界被重新划分（如 DFS 集体退相干 vs Zeno 子空间 vs 观察者耦合——三个退相干抑制机制的层面分离）

更新后检查是否需同步写 memory。

### 注册表覆盖范围 — 短标识符全入库（2026-08-05 Grilling #21 新增）

> **项目设计文档表格列头和参数名中出现的 ≤4 ASCII 字符短标识符，全部纳入 `term_registry.json`。**

**触发条件**: 新 .md 文件引入新表格列头/参数名缩写 → 同时入库。
**不在注册表中的短标识符 → 默认视为未定义（grilling §3.0 中无法查询到）。**

不适用范围: 工具脚本变量名、用户自然语言中的缩写、对话文本。

### 引用即读取铁律（2026-08-01 Grilling #16 新增）

> **任何事实性引用，必须先 `Read` 源文件确认。不得凭记忆引用项目内的参数、公式、设计决策、文件路径。**

**触发条件**：引用以下内容时必须先 Read 源文件——数值参数、公式、已有设计决策、文件路径、数据结构（如链路数量）、交叉引用目标。

**不触发**（可凭常识回答）：通用神经科学知识、游戏设计常识、对用户问题的理解性复述。

**自检触发信号**：出现"根据我们之前的讨论…""项目中使用的是…""按照 CLAUDE.md 的规定…"或任何不带文件路径/段落号的参数引用时 → 触发自检 → 有 Read 记录支撑则合法，无则违规。

**引用格式（段引用优先）**：`规则/核心机制.md` §5.1（段引用，稳健）；`:314`（行号，精确但易漂移）。优先段引用。

### 项目级引用规范（2026-08-01 Grilling #16 新增）

- **文件路径**：使用项目根目录下的相对路径（如 `规则/核心机制.md`）。`memory/` 是 `~/.claude/projects/-home-dog-game/memory/` 的简写。
- **参数定义**：所有数值参数须有来源注释（文献出处或设计文档章节引用）。新增参数标注 `[NEW]` + issue 编号，issue 关闭后下一次涉及该文件的编辑中摘除。
- **交叉引用**：格式 `[显示文本](相对路径)`，展示文本清晰描述目标内容（不只是裸文件名），优先用段引用（`§八`）而非 URL 锚点（渲染器差异）。
- **决策引用**：决策树节点引用格式 `决策树: [Grilling] <话题> §第N题 → 结论`。Memory 引用格式 `memory: <文件名> §N`。
- 详见 `docs/grilling-质量保障体系-v2.md` §四。

---

## 项目文件地图（按六维分类）

```
项目总览.md                  ← 入口 → 六维框架索引
docs/
  ├── 设计框架-六维状态.md    ← Grilling 路由表 + 全局检查点
  ├── 维度/                  ← 每维度的独立索引
  │   ├── 规则.md
  │   ├── 实体.md
  │   ├── 空间.md
  │   ├── 事件.md
  │   ├── 呈现.md
  │   └── 管线.md
  └── agents/                ← Agent定义/文档模板

规则/
  ├── 核心机制.md              ← 脑区链路模型/战斗结算/SAN/情境
  ├── 回合战斗流程.md          ← 基底节门控/双通道/速度排序/响应窗口
  └── 技能树系统/
      ├── 脑功能层级模型.md     ← 正典：50解剖实体 L0-L5 六层功能层级
      ├── 操作层/              ← L0-L5 技能操作 + 基础行动
      ├── 激活系统/            ← 情境自动激活、SAN恢复、休息
      ├── 调制参数/            ← 三因子调制模型
      ├── NPC AI 行为模型.md   ← 预烘焙查表/性格标签/敌我同构
      └── 3D可视化/            ← 可视化规范/Blender管线/HTML原型

实体/
  ├── 角色与面具.md            ← 14角色/面具/共享症状
  └── 敌人与事件.md            ← 敌人分类/CGI-S分级/思维造物

空间/
  ├── 空间与关卡设计.md        ← 三层空间模型/关卡设计
  └── 治疗中心建模/            ← 医院建筑参考/Blender场景

事件/
  ├── 世界观与叙事.md          ← 月光本质/观察者效应/拜月教
  └── 游戏循环.md              ← 连续世界/时间/死亡/结局/驱动力

呈现/                         ← (待建设)

管线/                         ← (待建设)

参考/
  ├── 研究与参考.md
  ├── 灵感收件箱.md            ← 未成形的想法
  ├── 文献/                    ← 学术论文/参考PDF
  └── 废弃/
      ├── 态度系统/            ← ⚠️ 已废弃（2026-07-26），保留为参考数据源
      ├── 情绪系统/            ← PAD 情绪空间（NRC VAD 数据）
      ├── 行为系统/            ← 7 驱动 + 意志力 + 硬解锁
      ├── 认知系统/            ← 5 模块 + SDT + 双加工 + CHC
      └── 卡牌系统/            ← ⚠️ 已废弃：卡牌设计（保留为参数参考）

data/                           ← 结构化数据（JSON/NPY）
tools/                          ← Blender自动化/数据生成脚本
sim_*.py                        ← 数值模拟验证
```

---

## 禁止事项

- ❌ 重新引入颜色系统（情绪/态度用颜色表示）
- ❌ 重新引入离散态度晶体（±1/0 三维向量）
- ❌ 将"态度"当作独立卡牌类型
- ❌ 重新引入卡牌交互方式（已迁移至 3D 技能树）
- ❌ 将战斗简化为"削血"逻辑
- ❌ 在仿真代码中使用与设计文档不一致的公式
- ❌ 凭空添加设计文档中没有的机制
- ❌ 用英文回答中文提问
- ❌ 在活跃文档（非决策树/非废弃标注段）中引用垃圾桶内的文件路径或已废弃的数字/术语

## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
