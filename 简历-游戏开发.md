# [姓名]

**求职方向：游戏客户端开发工程师 / 游戏系统开发工程师 / 技术策划**

[手机号] ｜ [邮箱] ｜ [所在城市] ｜ [个人主页/作品集]

GitHub：[verystrongdog/game](https://github.com/verystrongdog/game)

## 个人简介

独立设计并开发 3D 回合制心理对抗游戏《子非鱼》，覆盖玩法规则设计、数值模型验证、结构化数据生产、C# 逻辑引擎、Unity 呈现原型和持续集成全流程。能够把神经科学、心理疾病等复杂领域概念拆解为明确的游戏规则、状态机、数据契约与自动化测试，并通过工程门禁保持设计、数据和代码一致。

具备较强的系统抽象与工程落地能力：既能完成回合制战斗、NPC 决策、情境系统等逻辑模块，也能处理 Unity 动画、角色移动、场景生成、交互原型和资产校验。重视确定性、可测试性、故障前置暴露和交付状态透明，能够独立推进中大型个人项目。

## 专业技能

- **编程语言：** 熟悉 C#、Python；能够使用强类型领域模型、接口抽象、不可变数据、依赖注入和确定性随机数组织复杂游戏逻辑。
- **Unity：** Unity 6、Animator、Animation Clip、Humanoid/Mixamo、CharacterController、Animation Rigging/TwoBoneIK、PlayMode Test、Editor 扩展、场景与资产生成。
- **.NET：** .NET 8、xUnit、Console Harness、JSON 序列化、自定义转换器、Fail-fast 数据加载；了解 `net8.0` 与 `netstandard2.1` 的兼容边界。
- **数据与算法：** JSON Schema 风格契约、图结构、矩阵运算、状态机、概率模型、数值模拟、跨语言 Fixture 校验。
- **工程实践：** Git/GitHub、GitHub Actions、CI 门禁、依赖锁定、可复现构建、自动化校验器、Issue 依赖与验收管理。
- **内容与工具链：** Blender 5.x/4.5、`bpy`、控制骨与动作烘焙、FBX 回导、Unity Editor Builder、批量数据生成与迁移、技术规格和架构文档编写。

## 项目经历

### 《子非鱼》（You Are Not the Fish）——3D 回合制心理对抗游戏

**独立游戏开发者 / 系统设计与核心开发** ｜ **2026.07—至今**

**技术栈：** C#、.NET 8、Unity 6、Python、JSON、xUnit、GitHub Actions、Blender

#### 项目介绍

以神经科学为规则骨架、以“意识外显效应”为世界观的 3D 回合制心理对抗游戏。玩家在医院及意识空间中探索，通过物理攻击、精神攻击、防御和技能组合处理同时具有 HP 与 SAN 的敌对实体。项目采用设计驱动开发，目前处于纸面设计、数值模拟验证和引擎核心实施阶段；逻辑引擎、情境系统、NPC 决策和 Unity 呈现实验均已形成可运行技术载体，正式可玩切片仍在集成。

#### 核心职责

- 独立负责核心玩法、战斗规则、角色与疾病体系、NPC 行为、情境与事件、技能树、关卡空间及呈现管线的设计和工程落地。
- 维护从设计正典、结构化数据、逻辑引擎到 Unity 表现层的完整链路，并为不同层次建立可执行的验收标准。
- 编写自动化测试、数据校验器、生成工具和 CI 工作流，处理跨文件参数漂移、无效引用、资产 GUID 丢失及环境不可复现等工程问题。

#### 一、逻辑引擎与回合制战斗

- 使用 C#/.NET 8 构建独立于 Unity 的规则引擎，将规则结算放入无第三方运行时依赖的 Core 类库，使核心逻辑可以在命令行和测试环境中快速执行。
- 设计并实现 `TurnManager → ActionResolver → DamageCalculator → EventProcessor` 回合处理链，覆盖行动收集、速度计算、行动序生成、结算事件、倒地判定、回合收束和战斗结束条件。
- 建立 HP/SAN 双资源模型，区分物理攻击、精神攻击和防御等行动后果，避免将心理对抗简化为传统单血条伤害系统。
- 将随机数抽象为 `IRng`，提供固定种子的确定性实现，使行动序、概率分支和 NPC 选择能够稳定复现，便于回归测试与问题定位。
- 实现 `CombatState`、`ParticipantState`、`CombatAction`、`TurnResult` 等强类型领域对象，将战斗状态、输入动作和输出事件解耦，方便后续接入 Unity 或其他呈现载体。
- 开发 Console Harness，支持 `--seed`、情境演示和静息态轨迹等参数，可在无 Unity 环境下运行完整结算并输出可审查结果。

#### 二、神经模型、情境系统与 NPC 决策

- 将神经科学模型整理为 69 个功能脑区、51 个图节点和 1049 条连接，使用结构化数据表达皮层连接、脑干广播、CSTC 回路和特权通路等不同通信关系。
- 设计 69×8 感官输入矩阵，将视觉、听觉、躯体感觉、痛觉、社会认知、语言认知、嗅觉和温度感觉映射到脑区；将“事件到脑区”的大规模手工表拆为“事件→感官模态→脑区”两级映射，降低扩展成本。
- 实现 27 类情境原型选择器，根据环境、角色状态和概率强度选择当前全局情境，并将事件刺激、环境基调和月光场三个通道合成为神经输入。
- 实现皮层偏置、W 矩阵构建、皮层动力学、CSTC 门控、威胁判断、环境基调更新和月相状态等模块，将理论模型转换为可逐回合计算的系统。
- 实现 NPC Salience 决策，让 NPC 根据脑区链路、当前状态与行动候选集选择行为；决策过程使用统一随机源，可通过固定种子重放。
- 通过 Python 数值实验验证意识动力学、情境差异、NPC 行为和月光场模型，共维护 15 个模拟脚本，将研究性实验与正式运行时逻辑隔离。

#### 三、疾病与内容数据管线

- 建立疾病作为一等实体的内容模型，整理 8 个父类、26 类疾病及 348 条病理边，支持严重程度分档、左右偏侧偏移和病理连接调制。
- 将全部病理边映射到统一神经连接图，并通过校验器保证 348 条边均命中已有连接，避免内容侧随意添加无法解释或无法计算的链路。
- 使用 YAML Front Matter 与 JSON 组合管理疾病文档和机器数据，使策划可读描述与程序可消费参数能够相互引用、独立校验。
- 编写数据迁移、脑区重命名、路径修复、技能映射、脑图生成及内容卡片生成工具，支持数据模型演进时的批量机械更新。

#### 四、数据契约与故障前置处理

- 编写 `GameDataLoader` 统一加载运行时 JSON 数据，对缺失字段、空集合、重复 ID、非法枚举、Schema 主版本、矩阵形状及跨表引用执行 Fail-fast 校验。
- 针对 C# 反序列化默认值可能掩盖“字段缺失”的问题，直接检查原始 JSON 必需键，防止缺失嵌套对象被默认空对象静默接受。
- 建立 53 条正反例 Runtime Fixture，同时由 Python 和 C# 消费；在 CI 中逐条比较两侧接受/拒绝集合，确保工具链与游戏运行时对同一数据给出一致判定。
- 使用数据清单声明运行时允许读取的文件，区分生成期输入、运行时契约和研究数据，避免实验数据被意外接入正式逻辑。
- 通过术语注册表维护活跃、废弃及替代术语，配合校验器阻止旧模型和旧参数重新进入现行设计。

#### 五、Unity 呈现与交互原型

- 使用 Unity 6 搭建 ActionLab、WalkerLab 和 BrainView 等呈现实验载体，分别验证角色动作、第三人称移动和脑区网络可视化。
- 建立 17 项受控动作语义词表，以稳定 Action ID 隔离玩法语义和具体动画资源；覆盖基础移动、战斗、坐立以及单手提举/双手持握等状态，并支持动作变体和状态间切换。
- 接入 Mixamo X Bot 与 Humanoid 动画，编写 Animator 状态驱动和动作播放器，支持循环动作、一次性动作、受击打断、倒地保持及动作结束后的状态回切。
- 基于 CharacterController 实现相机相对的走、跑、跳、重力、落地和坡度处理，并为连续移动、跳跃回切及状态补发编写 PlayMode 测试。
- 实现“找到椅子才能坐”的交互流程，包括距离与朝向判定、座位占用锁、角色对齐、逐状态根位移和站起释放，避免仅通过按键播放孤立坐姿动画。
- 实现脑区节点与连接的 3D 可视化组件，可根据数据生成节点、连线、视角旋转和基本交互，用于检查神经模型的空间表达。
- 编写多个幂等 Unity Editor Builder，自动创建场景、角色层级、Animator Controller 和动作绑定，减少手工搭建造成的配置漂移。
- 开发 Unity 资产校验器，检查 `.meta` 成对性、GUID 唯一性、场景/Controller 引用可解析性及 FBX Rig 配置，使资产身份问题无需打开 Editor 即可在 CI 中发现。

#### 六、Blender 人形动作生产与 Unity 回导管线

- 以 Mixamo X Bot 为载体建立 Blender 人形动作母版：保留 65 根 `mixamorig` 变形骨和单一 Hips 根节点，新增 13 根非变形 `CTRL_*` 控制骨，通过约束层驱动原骨架，兼顾动作编辑效率与 Unity Humanoid 兼容性。
- 编写母版生成器与动作导出器，将控制骨动作烘焙到变形骨并导出 animation-only FBX；自动断言导出物为 65 骨、0 控制骨、0 网格、单根结构，避免调试控制层泄漏进运行时资产。
- 建立 Blender 5.1.2 基线和 4.5.x 兼容取道层，封装动作 Slot/F-Curve 等版本差异；双版本产物逐骨内容等价误差控制在 **0.000967 mm**。
- 为烘焙和回导设计 V1–V9、E0–E6 两组自检，实测烘焙保真误差 **0.0011 mm**、Blender 回导误差 **0.0014 mm**，并验证骨长、Rest Pose、父子关系与帧号偏移。
- 发现“源动作与导出物同时静止仍可让两侧误差测试通过”的平凡通过问题，增加动作位移量非退化断言；同时修复 `nla.bake` 摘除当前 Action、Unity 动画压缩丢失根位移关键帧及 Controller 重建导致 GUID 失效等问题。
- 在 Unity 中实现幂等回导调试 Lab，支持播放/暂停、逐帧步进、复位及 Hips/双手/双脚实时读数；同帧重复采样结果逐字节一致，并用数值代理检查骨架比例跳变和翻转。
- 接入 Unity Animation Rigging 1.4.1，搭建右手与双腿 Three-Chain TwoBoneIK 接触修正实验；以手部目标距离、足底高度、根漂移、骨长不变量和脚滑量等指标比较约束开关效果，并烘焙独立派生 Animation Clip。
- 开发 Blender 活体桥：通过带 Token 的 JSON/TCP 协议把代码送入已打开的 Blender 会话，由主线程队列安全调用 `bpy`，支持超时、长输出落盘和实时预览；实测活体预览与 headless 权威路径 **563 个可比数值全部一致**。
- 建立“自然语言动作需求→参考系与自由度→产物侧量化判据→实时回显→人工目视”的动作验收流程，使“看起来不对”的反馈可以定位到掌面、指尖、朝向或接触距离等具体指标。

#### 七、自动化测试与持续集成

- 为逻辑引擎建立 **416 项 xUnit 测试**，覆盖数据加载、战斗结算、行动序、NPC 决策、情境选择、神经动力学、月相状态、Console 行为和异常输入。
- 编写 **14 个 Python 校验器**，覆盖文档死链、段落引用、参数一致性、空间坐标、疾病映射、情境 ID、数据清单、Runtime Fixture 和 Unity 资产等约束。
- 配置 GitHub Actions 四任务工作流：文档与数据完整性、.NET 恢复/构建/测试、Issue 契约校验，以及对当前不可用 Unity CI 环境的显式状态报告。
- 将“未执行”与“测试通过”严格区分；Unity Editor 无法在 Ubuntu Runner 执行时，CI 明确报告 `NOT_AVAILABLE`，防止工作流表面全绿但实际漏测。
- 修复校验编排器因路径错误而“运行 0 个校验器仍返回成功”的静默假绿问题，增加找不到校验器即失败的保护，并与 CI 注册清单逐项对齐。
- 锁定 .NET SDK、Unity Editor 版本和 NuGet 完整依赖图，启用 Locked Mode；通过空包缓存和干净检出验证恢复、构建与测试不依赖开发机残留文件。
- 建立 `netstandard2.1` Unity 桥接工程，以链接源码而非复制的方式复用 Core 最小依赖闭包，并将桥接编译加入 CI，避免 Unity 兼容版本与主引擎源码长期分叉。
- Blender/Unity 管线验收基线记录 **64 项 PlayMode：63 通过、0 失败、1 跳过**；新增 8 项回导测试覆盖骨架、采样稳定性、逐帧控制与资产身份。

#### 八、项目架构与协作流程

- 设计 `design → data → code/src → code/unity` 单向依赖架构：设计文档定义规则，JSON 提供机器契约，Core 负责唯一逻辑结算，Unity 仅负责呈现和输入。
- 对代码中的数值常量要求标注设计来源，通过参数注册表与跨文件校验避免同一参数在文档、数据和代码中产生不一致副本。
- 建立 Issue 准入、依赖、门禁和验收字段，并为 Issue 校验器编写正反例测试，使需求拆分和“完成”判定同样可以机械检查。
- 用“设计声明 / 代码实现 / 系统集成 / 试玩证据”四轴记录功能状态，避免把文档完成、单元测试通过或孤立技术 Demo 错报为完整产品交付。
- 维护架构说明、构建测试手册、工作流、工程危险点和设计决策记录，使复杂项目能够从文档快速定位规则来源、实现边界与已知风险。

## 代表性技术成果

| 方向 | 成果 |
|---|---|
| 逻辑引擎 | 独立 .NET 8 规则核；确定性回合结算；HP/SAN 双资源；NPC、情境和神经动力学模块 |
| 数据规模 | 69 个脑区、51 节点、1049 条连接、69×8 感官矩阵、27 类情境原型 |
| 内容系统 | 8 个疾病父类、26 类疾病、348 条病理边，全部通过连接图契约校验 |
| 自动化测试 | 416 项 xUnit 测试；53 条 Python/C# 共享 Fixture；14 个数据与文档校验器 |
| Unity 原型 | 17 项动作语义层、Humanoid 动画、第三人称移动、就座交互、Animation Rigging 接触修正、脑区网络可视化 |
| 动画管线 | 65 骨/13 控制骨母版、Blender 4.5/5.x 兼容、animation-only FBX、逐帧 Unity 回导 Lab、实时 `bpy` 桥接 |
| 工程化 | GitHub Actions 四任务 CI、依赖锁定、干净检出验证、Unity 资产 GUID 校验、Issue 契约 |

## 项目亮点总结

- **复杂领域建模：** 能将神经科学、心理疾病和游戏玩法交叉领域转化为明确的类型、状态、公式、图数据及运行时接口。
- **数据驱动设计：** 玩法参数和内容不散落在表现代码中，通过 JSON 契约、集中配置和自动生成工具支持持续迭代。
- **强调可复现性：** 固定种子 RNG、锁定依赖、跨语言 Fixture 和干净检出测试共同保证问题可重现、构建可复现。
- **覆盖完整开发链路：** 从机制设计、数值实验和 Core 实现，到 Blender 动作生产、Unity 回导与接触修正、资产管线、测试和 CI 均有实际项目产出。
- **重视真实交付：** 主动记录已完成、部分完成、未集成和未验证事项，不以技术原型代替玩家可体验的完整切片。

## 教育经历

### [学校名称]｜[专业]｜[学历]

[入学年月]—[毕业年月]

- 主修课程：[数据结构、计算机图形学、操作系统、软件工程等——按实际情况填写]
- 在校经历：[奖项、比赛、社团、课程项目——按实际情况填写]

## 工作 / 实习经历

### [公司名称]｜[职位]

[开始年月]—[结束年月]

- [使用“做了什么 + 如何实现 + 结果如何”的结构填写]
- [如暂无正式经历，可删除本节并强化个人项目与作品集]

## 其他信息

- 英语水平：[CET-4 / CET-6 / 技术文档阅读能力]
- 技术博客：[链接]
- 游戏作品集或演示视频：[链接]
- 到岗时间：[填写]
