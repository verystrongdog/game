# Python
__pycache__/
*.py[cod]
*.pyo

# macOS
__MACOSX/
.DS_Store
._*

# Windows
*:Zone.Identifier
Thumbs.db

# IDE / Editor
.claude/
.vscode/
.idea/

# Unity (future)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]in/
/*.csproj
*.unityproj
/*.sln
*.userprefs

# Godot (if ever used)
.godot/

# Large reference PDFs — uncomment if you want to exclude them
# *.pdf
# 3D 脑模型资产（742 MB，主体不入库）
# 只放行「正典源 + 冻结快照」——登记与裁定见 design/presentation/visualization-3d/脑模型资产登记.md
design/presentation/visualization-3d/blender_assets/*
!design/presentation/visualization-3d/blender_assets/brain_skill_tree_backup_20260719_0158.blend
!design/presentation/visualization-3d/blender_assets/brain_skill_tree.glb
# 节点落在脑区 mesh 之外的核对产物（证据，9 KB）
!design/presentation/visualization-3d/blender_assets/verify_result.json

# Large reference data — re-downloadable from public sources
data/connectivity/abagen/

# 模拟实验结果（生成物，不进版本控制）
data/sim_results/

# .NET 沙箱内本地 NuGet 缓存（build 产物，不进版本控制）
.nuget-pkgs/

# 子代理/工具临时抓取（含 compare_fixture_verdicts.py 的中间判定文件）
.evtmp_*/

# 外部 AI 会话转写存档（过程物，仅本地；chatX 为 #117 输入已跟踪例外）
参考/会话存档/*
!参考/会话存档/chatX_transcript.txt

# .scratch——过程工作区，整体移出版本控制（2026-09-12 仓库重构 Phase 2 / D11）
#
# 迁出去向：
#   · 被代码或设计正典消费的承重内容 → 规格/（引擎规格 / 素材 / 数据源）
#   · 被决策树、六维状态或正典引用的各轮 grilling 源记录 → 设计归档/grilling/
#
# 保留在工作区、仅本地：外审存档 HTML、运行日志、运行状态、素材正文（版权内容）、
# 子代理临时文件、以及未被任何活跃文档引用的过程残留。
.scratch/

# .trash——垃圾桶。2026-09-13 整份移出仓库（原 design/archive/trash/，26 个文件 848K）。
# 本地保留、可查，但不入库：归档隔离由「不在场」保证，不再靠校验器守一份枚举清单。
# 历史仍可从 git 历史取回（`git show <sha>:design/archive/trash/<路径>`）。
.trash/

# 校验器运行状态（原 .scratch/.last_check_state.json）
.checks-state.json

# Unity editor install (local headless validation, Grilling #122)
.unity-editor/
.nuget-packages/
.nuget-http-cache/

# Unity project artifacts（2026-09-12 Phase 3：unity/ 迁至 code/unity/，前缀同步）
code/unity/[Ll]ibrary/
code/unity/[Tt]emp/
code/unity/[Oo]bj/
code/unity/[Ll]ogs/
code/unity/[Uu]serSettings/
code/unity/*.csproj
code/unity/*.sln
code/unity/Assets/Plugins/*.pdb

# Unity CLI local install / licensing (sandbox, Grilling #122)
.unity-home/
code/unity/*.log

# Unity 资产——**刻意不入库**（2026-09-12 #136 裁定，范围与理由见 code/unity/README.md §二·H）
#
# Kevin Iglesias Human Animations（68 MB 第三方资产包）：来源已整条切 Mixamo
# （design/presentation/动作库规格.md §五），且 #139 的验收标准明写「干净检出下
# Assets/Kevin Iglesias/ 不存在」——故它既不入库、也不得再作为受控文件的引用目标。
# 它只存在于本机工程里，供 KiWalkerLab 历史对照场景使用。
code/unity/Assets/Kevin Iglesias/

# Assets/Temp/：站↔坐探针与截图过程物（#137 的核对面），不是工程资产
code/unity/Assets/Temp/

# 仓库重构前的本地安全备份（bundle + 工作区快照，仅本地，重构完成后删除）
.refactor-backup/

# CI / 构建产物（P4a）
artifacts/
.ci-probe-pkgs/
*.trx
TestResults/

# Agent 本机文件（P4a：不得入库——含机器绝对路径；.claude/ 已整体忽略，
# 以下为曾被强制入库者，显式列出防复发）
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/projects/
