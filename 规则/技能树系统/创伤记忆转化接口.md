# 创伤记忆转化接口

> 创伤记忆（病因层）→ 疾病（后果层）的量化转化规则正典。定义「一条创伤记忆如何在 NPC/玩家身上落地为疾病档位与病理边 m 偏移」。设计依据：Grilling #88（Q1a 致病立场 C 定案 + 档位映射双维模型）+ #87 创伤记忆语义 + #90 记忆内容层 schema。**结构已锁定，数值为 `[NEW 初值可调]`（初值基于临床直觉 + 量级锚定，待模拟验证后修订）。**

## 目录

1. [定位与边界](#一定位与边界)
2. [输入：创伤记忆载体](#二输入创伤记忆载体)
3. [转化链三步](#三转化链三步)
4. [参数速查表](#四参数速查表)
5. [演变：档位状态机](#五演变档位状态机)
6. [修正通道](#六修正通道)

---

## 一、定位与边界

**转化接口回答**：创伤记忆的连续量（L 总负荷、I_max 单次烈度）如何映射为疾病的离散档位（轻/中/重）与病理边 m 偏移。

```
创伤事件 e_i（连续）          ← #87 生成功能产出
  ↓ ΔI = k × A
创伤记忆 R（连续：I_mem, L）   ← #90 存储（I_mem ∈ [0,1] 是强度非档位）
  ↓ f(L, I_max)              ← 本文档定义（转化函数）
疾病档位 s ∈ {∅,轻,中,重}（离散）← 疾病层才有档位
  ↓ 实例化 + B′ 过滤
m = m_offset[s]（病理边模板值）
```

**边界**：
- 创伤记忆**没有档位**——I_mem 是连续强度、L 是连续负荷；"轻中重"只存在于疾病层
- 本接口不定义疾病内容（26 病 + 348 病理边已完成，`pathology_edges.json`）
- 本接口不定义记忆存储/检索（#90 已定义）

**致病立场（Q1a-2，C 定案）**：创伤 = 累积到阈值。无创伤 + 无器质 + 非发育 → 健康（正常人模板 m 基线 0.3-0.5）。未达单次阈值的重复挫折经剂量-反应累积（L = ΣΔI×g）突破阈值 → 创伤记忆。不引入素质/遗传维度。

## 二、输入：创伤记忆载体

| 符号 | 定义 | 值域 | 来源 |
|------|------|------|------|
| A | 事件烈度 | ∈ [0,1]（6 特征：侵入性/现在感/碎片化/生动性/生理反应/牢固度，开放集合） | #87-D6 |
| k | 强度系数 | > 0（基准 1.0，人物脆弱性调制 [NEW] 后置） | #88 输入约定 |
| g | **不成熟度** | ∈ [0,1]（成熟者 g→0，不成熟者 g→1） | #88 定案（g = 不成熟度，公式零改动） |
| ΔI | 单次事件强度增量 | = k × A | #90 §3.1 |
| L | 总负荷 | ≥ 0 无上界，= Σ ΔI_i × g（剂量-反应，Kolassa 2010） | #90 §3.2 |
| I_mem | 记录强度 | ∈ [0,1]（单条记忆载荷字段） | #87-D6 |
| I_max | 历史最高单次 ΔI | ∈ [0,1]（记忆存储层） | #88 新增（急性维度） |
| N/F/R | 表征形态 | 叙事化/碎片化/现在感 | #87-D6 |

## 三、转化链三步

### 3.1 Step 1：病理化判定（双阈值）

```
病理化 ⟺ (I_max ≥ A_trauma_I) ∨ (L ≥ A_trauma_L)
  单次毁灭性事件（I_max 高）或 长期累积（L 高），任一达标即病理化
  否则 → s = ∅（非病理化：创伤记忆作为叙事/记忆存在，不致病）
```

### 3.2 Step 2：疾病选择（M:N 候选集模型，2026-08-21 修正）

**临床事实（已核验）**：1 创伤 → 多疾病（单次灾难 → PTSD+MDD+SUD 共病）与 N 创伤 → 1 疾病（多创伤累积 → 单一抑郁）均存在。转化关系为 **M:N 多对多**。

```
记忆层：Rᵢ → C(Rᵢ)（候选疾病集，1→N）
        事件类型 → 候选集（如 性创伤 → {PTSD, DID, BPD}，临床常见共路径）
        N/F/R 表征 → 选主病（碎片化 F → PTSD/解离、叙事化 N → 抑郁谱、现在感 R → 焦虑谱）
        其余候选 = 卫星病（聚合达标才成型）
  ↓ 聚合
疾病层：D_j 负荷 = Σ {L(Rᵢ) : D_j ∈ C(Rᵢ)} + I_max 加成
        D_j 档位 = f(聚合负荷)（沿用 §3.3 双维模型，输入变为聚合量）
  ↓
共病：多个 D_j 各自聚合达标 → 同时存在（N→1 + 1→N + N→N 全覆盖）
```

- 映射范围 = **全 25 经历型**（26 − ASD 发育性；#87-D3"18 种"为扩种前数字，Q0.2 扩种后更新）
- 事件类型枚举 = **开放集合**（同烈度 6 特征惯例，#87-D6），存独立数据文件；**增 = 默认未映射态（WARNING）+ 兜底"慢性冲突/高压"**，**删 = ERROR 护栏**（#86 S2+ 演化策略同款）
- 候选集映射表 C(e) = **[NEW] 待逐事件类型填充**（8 初始类型，见下节）

### 3.2.1 事件类型 → 候选疾病集（✅ 定案 2026-08-21，24 经历型全覆盖）

> **分类调整（2026-08-21 转化接口讨论）**：ADHD 移出经历型 → 发育性（遗传度 ~74%，Faraone & Larsson 2019，与 ASD 同性质，不参与创伤转化，走 W_npc 直调）。经历型 = 26 − ASD − ADHD = **24 种**。
>
> **立场 B（创伤 = 充分条件之一）**：创伤记忆 → 疾病是**一条路径**（创伤主导型的主要路径、应激触发型的可选路径）。无创伤 NPC 走「素质-应激兜底」（遗传素质 × 慢性应激，低概率自发，由 NPC 生成参数控制——[NEW 待 #87 生成功能定义]）。
>
> **卫星病机制**：主病（N/F/R 选择）正常成型；卫星病需**独立聚合阈值 ≥ 1.5**（防 MDD 泛滥，临床"多创伤→更重抑郁"由聚合自然表达）。

| 事件类型 | 候选集 C(e)（主病 + 卫星） | 临床锚 |
|---------|--------------------------|--------|
| 威胁/暴力 | {PTSD 主, 惊恐, 特定恐惧, **偏执型 SZ 卫**} | 急性威胁 RDoC；SZ 创伤路径 Varese 2012（OR 2.87） |
| 丧失/哀悼 | {MDD 主, PDD, BD-I, BD-II, 环性} | 丧失 RDoC；双相谱全覆盖（轻躁狂代偿） |
| 性创伤 | {PTSD 主, DID, BPD, BN 卫, AN 卫, BED 卫} | 高烈度跨域；进食障碍后遗症（暴食谱 BN/BED 共享） |
| 社会伤害（霸凌/排斥/羞辱） | {BPD 主, 社交焦虑, 分裂型, BN 卫, BED 卫, **未分化 SZ 卫**} | 社会过程 RDoC；SZ 创伤路径 |
| 忽视/遗弃 | {BPD 主, MDD, ASPD, **分裂情感 卫**} | 依恋损伤；早期剥夺→情感钝化 |
| 生理/疾病（重病/手术/疼痛） | {躯体症状 主, 惊恐, MDD} | 内感受 RDoC |
| 经济/剥夺 | {SUD 主, MDD, 拖延, **环性 卫**} | 慢性应激 |
| 慢性冲突/高压 | {GAD 主, MDD, OCD, SUD, **偏执型 SZ 卫**} | 慢性应激 + 未映射兜底 |
| （新增）控制剥夺/完美主义压迫 | {AN 主, OCD} | 进食障碍特异：控制主题 |
| （新增）公开羞辱/失败 | {社交焦虑 主, BN, BED, 未分化 SZ} | 评价创伤；暴食谱共享 |

**覆盖验证**：24 经历型全部 ≥1 事件类型可达；MDD 作为共同终点（5 事件类型）是临床事实（Kendler 2011 应激-抑郁模型），由卫星阈值 ≥1.5 管控。

### 3.2.2 N/F/R → 主病选择（✅ 定案 2026-08-21，偏好表软边界）

**表征输入**：`(N ∈ [0,1], F ∈ [0,1], D_seg ∈ [0,1])` 三维连续——N 叙事化（VAM，Brewin 1996）、F 碎片化（SAM；R 现在感由 F 派生 ≈ F，Ehlers & Clark 2000）、**D_seg 区隔化**（记忆分区存储，对接 #90 COMPARTMENTALIZED 状态；DID 核心机制，非凑数）。**非三分枚举**。

**匹配机制（B 方案，软边界）**：
```
主病 = argmax_{d ∈ C(Rᵢ)} sim((N,F,D_seg), (N_d, F_d, D_seg_d))
sim = 负欧氏距离：sim = −√((N−N_d)² + (F−F_d)² + (D_seg−D_seg_d)²)
软边界：表征连续变化时主病平滑切换；确定性函数（同输入同输出）
```

**疾病表征偏好表 (N_d, F_d, D_seg_d)**（24 经历型，[NEW 初值待文献复核]）：

| 疾病 | N | F | D_seg | 文献/机制锚 |
|------|:---:|:---:|:---:|-----------|
| PTSD | 0.15 | 0.95 | 0.10 | 闪回/侵入，SAM 主导（DRT） |
| DID | 0.10 | 0.98 | **0.95** | 区隔化碎片（#90 COMPARTMENTALIZED） |
| 惊恐 | 0.30 | 0.85 | 0.05 | 发作性体感碎片 |
| 特定恐惧 | 0.35 | 0.80 | 0.05 | 条件化碎片 |
| 社交焦虑 | 0.50 | 0.60 | 0.05 | 可叙述但情绪碎片 |
| 躯体症状 | 0.30 | 0.75 | 0.05 | 内感受碎片 |
| 偏执型 SZ | 0.35 | 0.75 | 0.10 | 威胁碎片 |
| 分裂型 | 0.40 | 0.70 | 0.10 | 知觉碎片/源监控失败 |
| 未分化 SZ | 0.40 | 0.70 | 0.10 | 同谱系 |
| 分裂情感 | 0.45 | 0.65 | 0.10 | 情感混合 |
| BPD | 0.55 | 0.65 | 0.15 | 情绪记忆高反应但可叙述 |
| 环性 | 0.70 | 0.40 | 0.05 | 轻躁狂叙事 |
| 双相 II | 0.65 | 0.45 | 0.05 | 混合 |
| 双相 I | 0.60 | 0.50 | 0.05 | 躁郁混合 |
| BN/BED | 0.55 | 0.50 | 0.05 | 暴食循环记忆 |
| 拖延 | 0.60 | 0.35 | 0.05 | 努力折扣叙事 |
| AN | 0.65 | 0.40 | 0.05 | 控制叙事 |
| SUD | 0.65 | 0.35 | 0.05 | 依赖叙事 |
| ASPD | 0.70 | 0.30 | 0.05 | 冷漠叙事化 |
| GAD | 0.75 | 0.30 | 0.05 | 泛化担忧反刍 |
| OCD | 0.80 | 0.25 | 0.05 | 侵入想法但叙事完整 |
| MDD | 0.90 | 0.20 | 0.05 | 反刍 VAM 主导 |
| PDD | 0.95 | 0.10 | 0.05 | 慢性叙事 |

> **区分度验证（2026-08-21）**：PTSD (0.15,0.95,0.10) vs DID (0.10,0.98,0.95) 欧氏距离 = √(0.0025+0.0009+0.7225) ≈ 0.852——三维分离生效（D_seg 主导），候选集 {PTSD, DID, BPD} 内 DID 可独立成为主病 ✓。D_seg 由记忆区隔化标记（#90 生命周期状态）提供。

### 3.3 Step 3：档位映射（双维模型，聚合输入）

```
D_j 档位 = clamp( g(L_agg) + h(I_max_agg), ∅, 重 )
  L_agg = Σ {L(Rᵢ) : D_j ∈ C(Rᵢ)}      （指向该病的全部记忆聚合负荷）
  I_max_agg = max {I_max(Rᵢ) : D_j ∈ C(Rᵢ)}  （指向该病的最高单次烈度）
  g(L_agg)：L_agg < 1.0 → ∅；1.0 ≤ L_agg < 2.0 → 轻；2.0 ≤ L_agg < 3.5 → 中；L_agg ≥ 3.5 → 重
  h(I_max_agg)：I_max_agg ≥ 0.9 → +1 档（单次毁灭直跳重档）
  B′ 过滤：档位落在该病可用档内（DID 无轻档→实际 m 第一档 0.0 占位）
```

### 3.4 Step 4：实例化

```
m = 疾病模板 m_offset[s]    （pathology_edges.json 每边三档值，s 索引）
档位切换时 m 跳变（演变 = 档位状态切换，非 m 连续变化；保持 :1042 m 固定值决策）
```

## 四、参数速查表

| 参数 | 符号 | 初值 | 标注 | 位置 |
|------|------|------|------|------|
| 强度系数 | k | 1.0 | [NEW 初值可调] | §二 |
| 不成熟度 | g | ∈ [0,1]（NPC 由 #87 生成，玩家预设） | [NEW 初值可调] | §二 |
| 单次创伤判定阈值 | A_trauma_I | 0.7 | [NEW 初值可调] | §三.1 |
| 累积创伤判定阈值 | A_trauma_L | 1.0 | [NEW 初值可调] | §三.1 |
| 轻档阈值 | θ_L1 | 1.0 | [NEW 初值可调] | §三.3 |
| 中档阈值 | θ_L2 | 2.0 | [NEW 初值可调] | §三.3 |
| 重档阈值 | θ_L3 | 3.5 | [NEW 初值可调] | §三.3 |
| 急性加成阈值 | θ_I | 0.9 | [NEW 初值可调] | §三.3 |

**初值锚定说明**：3 次中度挫折（A=0.3, g=1）→ L=0.9 未达标 = 健康；4 次 → L=1.2 = 轻病。"长期高压"约 4+ 次才轻病——量级对齐 m 分布（0.1-0.3）与敌人三档基线。

## 五、演变：档位状态机

```
s(t) ∈ {∅, 轻, 中, 重}
  升档：L 增长跨阈值（新创伤事件 ΔI 累积）→ s 升
        I_max 更新（更强事件）→ 急性 +1
  降档：治疗/干预 → #90 ΔI_int < 0（再巩固改写负向）→ L 下降 → s 降
        （P_acc 衰减不降档——铁律：遗忘不作用于 m，创伤记忆不随时间衰减）
  特殊：#90 RESET（DID 区隔/Boss 击败清零）→ s = ∅ 或换病
```

档位迁移由 `L(t)`/`I_max(t)` 实时驱动（#90 生命周期更新这些量），转化函数 f 不变。

## 六、修正通道

- **模拟验证**（战斗蒙特卡洛/NPC 生成测试）发现不合理 → 校准批次修订（数值表，不动结构）
- **设计评审**发现违反直觉 → Grilling Step 6 闭合后修正（决策树 🔧 行 + issue 评论）
- 结构（双阈值判定/双维档位/g 不成熟度公式/状态机）**已锁定**，修改需走完整 grilling

---

*创建: 2026-08-21 | 更新: 2026-08-21*
*关联: [grilling-88](../.scratch/grilling-88-etiology-disease-neural/grilling-88.md), [记忆内容层](记忆内容层.md), [pathology_edges.json](../../data/connectivity/pathology_edges.json), [创伤记忆机制-文献综述](../../参考/文献/创伤记忆机制-文献综述.md)*
