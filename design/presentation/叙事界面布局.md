# 叙事界面布局

> 探索与对话共存时的屏幕分区、阅读节奏和输入反馈。本文只定义呈现层，不新增剧情状态、检定公式或正式玩家行为。

## 一、目标与边界

叙事界面需要同时保留两个注意对象：玩家正在身处的医院，以及人物此刻说出或被玩家感知到的内容。界面采用“左侧世界、右侧叙事流”的稳定结构，使对话发生时不切走场景，也不把世界降为对话框背景。

原型借鉴调查型 CRPG 的阅读节奏，但不复制外部作品的素材、字体、图标或具体视觉资产。《子非鱼》的识别来自医院夜景、月光、病历纸张、低饱和青灰色与意识扰动。

本文不决定：

- 对话节点的正式文本与分支；
- 检定概率、SAN 变化或关系变化数值；
- StoryEngine 的数据契约与实现；
- 战斗 HUD 如何与叙事栏同时出现。

## 二、屏幕结构

### 2.1 宽屏

宽屏由两个永久区域组成：

```text
┌──────────────────────────────┬──────────────────┐
│                              │ 地点 / 时段 / 状态 │
│                              ├──────────────────┤
│          游戏画面             │                  │
│                              │    叙事历史流      │
│                              │                  │
│                              ├──────────────────┤
│ 场景目标 / 交互提示 / 玩家状态 │    回应选项       │
└──────────────────────────────┴──────────────────┘
```

- 左侧占主要面积，承担空间、人物站位、环境变化和可交互物提示。
- 右侧是独立滚动容器，新内容追加到底部，旧内容保留以支持回读。
- 选择区固定在右侧底部，历史增长不得把当前选项推出屏幕。
- 分栏可拖动调整，但原型阶段只验证固定比例。

### 2.2 窄屏

窄屏改为上下布局：场景在上、叙事在下。对话选项仍固定在叙事区域底部；不使用覆盖全屏的模态框。

## 三、叙事信息层级

右侧叙事流只使用以下类型：

| 类型 | 作用 | 呈现 |
|---|---|---|
| 说话者 | 建立当前社会关系 | 姓名、小型身份描述、可选肖像占位 |
| 客观描述 | 对应剧情节点 `text` | 常规正文 |
| 呈现变体 | 对应 `perceptions` | 与客观描述分层，标明观察者来源；不使用虚构的“感知回声”说话者 |
| 经历影响 | 玩家已选经历触发的额外识别或措辞 | 琥珀色文字 + “经历”标签 + 左边线；无对应经历时不出现 |
| 疾病影响 | 疾病的打开、关闭、代价及四种表现形态 | 按闯入/信念/接管/摆荡使用不同文字样式，同时保留疾病名和形态标签 |
| 内在反应 | 玩家角色内部的竞争性解释 | 独立标签与边线，不伪装成客观事实 |
| 系统回声 | 线索、承诺、自问变化 | 短句提示，可进入笔记本复查 |
| 玩家回应 | 当前可选行动 | 编号、回应正文、已知前提；未知后果不预告 |

感知文本必须遵守[剧情系统设计 §3.11](../events/剧情系统设计.md)的客观层与呈现层分离；界面可以让两层看起来不同，但不得把感知版本写成世界真相。

## 四、交互规则

1. 点击回应后，该回应进入历史流并锁定，随后追加 NPC 或环境反馈。
2. 当前选项支持数字键选择；焦点状态必须同时具有形状、边线或位移反馈，不能只依赖颜色。
3. 对话与场景不互相遮挡。对话期间左侧允许发生灯光、站位、人物注视等呈现变化。
4. “对话 / 笔记 / 开发者选项”页签只切换右栏内容，不暂停或回滚世界状态；开发者选项只收纳原型调试入口，不作为玩家世界内行为。
5. 笔记页直接复用[任务目标系统 §二](../events/任务目标系统.md)的承诺、线索、自问三层，不新增第四种任务分类。
6. 离开会话后状态不回滚，符合[剧情系统设计 §3.7](../events/剧情系统设计.md)的会话语义。
7. 每名 NPC 的台词、选项与三档对话深度独立存放；HTML 只提供容器，对话运行时按时段、关系、见证标记、玩家经历与疾病选择入口。
8. 右侧对话栏不提供可远程发起交谈的人物列表。二维地图必须与 NPC 位于同一逻辑地点，三维场景必须进入人物近距离交互范围，才显示“开始交谈”；人物档案可以先查看，但不得绕过空间距离直接打开对话。⚠️ **已登记的临时例外（2026-09-23）**：一层还没有玩家坐标，[500 床一层跑团地图原型 §2.4](500%E5%BA%8A%E4%B8%80%E5%B1%82%E8%B7%91%E5%9B%A2%E5%9C%B0%E5%9B%BE%E5%8E%9F%E5%9E%8B.md) 的人物对话浮层暂不做距离判定，并在界面上明写这一条；玩家位置层建立后必须恢复距离判定。距离限制针对的是**右侧常驻对话栏**，本条不因此放开它。
9. **不欢迎的交互呈现：浏览器原生 SVG 焦点轮廓。** 可点击的 SVG 点、线、区域和图标必须在基础样式中关闭原生 `outline`，再以项目定义的固定屏幕尺寸描边、形状或位移提供鼠标选中与键盘焦点反馈。禁止让原生轮廓随非等比 SVG 坐标、元素包围盒或地图缩放变成巨大黑框、黑盘或椭圆；也不得只关闭轮廓而不补充可见的键盘焦点状态。

## 五、原型场景

原型默认使用[500 床一层跑团地图原型](500%E5%BA%8A%E4%B8%80%E5%B1%82%E8%B7%91%E5%9B%A2%E5%9C%B0%E5%9B%BE%E5%8E%9F%E5%9E%8B.md)推进开局，同时保留可切换的三维医院作为无正典效力的美术/技术实验视图：

- 页面首先显示独立的开始界面，只提供“开始新游戏”。当前原型没有存档读取能力，因此不伪造“继续游戏”；开始后进入开局人物五屏，确认人物后才显示医院。
- 左侧三维环境采用 AWS RoboMaker Hospital World 的两套建筑平面、护士站、电梯和病房隔帘；CC0 套装不再以整间房的原始坐标自动混入，而是由素材目录按 GLB 节点实例拆成可独立布置的家具与设备；
- 页面刷新与三维资源加载期间只显示三维画布底色和加载状态；旧二维护士站场景不再作为首屏或加载失败时的回退界面，加载失败应留在当前画布上明确报告错误；
- 玩家可打开“布置医院”，在当前楼层放置、选中、平移、旋转或删除单件素材；选中后可直接按住物体沿楼层平面拖动，也可使用变换轴、旋转按钮或方向键微调，并可在关闭吸附与 0.1/0.25/0.5/1 米网格间切换；布置器用轴对齐包围盒检查越界、角色当前站位、墙体冲突和家具互相重叠，冲突的变换不能落下；已放置物的包围盒同时注册到角色移动碰撞，角色不能穿过家具；合法布局按楼层保存到本地浏览器；
- 三层功能目标为：一层门诊与接待大厅，二层住院病区，三层手术与检查区；二、三层采用不同于一层的 `floor_02` 墙体和走廊平面，具体设备布局由布置结果决定；
- 等距视角的墙体剖切以角色可见性为准：只处理摄像机与角色之间实际命中的墙面，在遮挡点周围生成具有柔和边缘和时间过渡的局部透视窗，并将渐变连续传递到相邻墙段，不暴露网格切分边界；未遮挡角色的墙、窗框、楼梯和扶手保持不透明；
- 医院布置功能通过 `web/src/hospital/placement-editor.js` 的小接口接入页面；素材解析、碰撞算法和样式分属独立模块，不再继续堆入单个 HTML 实现；
- 每层的功能区、走廊与电梯厅位于连续坐标空间；玩家必须走到电梯厅或楼梯区域并使用世界交互上下楼，不设置房间或楼层界面按钮；
- 建筑布局是无正典效力的呈现资产，用于验证整层探索和叙事界面的关系，不反向定义正式医院平面、功能分区或故事事实；
- 右侧接入郑晓敏、谭丽娟、吴桐、周卫国与唐念安五份独立人物对话；双护士线沿用[开局剧情逻辑原型](%E5%BC%80%E5%B1%80%E5%89%A7%E6%83%85%E9%80%BB%E8%BE%91%E5%8E%9F%E5%9E%8B.md)的记录与昼夜触发，其余人物内容仅用于试玩仓库人物素材，不取得剧情正典地位；
- 点击不同回应产生不同的局部文本、线索提示与当前目标变化；
- “笔记”页演示承诺、线索、自问三层；
- 所有文案只用于验证阅读与交互，不进入故事正典。
- 开局人物按[疾病特长 §六·待办 8](../entities/%E7%96%BE%E7%97%85%E7%89%B9%E9%95%BF.md)的五屏结构，作为首次进入医院前必须完成的开局流程，而不是地图顶栏工具。玩家可在经历未选满时查看后续，但必须补满经历后才能最终确认；只有最终确认才把经历与疾病试玩档案交给对话运行时。每段经历的原始属性净值固定为 +2，并支持 +3/−1 取舍。属性页按 0–10 结算，每张属性卡顶部保留插画空位，下方显示当前程度对应的能力描述。疾病可选数量等未决项仍在页面中明确标为原型口径；镜子中整理疾病外观面具的设计已记录在[疾病特长 §4.5](../entities/%E7%96%BE%E7%97%85%E7%89%B9%E9%95%BF.md)，因低优先级且无既定美术/物理依赖，本原型暂不实现编辑器。
- 开发服务器中的开局人物页额外提供“开发默认人物 · 直接进入”夹具：预填 `长期上夜班、三班倒`、`家里有病人，你随时待命`、`父母长期不在`、`长期扮演一个“懂事”的角色`、`长期做核对类工作`、`有硬指标的工作`、`长期反复搬家、转学、换地方`、`从小在两套完全不同的环境里来回`、`重体力劳动`、`长期慢性疼痛`十段经历，并选择其已解锁的“广泛性焦虑障碍”。它只用于少点十次、快速进入医院，不是正典主角，也不改变正式选择规则；生产构建不显示该入口。
- 上午、下午、夜晚使用不同的呈现色调：上午偏清冷且较亮，下午偏暖，夜晚降低环境亮度并增加蓝绿色。滤镜只作用于视觉呈现，不改变图纸坐标、可达性、对话条件或时间状态；时段仍以[剧情系统设计 §3.8](../events/%E5%89%A7%E6%83%85%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1.md)的三时段为准。二维地图的时间色调层固定在地图视口上，静止 SVG 与缩放期间的 SVG 派生 PNG 缓存共同经过同一层；缓存本身不烘入色调或暗角，切换底图时也不得隐藏该层。
- 右栏新增“开发者选项”，集中收纳二维地图 / 3D 实验切换、地图缩放旋转复位、呈现变体、时段预览、重新查看开局人物、医院布置和显示设置。地图本体只保留坐标读数，并将其排成不遮挡图纸的单行状态条；“布置医院”入口也不得悬浮在场景画面上。
- “显示设置”提供 70%–140% 的本地亮度调节并记住选择；它只对浏览器呈现做后处理，不修改 Three/Unity 场景灯光、叙事状态或正典参数。

## 六、验证问题

原型评审只回答以下问题：

1. 玩家能否在阅读对话时仍持续感知自己身处医院空间？
2. 右侧长文本是否具有清晰的说话者、感知层与选择层级？
3. 选择后的反馈是否同时作用于叙事流和左侧场景？
4. 对话与笔记切换是否足够直接，且不让玩家误以为世界暂停或回滚？
5. 在桌面宽屏与手机窄屏上，当前选项是否始终可达？

本原型通过不代表玩家行为已准入，也不替代 Slice must-prove 或绑定 build 的试玩证据。

## 七、Unity 展示壳（先看页面）

2026-09-17 起，`ActionLab` 在 Play 时会自动挂载 UI Toolkit 展示壳：

- `Assets/Resources/NarrativeUI/NarrativeHud.uxml` 固定页面语义与层级；
- `NarrativeHud.uss` 实现宽屏左右分栏、窄屏上下布局和视觉状态；
- `Assets/Scripts/NarrativeUiPreview.cs` 只负责展示壳挂载、响应式 class 与“对话 / 笔记”切换。

这一阶段刻意不接 StoryEngine。UXML 内的地点、人物、台词、感知、线索和回应均为非正典预览内容；回应按钮不推进节点，也不写回 Q/R/F/W。后续接入真实逻辑时，应保留 UXML/USS 作为视图，把静态内容替换为 Unity 适配层提供的只读视图模型。

浏览器版不再依赖临时 Python 静态服务器。本地执行 `cd web && bun install && bun run dev` 后访问 `http://localhost:5174/`；Vite 直接托管本文件同目录下的 `叙事界面原型.html`，页面内容仍只有一个权威副本。端口与电车项目的 `5173` 分开，允许两个前端同时运行。

Unity Web 的长期集成采用“左侧 Unity 世界、右侧 HTML 叙事”的单一消息接缝；状态所有权、消息信封、生成物目录与迁移阶段见 [Unity Web 集成设计](Unity%20Web%E9%9B%86%E6%88%90%E8%AE%BE%E8%AE%A1.md)。

---
*创建: 2026-09-17 | 更新: 2026-09-23（§四·8 登记地图人物浮层的临时距离例外）*
*关联: [剧情系统设计](../events/剧情系统设计.md), [任务目标系统](../events/任务目标系统.md), [战斗界面布局](战斗界面布局.md), [当前可玩状态](../../PLAYABLE.md)*
