# Grilling #38 — 月光场重建 + 意识外显效应机制桥接

> **状态：激活（2026-09-02）**。Issue [#38](https://github.com/verystrongdog/game/issues/38) scope 更新为完整链路：**月光场重建（理论）→ 意识外显效应机制桥接（机制）**。
>
> 来源：用户主任务 = 寻找理论模型与游戏实现的接口；经 #99 复核确认 D5/D8 边界后开题。

## 链路上下文

```
理论层（已就位）
  #37 CGL+AGC 时空结构数学框架（内容骨架）  design/rules/时空结构数学框架.md
  #41 意识包裹（本体论地基，D5/D8 约束）   design/events/世界观与叙事.md §世界观地基
  #69 s(t) 三通道定义（m_field 第三通道）   决策树 :2298-2318
  #75 P2 情境系统（m_field 占位全 0）      决策树 :2553
接口层（本 grilling 主体）
  月光场 → SAN / 情境 / 检定 / 胜利路径
机制层（消费端）
  m_field 强度 + 节点级落点（functional_id）
  SAN 场桥接 / 情境基线偏移（核心机制 §6.3 v2 废弃）
  造物具象化/稳定性（敌人与事件 §八 旧月相表废弃）
  "相信被打败"胜利路径（核心机制 :313 待设计）
```

## 历史遗留

- v2 标量场（η/g_0/c(SAN)/κ/η_crit/ρ_off）已废弃（2026-08-11 #38 取代注，决策树 :1518/:1670）
- 本目录曾缺失导致链接断裂（决策树/六维状态引用）——现已重建

## 已裁决（2026-09-02，用户经 ChatGPT 双轮确认）

- **Q1 = a**：开发期完整动力学定形 → 运行时简化标量接口。CGL+AGC 在开发期跑完整数值仿真（验证动力学/识别 regime/锚定参数/提取低维响应）；运行时不复制完整场求解，消费理论投影 q(t)。
- **Q1a = a1**：CGL+AGC 保持确定性；游戏层随机性（ξ_game）由机制侧独立定义，不得宣称来自当前理论。概率/Fokker-Planck 扩展不纳入本轮。
- **c（查表）= a 的实现技术**（查表/插值/低阶 ODE/拟合/surrogate），非并列架构。
- **接口深度 = 完整闭环到 m_field 填值**（但不产出最终校准数值）：
  `适用域判据 → φ_moon 实例化 → q(t) 定义 → q→m_field 接口 → 参数锚定位置 → 校准实验设计 → m_field 非零可解释输入`
- **验收终点**：能回答「MFieldStrength 从 0 改成真实输入——从哪来、经过什么计算、单位/范围、哪些参数未校准」。

## 新增原则（待 Step 4 写入正典）

1. **定位重述**：CGL+AGC = 世界内容的一致性骨架（分布量族的 CGL+AGC 子类），月光场是第一个实例——接口对象是分布量族，非月光场专用。
2. **理论投影（theory projection）**：φ(x,t) →(Π)→ q(t)，游戏只消费低维观测 q——理论投影，非理论复制。
3. **防腐机制**：需求与 CGL+AGC 不兼容 → 检查实例是否真属适用域，不反向修改理论。
4. **实例化判据**：X ∈ C_{CGL+AGC} 判定（待 Q2 裁决）。

## Q2 已裁决 — 实例化判据（2026-09-02）

- **C1-C8 判据集采用**（本体类型/动力学可写性/序参量/非线性类/耦合结构/持久性/去噪不变性/固定拓扑），**全硬门槛**：C1∧…∧C8 全过才获实例资格。软性入族（C2+C4+C6 勉强）会重开 v2 漏洞。
- **C7 = 去噪不变性（机械化）**：关闭 ξ_game 后，CGL+AGC 要求的核心确定性性质仍可观测（月相驱动参数变化、能量壳钉扎、regime 行为、持久动力学结构不消失）。月光场 ✅。
- **C8 = 固定拓扑假设**：月光场实例必须声明固定图 G/K；K(t)≠K 导致连通性/持久通道数结构性改变 → 降级/出族（不悄悄套公式）。动态拓扑扩展=未来独立理论工作。
- **区分**：「不满足 C8 但可近似使用」≠「它属于 CGL+AGC」——前者可走近似/降阶接口，后者必须全判据通过。
- 月光场自检：C1-C6 ✅，C7 ✅（去噪不变性），C8 ✅（固定拓扑假设下）——**月光场当前定义下通过入族**。

## Q3 待裁决 — φ_moon 月光场实例化

## Q3.1 已裁决 = a — 中性强度场（2026-09-02）

> φ_moon 是共享基质中的中性复标量时空分布量，机制侧语义仅为场的动力学状态与强度。A/B/C 三重版本不属于 φ_moon 的机制状态空间，不进入 CGL+AGC 动力学，也不参与 m_field 的定义；A/B/C 是观察者对共享场的认知/呈现读出，由观察侧根据观察者条件选择。

- 机制链路：φ_moon(x,t) →(动力学)→ q(t) →(机制)→ m_field；q(t) 含场强/相对 E_c 位置/regime/月相确定性调制/壳上持久状态，不含 A/B/C 标签
- 观察侧并行线：(φ_moon, O_i) → V_i ∈ {A,B,C}——两条线不互相污染
- b 否决（相位携带版本 → 版本物理化 → 破坏"取版本在观察侧"）；c 否决（三场 = 多场耦合，§八 排除 + 为设定反向扩张理论）

## Q3.2 待裁决 — 场源与 regime（(A) 族 vs (B) 族；F(t) 是什么；月相的角色）

## Q3.2 已裁决 = a′（2026-09-02）

- **族 = B 族**：E_th > 0，d(E=0) > 0 零态稳定；F(t) 推过 E_th → AGC 自持 → 收敛 E_c
- **月相 = 确定性慢周期参数调制**（λ_moon(t) = f_lunar(phase(t))），不是能量源；「满月 d<0/弦月 d≈0/新月 d>0」降级为**待校准的预期 regime 映射**（d 符号由 E 相对 E_th/E_c 决定，月相只改变 regime 倾向）
- **F(t) = F_collective(t)**：拜月祭礼/群体精神事件/Boss 战胜利/SAN 归零/信念聚集 → E↑ → 跨 E_th → 壳自持。「信念是物理力」= 信念聚集 → F(t) → E → regime
- **个体心想事成**：F_individual(t) = η_i·F_collective(t)，0≤η_i≪1——同一动力学不同源强，不另造机制
- **保留问题（初始化）**：B 族 + F=0 + E<E_th → 无月光；"月光恒在"正典需基线态解释——候选：(1) 历史事件已踢入亚稳自持 (2) 月相周期变阈值 (3) 持续背景源 F_bg（谨慎：重新回答"月光基础能量从哪来"）

## Q3.2a 待裁决 — 基线态（初始化） + 月相调制对象（耦合二问）

## Q3.2a 已裁决 = A1 + B1（2026-09-02）

> 月光场采用 B 族动力学。其持续存在来自历史上的一次或多次阈值激活，并以能量壳为当前基线，不设置永久背景源。月相不直接作为能量源，而是周期性调制壳能量 E_c 与激活阈值 E_th：前者决定月相相关的场强基线，后者决定外部精神事件触发高强度状态的难易。具体调制函数及数值留待后续校准。

- **A1 基线态**：历史激活 → E>E_th → E≈E_c 壳上常态；F(t)=0 不人为拉回零态；不引入 F_bg；不恢复零号病人；"月光恒在"= 系统历史条件/初始条件（非永久能量源）；"永恒不变"需数值验证（F=0 时不会脱离壳态）
- **B1 月相调制**：E_c(λ)=f_c(λ;θ_c) 场强基线/壳高度 + E_th(λ)=f_th(λ;θ_th) 激活难度，两通道独立；E_moon(t)≈E_c(λ(t)) 主导月相周期
- **约束**：「新月低/满月高；满月 E_th↓」= 待验证结构假设；0.675/0.792/0.900 旧值只作校准目标/先验参考，不作理论输入（防 v2 式反向规定函数）

## Q3.3 待裁决 — φ_moon 空间构造（均匀背景 vs 月源模态 vs 观察者相关分布）

## Q3.3 已裁决 = (d) 均匀基线 + 局部源调制，晶格 = 医院空间图 G_H（2026-09-02）

- 场本体：φ_moon: V_H×t → ℂ；V_H = 医院房间/区域节点，E_H = 空间连通；S = G_H Laplacian；M = 节点质量矩阵
- 均匀 = 全局基线约束（月相调 E_c 全局），非 φ_i=φ_j 恒等；空间差异由局部源 + S 耦合产生
- F(x,t) = F_collective(t) + F_local(x,t)；局部性来自源的空间分布，非"月亮照射模态"（保月光非天体边界）
- **两个空间分离**：世界空间 G_H → φ_moon → q(x,t)；神经空间（Q5）：q(x,t) → m_field⁽ⁱ⁾(t)。69 脑区不塞进 S（防医院↔脑区未论证同构）
- 方案表：a❌（无局部增强）b❌（非天体冲突）c❌（观察者物理化，破坏 Q3.1）d✅

## Q3.4 待裁决 — M、S、F_collective、F_local 数学定义与耦合（含 G_H 覆盖范围）

## Q3.4 已裁决（2026-09-02）

- **M = I**（均匀惯性起步，防过度参数化；M_ii≠M_jj 留待实验证据）
- **S = L(G_H)** 组合 Laplacian，L_ij = −w_ij，w_ij ≥ 0 空间连接强度；**边权只作待校准参数，不附加世界观解释**（防数学自由度=叙事属性）
- **F(x,t) = F_collective(t)·1 + F_local(x,t)** 双通道叠加，均外部驱动
- **锁定接口规则**：E(φ) 全局能量、d(φ) 全局决定 → **局部增强 ≠ 局部 AGC**（局部源只改 |φ_x| 局部动力学，经 S 耦合全场；不存在独立 E_x/局部 regime）
- **G_H = (ii) 分层图**：G_0 物理层 / G_1 扭曲空间 / G_2 患者世界，各自图 + 允许层间连接；E_c(λ)/E_th(λ) 各层共享。不选 (i)（动态增删边撞 C8）、不选 (iii)（患者世界月光=机制复制）
- 骨架：φ_moon = (G_0, G_1, G_2, M=I, S=L, F_collective+F_local, E_c(λ), E_th(λ))
- 动态空间变化违反固定拓扑 → 降级/出族（Q3.3 裁决延伸）

## Q3.5 待裁决 — 三空间层场结构（全局场 vs 每层场）+ 医院主导观察者进入 q→m_field 的位置

## Q3.5 已裁决 = A(a) + B(i)（2026-09-02）

- **单全局场**：φ_moon: G→ℂ，G = G_0∪G_1∪G_2——三层 = 同一场的三个空间区域；单一全局 E(φ)、单一 d(φ)，无每层 regime；三层共享 AGC 状态（保家族边界）
- **C8 明确**：G(t) = G_0∪G_1∪G_2 = constant（运行期固定）；不动态改拓扑表达鬼打墙/门控——空间切换由上层空间系统改可达性语义
- **主导观察者不进主链**：O_hospital → 情境基线（情境系统 §6.2）；(φ_moon, O_i) → V_i ∈ {A,B,C}（观察侧）；不添加 O_hospital → m_field⁽ⁱ⁾（防三渠道职责重叠）
- 主链：λ(t) → E_c(λ)/E_th(λ) → 单一 φ_moon → 全局 E(φ) → 单一 d(φ) → q(x,t) → m_field⁽ⁱ⁾ → 个体机制调制
- **Q3 全链闭合**（本体/regime/初始化/月相/空间/源项/三层结构/观察者职责）

## Q4 待裁决 — q(x,t) 投影数学（Π 的形式/维度/归一化）

## Q4 已裁决 = a′（2026-09-02）

- **q_i(t) = ½|φ̇_i|² + ¼Σ_j w_ij|φ_i−φ_j|²**（正定局部能量密度，边能量均分两端；q_i ≥ 0；Σ_i q_i = E(φ)）
- **r(t) = E(φ)/E_c(λ(t))**：全局相对壳能量诊断量（壳上 r≈1），不是 m_field
- **q̂_i(t) = q_i(t)/q_i^base(λ(t))**：节点基线归一（q̂_i≈1 = 正常月光；局部祭礼 → q̂_i>1）；q_i^base(λ) 待校准
- 三层分离：φ → q_i（理论投影）→ q̂_i（接口归一化）→ m_field（游戏语义量）
- 否决：原 a 公式（局部势能可为负；q/E_c 节点不≈1）、b（|φ_i|² 接口弱）、c（多观测量复杂化）

## Q5 待裁决 — q̂ → m_field（个体化映射 + 神经节点落点 + 更新语义）

## Q5 已裁决 = A(a) + B(i)（2026-09-02）

- **m_field_vec⁽ⁱ⁾(t) = q̂_{x_i(t)}(t) · g(SAN_i, state_i) · R_i**
  - q̂ = 理论投影输出；g = 机制层调制（**方向不定死**：SAN↓→g↑/↓ 均不预设，待校准机制函数）；R_i ∈ ℝ^69 = 神经落点向量（只定接口存在，未填=0，functional_id 留独立 grilling）
- **单向接口**：m_field_vec ↛ φ_moon（神经空间/观察者/69 fid 不反向倒灌理论场）
- 否决 iii（单标量 MFieldStrength 无法表达 #69 节点级落点）
- 演进链：MFieldStrength=0 → q̂(x,t) → ×g(SAN,state) → ×R_i[69] → m_field_vec[69]
- **Q3→Q4→Q5 接口链闭合**（未反改 CGL+AGC 本体）

## Q6 待裁决 — 参数锚定位置（理论确定 vs 必须校准）+ 校准实验设计

## Q6 已裁决（2026-09-02）

- **Q6.1 术语修正**：「理论确定」→ **「结构锁定项 vs 待校准项」**（结构选择≠数学唯一推出）
  - 结构锁定：方程骨架 / M=I（基线选择）/ B 族 / 单全局场+联合医院图 / F=F_collective+F_local / Q4 正定局部能量投影 / r=E/E_c 诊断 / q̂=q/q^base 接口归一化形态 / m_field=q̂·g·R / A·B·C 观察侧
  - 待校准：f_c(λ;θ_c)、f_th(λ;θ_th)、w_ij、d_max、ε、F_collective、F_local、初始条件、q^base(λ)、g(SAN,state)
  - 「满月峰值」「满月易激活」= 待验证假设；0.675→0.900 = 先验/验收参考曲线，非理论输入
- **Q6.2 F_local = 事件触发局部注入 + 动力学自然恢复**：F_local 负责注入，S+d(φ) 负责演化；**不设 τ_local**；实现：F_local(x,t) = A_event(x)·W_event(t−t₀)，W_event 最小事件窗口，不加独立衰减时间常数
- **职责边界**：接口数学形态定死，不替数值校准轨预先拍参数

## Q7 待裁决 — 校准实验设计（sim 实验清单 + 归属）

*创建: 2026-09-02 | 更新: 2026-09-02*
