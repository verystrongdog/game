# Grilling #124 — ActionLab 动作填充尝试：载体锚定 + 执行形态：决策记录

> Issue: [#124](https://github.com/verystrongdog/game/issues/124) | 维度: 呈现 + 管线 | 形态: #123 推迟清单首项的启动（实施执行层）| 状态: ✅ 决策闭合，Batch0 实施待交付
>
> 前置: #123 动作库规格（词表 12 词条 / 契约 A / 批次 Q5 / 防漂移 §3.3-4）| 起因: 用户启动「尝试根据动作词表填充动作」；纠结 AI cli 直驱 Unity 的方便与复杂任务不可靠之间的矛盾

## 背景与沿革

- 用户上午 ChatGPT 会话总结（共享链接 `6a9f893d`）攻击审查：无可采纳的新机制/修改（唯一可留结论——站↔坐是契约 A 的首个试金石、可靠性分层——用户裁定「既然没有任何新机制、修改」→ 不以机制形式采纳，仅作验收判据参考）。
- 用户顾虑「现在就上成规模的 AI 操作会有问题」→ 化解框架 = **分域原则**（AI 直驱安全的判定条件：① 操作结果 AI 可用同通道读回自证；② 好坏判据非人眼判定）+ **粒度闸门**（一次 1 词条）+ **试点校准**（Jump 1 词条实测定 Batch1 形态）。
- 载体问题：用户已下载 Mixamo 资产（X Bot/Y Bot 角色 + 5 条动画）→ 逐文件二进制解析核验（无蒙皮纯动画 ×5、带蒙皮角色 ×2、Taunt.fbx = Beta 角色+嘲讽动画）。
- 决策树/gh 查重通过；Issue #124 创建（标签 维度:呈现, 维度:管线, needs-triage, grilling）。

## 决策表

| # | 决策 | 内容 | 位置 |
|---|------|------|------|
| Q0 | 执行形态 | **Batch0 代码先行（AI 零 Editor 操作）** + 事后 Jump 1 词条 AI 直驱试点（用户本机 Editor+pipeline 就绪后，实测定 Batch1 形态）；不选纯手动（Q2d 学习已完成）、不选先试点后产码（无谓等待） | 本记录 + issue #124 |
| Q-c | 上午总结采纳度 | 无可采纳新机制/修改；严格按 #123 规格原样推进 | 本记录 |
| Q-v | 载体锚定（**#123 闭合后修正**） | ActionLab 演示载体 = Mixamo **X Bot（默认）/ Y Bot（备用）**，替换 KI dummy；KiWalkerLab 与 KI dummy 保留（对照 + KI clip 数据源）。理由：词表 12 条中 7 条来自 Mixamo → 载体锚 Mixamo 侧把 retarget 面压到最小（仅 5 条 KI 通用动作 retarget，低风险），高风险接触动作（物攻/受击/倒下/坐立）全原生 | 动作库规格.md §一/§五 + #123 修正评论 |
| Q-v2 | Taunt 不入表 | Taunt（嘲讽）/Beta 角色不作载体、不入词表（扩展协议三条件不满足）；归档词表外素材 | 本记录 |
| Q-B1 | 机器源形态 | `ActionIds` = 12 词条常量（三面对一锚）+ `ActionCatalog` = 只读元数据表（id → {category, loop, priority, fade, clipFbxPath}）；单一机器真相；仍 C# 机器源（不越「JSON 化=升级触发器」边界） | Batch0 交付清单 |
| Q-B2 | controller 生成 | 含幂等 `ActionLabBuilder`（菜单「YANTF → 动作演示 → 创建 ActionLab」触发，用户操作）；状态名=id 由构造成立；缺口状态先建、clip 留空 + Console 警告清单 | Batch0 交付清单 |
| Q-B3 | ActionPlayer 作用域 | 含 locomotion 通道（Move(speed) → Idle/Walk/Run 阈值）+ Play(id) 动作请求（优先级覆盖，一次性播完回退）；WASD/Shift 位移走 CharacterController（applyRootMotion=false）；闭合目视清单 ① 与 X Bot retarget 验证 | Batch0 交付清单 |
| Q-B4 | L2 落点 | ActionIds/Catalog = 12 词条全量（level 标注 L1/L2）；**controller 仅建 L1 9 态**；Play(L2 id) → Debug.LogWarning（L2 登记未接线）；Sit/Stand/Talk 待消费端就绪走 L2→L1 迁移 | Batch0 交付清单 |
| Q-B5 | 防漂移断言分档 | 断言按 clipFbxPath 资产存在性分档：存在 → 「状态存在且有 clip」；不存在 → 「状态存在且 clip 为空（显式待填）」；随用户导入自动升级（Batch0 仅 KI 4 条时 4/9 有 clip 断言，导入后 9/9） | Batch0 交付清单 + 测试 |
| Q-m | 来源矩阵实证映射 | 5 条 Mixamo clip 全部无蒙皮下载体（已逐文件解析核验）：`Jab Cross`→PhysicalAttack（直拳，Play 验单次性）、`Charge`→MentalAttack（**伸手指人 = A 前指宣言**，文件名陷阱实证：Charge 语义非冲锋）、`Short Left Side Step`→Defend（循环格挡，文件名陷阱实证）、`Head Hit`→HitReaction（受击语义对，通用性 Play 后判）、`Dying`→Down | 动作库规格.md §二 + 手册 |

## 写入验证表

| 决策 | 写入文件 | 位置 | 验证 |
|------|---------|------|------|
| Q0/Q-c/Q-v/Q-B1..5/Q-m | 本决策记录（新建） | 全表 | ✅ 已写 |
| Q-v/Q-m | 呈现/动作库规格.md 🔧 修正注记 + §一载体 + §二来源行 | 顶部/§一/§二 | 待写 |
| Q0-Q5 决策树 | docs/决策树/ #124 条目 + #123 推迟项「→ 见 #124」 | 尾部 | 待写 |
| 六维状态 | docs/设计框架-六维状态.md 呈现行更新 + footer | 呈现 ✅ / footer | 待写 |
| 入口 | unity/README.md §二·E（手册）+ 项目总览.md 呈现行 | — | 待写 |
| memory | 动作集实施-grilling-124.md | memory 目录 | 待写 |
| issue | #124 关闭 + 总结评论；#123 🔧 闭合后修正评论 | GitHub | 待写 |
| Batch0 代码 | ActionIds/ActionCatalog/ActionPlayer/ActionLabDriver/ActionLabBuilder/ActionLabSmokeTests | unity/Assets | 待交付 |

## 推迟清单

- Jump 1 词条 AI 直驱试点——依赖用户本机 Editor + pipeline 就绪（当前未连接）
- Batch1 用户执行：X Bot/Y Bot 导入 + 5 clip 改名入 Combat/ + 菜单生成 + Play 目视验收（手册 §二·E）
- 物攻（Jab Cross 组合拳）单次直拳语义、HitReaction（Head Hit）通用性——Play 目视后判
- L2（Sit/Stand/Talk）接线——消费端就绪时走 L2→L1
- Charge/Side Step 文件名陷阱记录——已并入来源矩阵注释

*创建: 2026-09-08 | 更新: 2026-09-08*
*关联: [呈现/动作库规格.md](../../../%E5%91%88%E7%8E%B0/%E5%8A%A8%E4%BD%9C%E5%BA%93%E8%A7%84%E6%A0%BC.md), [决策树 #124](../../../docs/%E5%86%B3%E7%AD%96%E6%A0%91/README.md), [决策树 #123](../../../docs/%E5%86%B3%E7%AD%96%E6%A0%91/README.md), [unity/README.md](../../../unity/README.md)*
