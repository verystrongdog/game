# #101 [Grilling] R_i 神经落点（functional_id 映射）

> 状态：关闭 · 创建 2026-08-29 · 关闭 2026-08-30
> 标签：维度:规则, 维度:管线, needs-triage, grilling
> 原始：https://github.com/verystrongdog/game/issues/101

> **归档**：grilling 流程已停用，本文是只读历史记录。

---

## 正文

## 话题

填充 m_field 游戏接口的最后缺口：R_i ∈ ℝ^69 神经节点落点向量（#38 Q5 接口锁定，内容留独立 grilling）。任务：确定 69 个 functional_id 中哪些节点接收月光场通道输入、权重如何分配、与 #69 三通道（s_事件/s_env/m_field）的职责边界。

## 六维定位

- **维度**: 规则（m_field 接口消费端）+ 管线（数据字段/校验）
- **依赖**: Grilling #38 已闭合（§11.5 m_field_vec⁽ⁱ⁾ = q̂·g·R_i，单向接口 m_field_vec ↛ φ_moon）/ #69 三通道定义 / #90 记忆内容层（s_total 消费端）
- **阻塞**: m_field_vec[69] 完整实现（#70 P2 情境系统当前 m_field 占位全 0）；与 E1-E7 校准轨（#35）并行，不依赖 E1 数值结果

## 当前状态

- 接口形态已定（数学框架 §11.5）：R_i ∈ ℝ^69 只定接口存在，未填=0；三职责不可互换（q̂ 理论投影 / g 机制调制 / R_i 神经落点）
- 约束：69 fid ≠ 医院空间节点（Q3.3 两个空间分离——医院图 G_H 与神经空间不塞进同一 S）
- 待裁决：落点选择原则（哪些 fid 接收）、权重语义（二进制掩码 vs 连续权重）、与情境基线（核心机制 §6.2）的边界

## 来源

决策树 Grilling #38 推迟项（:2247）+ 规则/时空结构数学框架.md §11.5 + 决策树 :2298-2318（#69 s(t) 三通道）

---

## 评论（2 条）

### verystrongdog · 2026-08-30

## Step 0 边界锁定（2026-09-02，用户经 ChatGPT 分享页 ×2 确认）

**#101 研究对象**：R_i ∈ ℝ^69 的神经落点结构。

**#101 负责**：落点 functional_id 集合、权重结构及其数学约束（非负性/稀疏性/归一化/主辅落点/是否分功能簇/R_i 是否随空间节点 i 改变）。

**#101 不负责**：具体权重数值（w_k = ?）与最终效应大小 → #35 校准轨；不重新定义 m_field 接口；不修改 q̂/g 职责；不承担 E_c/E_th 数值校准。

**关键措辞边界（写死）**：『怎么加权』的形式/约束由 #101 定义；『权重数值』由 #35 决定。例：#101 定 R_i = Σ_k w_k·e_fid_k 的形式与约束，#35 定 w_k 的标定值。

**正典约束**：69 fid 来源 = data/brain_regions.json + 脑功能层级模型.md（不重新创造 fid 编号）；69 fid ≠ 医院空间节点（时空结构数学框架 §11.3）；单向接口 m_field_vec ↛ φ_moon。

**依据**：决策树 #38 :2247/:2249 + 时空结构数学框架 §11.5 + #69 三通道（D6/D7）+ #75 P2 占位全 0。

进入 Step 3 追问。

### verystrongdog · 2026-08-30

## 决策总结（Grilling #101 闭合，2026-09-02）

### 决策表

| # | 决策 | 内容 |
|---|------|------|
| Step 0 | 边界 | #101 定 R 结构/语义；数值（r、α_s）归 #35 |
| Q1 | 落点集合 | S_core = 12 节点 = L1∪L2 ∩ W活跃；落点 ⊆ W活跃 硬约束；S_ext 暂不纳入 |
| Q2 | 结构+扩展 | R = Σ_s α_s·v_s^norm；互斥分区 + 语义内归一化 + 独立缩放 + 扩展性不变量 + PLACEHOLDER 对齐 #103 |
| Q3 | 通道语义 | m_field 独立输入源；三通道输入层加法共存；禁交互项；「放大」降级系统响应层描述 |
| Q4 | 权重语义 | 主辅两档：v_s^norm(j) = r/(4r+8)[主] 或 1/(4r+8)[辅]；r 归 #35；r=1 仅 PLACEHOLDER |
| Q5 | 固定性 | R_i ≡ R ∀ i；职责正交化（空间→q̂ / 状态→g / 月相→q̂基线 / 落点→R） |
| Q6 | 符号稀疏 | R[j] ≥ 0（输入层性质）；当前硬稀疏 12；supp(R) ⊆ W_active 长期不变量 |
| Q7 | 情境独立 | 与情境系统无运行时协调；情境选择不读 m_field |
| Q8 | 撤回 | 消费端/数据形态/校验归管线，非设计决策 |

### 受影响文件

- 规则/时空结构数学框架.md（§11.5 R 结构 + 参数速查表）
- 规则/技能树系统/运行时状态模型.md（§4.5）
- 规则/核心机制.md（§6.3 措辞修正）
- docs/决策树.md（#101 条目 + 3 处旧延迟项加注）
- docs/设计框架-六维状态.md（规则 +1 → 38）
- 项目总览.md（5.1 行）
- data/term_registry.json（s_env 措辞修正 + R/神经落点 入库）
- memory（月光落点-R_i-grilling-101.md；memory 69 措辞同步）

### 推迟清单

- moonlight_landing.json + 引擎实现 + 结构校验（互斥/归一化/supp⊆W_active/非负/扩展性不变量回归）→ #70/#71 管线
- r、α_s 正式数值 → #35（PLACEHOLDER 消费分级）
- 幻觉调制/信念胜利路径等未来语义 → 走 Q2 互斥分区新增

### 质量门禁（v2 手动等效）

- 一致性清扫：活跃文档「放大所有情境」「待独立 grilling」残留清零 ✅；决策树历史原文合规保留 ✅；term_registry JSON 校验通过（262 术语）✅
- 写入验证表：11 项全部 ✅ 已验证；3 项 ⚠️ 需人类复查（#38 Q5 正文历史保留、#75 task-plan 占位、引擎侧待 #70 实施）

---
*导出: 2026-09-12 | 来源: GitHub issue*
