# 门边「推到底」能播段证据（#166 · `通过开度` · 逐帧持续判据 · 导出 E0–E6）

> [#166](https://github.com/verystrongdog/game/issues/166) 的证据。权威 =
> [动作描述口径](../../presentation/%E5%8A%A8%E4%BD%9C%E6%8F%8F%E8%BF%B0%E5%8F%A3%E5%BE%84.md) **§13.6**（完成定义 · 自由度表 · 终态 = `通过开度`）·
> **§十**（判据 V-f 与能播类三项新判据）· **§13.4.1**（`F4a` 判据时域）；
> 术语 `通过开度` 见 [注册表](../../../data/term_registry.json)；接触相位的算法与容差来源见
> [接触相位识别件](../../../code/tools/xbot_contact_phase.py) 与[它的证据](xbot-contact-phase-2026-09-15.md)。
> 必填字段按 [阶段证据说明](README.md) 与 [WORKFLOW.md §五/§六](../../../WORKFLOW.md) 记录。

## 〇、两轮记录（**先读这一行**）

⚠️ 本证据有**三轮**，读的时候别串：
**§一–§十三 = 第 1 轮**（门**朝角色这侧**开、角色后退推）——机械面**全绿**，而 owner 2026-09-16
目视**不通过**（四条指名）。**§十四 = 第 2 轮**（按四条指名改完）——**数值以 §十四 为准**。
**§十五 = 第 3 轮**（「Armature 里多余骨骼」的根因与判据）。
✅ **owner 2026-09-16 目视确认**：「**可以了，现在没问题了**」——**8 条验收标准逐条结账见 §十五.7**；
⚠️ 但第 8 条走了 **3 轮**返工（口径 §13.8 的目标是 ≤2 轮）⇒ `Health` 仍不判 `PASS`。
第 1 轮不是废纸：它是「**机械判据全绿、owner 目视不通过**」在本仓的**第二次现形**（第一次是 #163），
也是 §十 V-f 那条判据第一次有真实载体之后收到的第一份判决。

## 一、结论（先读这一段）——**第 1 轮**（已被 §十四 取代，保留为记录）

| 项 | 值 |
|---|---|
| 本轮结论 | **门边「推到底」从"静帧 + 只推 8°"变成一条 61 帧、逐帧解算、逐帧打键的能播段**：新宿主 `--task doorpush`（`code/tools/author_xbot_chair_grab.py`，**与既有四个宿主并列**），口径 §13.6 的自由度表 8 行**逐行落地**。改前的两处事实（`--task door` 只到 28° 静帧、无时序与接地读数、未跑导出）**逐条被改后读数取代** |
| 终态（`通过开度`） | **49.486°**（从半掩 20° 再推 **29.486°**）——推导 = `门宽·sinθ ≥ 胶囊直径 2r`：`sinθ ≥ 0.6234/0.82 = 0.760244`。**不是选定角度**（推导与来源见 §四） |
| 改族判定（口径 §13.5） | **未触发**：弧上扫描 20° → **88°** 全程"保持扣住"可解（三条接触对残差 **0.00 mm**、手臂不夹直、肩→腕 **0.465 → 0.468 m** 恒小于臂长 0.5617）⇒ `通过开度` 落在可达域**内部**，不改族（§五） |
| `F4a`（**持续**型，逐帧） | 帧 **13–61**（49 帧）三条接触对**逐帧全绿**：虎口↔近侧竖棱 **0.00** · 拇指↔近侧面 **2.33** · 四指↔远侧面 **−1.37 / +0.69 / −1.49 / +0.90** mm（容差 2.8）——整段最坏 **2.33 mm**，**松一帧即红**的形态**从未触发**（§七.2） |
| 时序（零阈值判据） | **声明到位帧 49 == 产物侧极值帧 49**（判据量 = 手骨绕铰链轴的水平方位角；**从产物逐帧算**，49 帧平台期取**首个**极大）。⚠️ 该曲线若"门在转、手不动"立刻变平 ⇒ 极值帧必然偏离声明帧（§七.3） |
| 接地与支撑相位 | 足底最低点 ↔ 地面沿用 `soleMargin = 2.8 mm`：**无支撑帧 0 个** · **换脚帧 22 / 27 / 32 / 37 / 42 / 47** · 支撑脚最低 **+0.01 mm**（0 处陷入）· 摆动脚窗口内最低 **+25.01 mm**（明确离地）。逐帧表由 [#165](https://github.com/verystrongdog/game/issues/165) 的件独立复算，**与宿主自读逐值一致**（§七.4 与 §九） |
| 穿模（能播类三项之一） | 判据 4（骨段 ↔ 门板盒）：躯干/上臂/大腿最坏 **+155.18 mm**（阈值 0）· 手/前臂/五指最坏 **−19.24 mm**（阈值 −20，⚠️ 只差 0.76 mm）· 非接触部位最坏 **+43.33 mm**（阈值 +10）⇒ **违规 0 处**（§七.5） |
| 导出侧 E0–E6 | **全绿**：骨集 **65** / `CTRL_` **0** 条 / 单根 `mixamorig:Hips` / **0** 网格 / 烘焙保真 **0.001 mm** / 回导保真 **0.0015 mm** / Rest Pose 端点最大差 **0.122 mm**（容差 0.5）（§八） |
| **owner 目视** | （第 1 轮当时）⏳ 未做——本件验收标准第 8 条明写"**不以机械全绿代替**这一条"。**8 张关键帧静帧**（`.scratch/doorpush/stills/`，过程物不入库）已产出供目视：帧 1 站立 / **13 扣住** / 19 开始推 / **22 迈步①** / **32 迈步②** / 42 迈步③ / **49 到位** / 61 保持 |
| 未闭合（§十二） | ① **owner 目视未做** ② `重心`/`支撑多边形` 的**判据部分**仍未建（本件只报读数）③ **改族边未验**（卡 4 不在场）④ 宿主 3（`--task door`）的两处病**本件不改**（分家，见 §六）⑤ `solve_gaze` 在转身 ≥ 45° 发散 ⇒ 新宿主改用扫描版 ⑥ 肘平面扫描有 7 帧"预热"漂移（**可复现**，不影响任何判据）⑦ **穿模判据最坏只差 0.76 mm 就红**（成因是掐棱握式本身：四指骨心必落在 40 mm 板体内）⑧ 「推到底」实为**被门推着退**（目视裁） |

## 二、绑定

| 字段 | 值 |
|---|---|
| base SHA | `3f47a6c`（#167 的收尾提交） |
| head SHA | 本提交（新宿主 + 库层扩展 + 证据 + 切片三处回填） |
| 阶段 | [#166](https://github.com/verystrongdog/game/issues/166)（`Task` · 门禁 `docs-integrity` + `author_xbot_chair_grab.py` + `export_xbot_action.py`） |
| 工具版本 | Blender **5.1.2**（`hash ec6e62d40fa9`）· Windows 11 宿主 · Python 3.10.12 · 由 WSL 侧经 `/mnt/c` 直调 `blender.exe`（先例 #164/#165） |
| 样本 | 母版副本 `.scratch/doorpush/push_clip.blend`（由 `.scratch/blender_assets/xbot/XBot_AnimationTemplate.blend` 逐字节拷贝而来，`5601197` 字节） |
| 产物（入库） | `code/tools/author_xbot_chair_grab.py`（宿主 5 + 库层"⑤ 门边推到底"只增块）· 本证据 · `design/slices/CP-01-ward-1f/slice.md` 三处回填 |
| 过程物（`.scratch/doorpush/`，不入库） | `push_report_{1,2}.json` · `push_report_leftfirst.json`（**另一个解**）· `scan_arc_report.json` · `scan_stance_report.json` · `export_report.json` · `contact_phase_report.json` + `contact_phase_sample.md` · `PushDoorEdgeFull.fbx`（**不入库**）· **8 张关键帧静帧**（`stills/`）+ 渲染脚本 `stills.py` · 六个侦察脚本（`probe1..6.py`） |
| 未改（本件一字不动） | `data/hand_grip_cards.json` · `code/tools/validate_grip_cards.py` · `code/tools/xbot_contact_phase.py` · `code/tools/export_xbot_action.py` · `code/tools/blender_action_compat.py` · `code/unity/**` · `data/action_set.json` · `design/rules/回合战斗流程.md` §10.13 · 宿主 3（`main_door()`）的全部代码与常量 |

## 三、命令与退出码

```bash
B="/mnt/c/Program Files/Blender Foundation/Blender 5.1/blender.exe"     # 先例 #164/#165
H='Z:\home\dog\game\code\tools\author_xbot_chair_grab.py'
T='Z:\home\dog\game\.scratch\doorpush\push_clip.blend'                  # 母版副本（宿主写回这里）
# ① 逐帧解算 + 逐帧打键 + 产物侧三类判据（跑两遍核可复现）
"$B" --background --factory-startup --python-exit-code 1 --python "$H" -- \
  --task doorpush --template "$T" --host headless \
  --report '…\push_report_1.json' --still '…\push_arrival_1.png'                    # 退出码 0（两遍同）
# ② 弧上扫描：保持扣住可达的最大角（改族判定）
#    … --task doorpush --no-save --scan-arc --scan-max 88 --scan-step 2              # 退出码 0
# ③ 身体摆放（净距）的解空间——口径 §4.2 硬规矩 5 的 ≥2 解并列
#    … --task doorpush --no-save --scan-stance                                       # 退出码 0
# ④ 另一个解：先迈另一只脚（自由度表第 2 行）
#    … --task doorpush --no-save --first-foot Right                                  # 退出码 0
# ⑤ 导出侧机械门禁 E0–E6
"$B" --background --factory-startup --python-exit-code 1 \
  --python 'Z:\home\dog\game\code\tools\export_xbot_action.py' -- \
  --template "$T" --action PushDoorEdgeFull \
  --output '…\PushDoorEdgeFull.fbx' --report '…\export_report.json' --force          # 退出码 0
# ⑥ 接触相位（#165 的件；读的是**产物**）
"$B" --background --factory-startup --python-exit-code 1 \
  --python 'Z:\home\dog\game\code\tools\xbot_contact_phase.py' -- \
  --blend "$T" --action PushDoorEdgeFull --name doorpush \
  --report '…\contact_phase_report.json' --markdown '…\contact_phase_sample.md'      # 退出码 0
# ⑦ 门禁（本仓自检四条）
python3 code/tools/validate_cross_refs.py            # 0 死链 / 0 段引用警告
python3 code/tools/validate_trash_isolation.py       # 0 违规
python3 code/tools/validate_params.py                # 0 failed
python3 code/tools/validate_issues.py --from-github  # 0 违规
python3 code/tools/run_all_checks.py                 # 16/16 退出码 0
```

| 门禁 | 结果 |
|---|---|
| `validate_cross_refs.py` | **0 死链 / 0 段引用警告** |
| `validate_trash_isolation.py` | **0 违规**（注册表三档 0 命中） |
| `validate_params.py` | **0 failed**（7 条既有 WARN 与本次无关） |
| `validate_issues.py --from-github` | **0 违规** |
| `run_all_checks.py` | **16/16 退出码 0**（本件不新增校验器） |
| `export_xbot_action.py`（E0–E6） | **全绿**（§八） |
| `dotnet test` | 未跑——本次**未改 `code/src/`** |

## 四、终态量 `通过开度`：推导与来源

`通过开度` 的定义在术语注册表里（"门开到**角色（胶囊）能通过**所需的最小开度——由**体型实测**推出，
**不是一个选定角度**"）。本件把它**算出来并当成终态**：

| 项 | 值 | 来源 |
|---|---|---|
| 胶囊半径 | **0.3117 m** | `code/unity/Assets/Scripts/ActionLabDriver.cs` `SizeControllerToModel()`（`_cc.radius = Clamp(h × 0.19, 0.25, 0.4)`）；**记录值 0.3117** 见 `ActionLabBuilder.cs` 与 `ChairSeat.cs` 的注释（**两处既有实测记录**，本件不新拍） |
| 胶囊直径 | **0.6234 m** | 同上 ×2 |
| 门宽 | **0.82 m** | `DOOR_WIDTH`（#164 起使用的门代理尺寸） |
| **净通道宽度** | `门宽 · sinθ` | 几何：门板从铰链出发沿 `u(θ)` 伸出，**远侧门框到该直线的垂距** = `W·sinθ`（θ=90° 时退化 = 整幅门宽） |
| **`通过开度`** | **49.486°** | `asin(0.6234 / 0.82) = asin(0.760244)` —— 与注册表里 owner 记的"粗算 ≈ 49°"一致；**从半掩 20° 再推 29.486°** |

⚠️ **如实登记一处数源对不上**（不影响上面的取值，但必须记）：Unity 侧把 `_cc.radius` 写成
`h × 0.19`，而 **0.3117 / 0.19 = 1.6405 m** —— 与 Blender 侧实测的 X Bot 蒙皮高度
（`Beta_Surface` z ∈ [0, 1.809]）**不一致**；若按 1.809 m 推，半径应为 0.3437 ⇒ `通过开度` = **56.9°**。
本件采用**注册表与两处代码注释共同记录的 0.3117**（= 49.486°），理由是它是**已在册的实测记录**、
且**更保守**（开度更小）。⇒ 登记为待核项（§十二）。

**终态的实现**：`DOOR_PUSH_*` 常量与 `door_pass_open_deg()` 在宿主里；门角在推段（帧 19 → 49）
线性从 20° 走到 49.486°，帧 49–61 保持。**脚本里没有任何"选定角度"常量**——
`DOOR_OPEN_END_DEG = 28.0`（宿主 3 的那个）在本件的路径上**不被引用**。

## 五、自由度表（口径 §13.6）逐行落地 + 解空间并列（§4.2 硬规矩 5）

| # | 自由量 | 归属 | 本件的解 | 依据 / 读数 |
|---|---|---|---|---|
| 1 | 终态门角 | **owner 圈定** | **49.486°** = `通过开度` | §四（几何推导） |
| 2 | 迈步：哪只脚先迈 | agent 解（owner 可改） | **左脚先迈** | **两解都解过**（下表）；采用左先迈的理由 = 穿模余量 |
| 3 | 迈步步长 | agent 解 | **0.129 / 0.150 / 0.340 m**（三段） | 由"身体必须跟着自由边扫弧"反解：弧长 = 0.82 × 29.486° = **0.422 m**，三段合计同量 |
| 4 | 转身角 | agent 解 | **= 握点方位角**（20.00° → 49.49°，逐帧） | 由可达性反解；**同时是口径 §6.2 的第二个动作复核 = U4**（§七.1） |
| 5 | 前倾量 | agent 解 | **净距 0.38 m**（身体到门板中面） | **≥2 解并列**见下：0.35 / 0.38 / 0.42 / 0.46 各自读数 |
| 6 | 支撑相位 | agent 解（首次建件） | **双支撑四段 + 单支撑三段**：双 `[1,21] [27,31] [37,41] [47,61]` · 仅左 `[22,26] [42,46]` · 仅右 `[32,36]` · **无支撑：未出现** | #165 的件从**产物**算出（§九），与宿主自读**逐值一致** |
| 7 | 重心（Hips 水平投影） | agent 解（**只报读数**） | 逐帧报出：Hips 水平投影 + **到最近支撑接触点的水平距离**（实测区间 **35.3 → 178.5 mm**） | ⚠️ **不建判据**（`支撑多边形` 的凸包/余量仍未建，见 §十二） |
| 8 | 时序帧表 | agent 解 | 起手 1 / 扣住 **13** / 开始推 **19** / 迈步 21·31·41 / **到位 49** / 保持 61 | 判据 = **V-f**：声明帧 == 产物极值帧（§七.3） |
| 9 | 抓握高度 · 手型 | ❌ 不是自由量 | 引用卡 1 侧握基准格（虎口 0.00 / 拇指 2.33 / 四指 ±1.5 mm） | 帧 13–61 **逐帧逐值相同**（§七.2） |

**解空间并列 ①「身体摆放（净距 b）」**（`--scan-stance`，`DOOR_PUSH_STANCE_CANDIDATES`）：

| θ | 净距 b (m) | 离近侧面 (m) | **胶囊余量** (m) | 肩→腕 (m) | 残差 (mm) | 肘 (°) |
|---|---|---|---|---|---|---|
| 20° | 0.33 | 0.310 | **−0.002**（穿门，不可行） | 0.426 | 0.00 | 98.7 |
| 20° | 0.35 | 0.330 | +0.018 | 0.441 | 0.00 | 103.6 |
| **20°** | **0.38（采用）** | 0.360 | **+0.048** | **0.465** | **0.00** | 111.8 |
| 20° | 0.42 | 0.400 | +0.088 | 0.498 | 0.00 | 124.8 |
| 20° | 0.46 | 0.440 | +0.128 | 0.531 | 0.00 | 142.3 |
| 49.486° | 0.33 / 0.35 / **0.38** / 0.42 / 0.46 | — | −0.002 / +0.018 / **+0.048** / +0.088 / +0.128 | 0.427 / 0.442 / **0.466** / 0.499 / 0.533 | 全 0.00 | 98.9 / 103.9 / **112.2** / 125.3 / 143.0 |

⇒ 可行域被两条硬约束夹住：**下界 = 胶囊半径**（b ≥ 0.35）、**上界 = 臂长 0.5617**（b ≤ 0.50 量级）。
采样点里**四个可行解**（0.35 / 0.38 / 0.42 / 0.46），读数差异 = 肩→腕 **0.441 → 0.531 m**、肘 **104° → 142°**。
采用 0.38 的理由：净距余量 48 mm、手臂余量 **97 mm**（0.5617 − 0.465），两侧都不贴边。

**解空间并列 ②「先迈哪只脚」**（`--first-foot`，两遍全跑）：

| 解 | 步长（三段） | 穿模·非接触部位最坏 | F4a 最坏 | 时序 | 腿残差 | 换脚帧 |
|---|---|---|---|---|---|---|
| **左脚先迈（采用）** | 0.129 / 0.150 / 0.340 m | **+43.33 mm** | 2.33 mm | 49 == 49 | 0.0 mm | 22·27·32·37·42·47 |
| 右脚先迈 | 0.063 / 0.253 / 0.290 m | **+15.43 mm**（贴阈值 10 mm） | 2.33 mm | 49 == 49 | 0.0 mm | 22·27·32·37·42·47 |

⇒ 两解**判据面逐值相同**，差别在余量：左先迈的**穿模余量 33 mm** vs 右先迈的 **5.4 mm** ⇒ 采用左先迈。

✅ **owner 2026-09-15 回话**：两个解**都照采用值定**——「**就这样（左脚先迈 + 身体净距 0.38 m）**」。
⇒ 第 2 / 第 5 两行由 `agent 解` 升为**已确认**（要改仍是一句话的事：`--first-foot Right` /
`--scan-stance` 里那 4 个候选净距）。

## 六、宿主 3 上的两处病（本件**不改**它，但把它"像是有病"的根因钉死）

owner 2026-09-15 对门边静帧的原话是「手的抓握效果是对的，但**整体开门的样子不太对、像是有病一样**」
（口径 §13.6 引）。本件在开工探路时把这句话**机械复现**了：

| # | 病 | 实测读数（本件新测） | 后果 |
|---|---|---|---|
| ① | 宿主 3 把"转身"写在 `CTRL_Hips` 的 **`z`** 上 | `CTRL_Hips z=+40°` ⇒ 脊柱 = (−0.641, 0.058, 0.765)、**双脚被甩到 (0.677, 0.024, 0.364)**；而 **`y=+40°`** ⇒ 脊柱 = (−0.044, 0.039, 0.998)、左向 = (0.766, 0.643, 0.008)（**这才是绕世界 +Z 的转身**） | 宿主 3 的"转身 37°"实际是**身体横着倒 37°**；同姿势下 `solve_gaze` 报 **目视水平 108.361°**（头也歪着）——这就是"像是有病" |
| ② | 宿主 3 的**打键表里没有腿脚控制骨** | 产物里 78 条曲线 / 26 个 `data_path`，**腿脚一条都没键**（#165 已实测：双侧全程无支撑、离地 +160 ~ +313 mm） | 骨盆一动脚就跟着骨盆飘；接地判据（M2）**无从谈起** |
| ③ | 由此暴露的第三条（本件新测） | **M2 的判据是单边的**（`≤ soleMargin`，**含陷入**）⇒ 一条**没进打键表**的腿会让脚**陷进地面 60 mm**，而 M2 **照样读成"接触"**（本件第一版就是如此：`换脚帧` 只报出 2 个，右脚全程"接触"） | 判据被绕过：**"接地"绿 ≠ 脚在地上**。⇒ 本件补 `|最低点| ≤ soleMargin` 的**对称用法**（同一常量，不是新阈值）后，那条腿当场报红 |

⚠️ **本件不改宿主 3**（口径 §13.6 的预期差分：`--task door` 的读数留在 #164 的证据里；新旧宿主分家）。
⇒ ① ② 两条**仍在宿主 3 里**，登记在此供后续件处置（§十二）。

## 七、逐帧读数（**全部从产物取**：打键后的 .blend，`frame_set` + `evaluated_get(depsgraph)`）

### 7.1 帧表与身体（抽帧；完整 61 帧在 `push_report_1.json` 的 `frames`）

| 帧 | 门开角° | 转身° | 骨盆 (x,y) m | 虎口 mm | 拇指 mm | 四指 mm | 残差 mm | 肘° | 肩→腕 m | 腿残差 mm | 目视° |
|---:|---:|---:|---|---:|---:|---|---:|---:|---:|---:|---:|
| 1（中立） | 20.000 | 0.00 | (−0.000, −0.015) | — | — | — | — | — | — | 0.00 | 5.91 |
| 7（起手中） | 20.000 | 10.00 | (+0.094, −0.007) | — | — | — | — | — | — | 0.00 | 2.90 |
| 13（**扣住**） | 20.000 | 20.00 | (+0.141, −0.003) | 0.00 | 2.33 | [−1.37, 0.69, −1.49, 0.90] | 0.00 | 116.0 | 0.476 | 0.00 | 0.29 |
| 19（开始推） | 20.000 | 20.00 | (+0.141, −0.003) | 0.00 | 2.33 | 同上 | 0.00 | 111.8 | 0.465 | 0.00 | 0.29 |
| 21 | 21.966 | 21.97 | (+0.118, +0.019) | 0.00 | 2.33 | 同上 | 0.00 | 111.8 | 0.465 | 0.00 | 0.31 |
| 27（第①步落） | 27.863 | 27.86 | (+0.047, +0.079) | 0.00 | 2.33 | 同上 | 0.00 | 111.9 | 0.465 | 0.00 | 0.39 |
| 34 | 34.743 | 34.74 | (−0.043, +0.140) | 0.00 | 2.33 | 同上 | 0.00 | 111.9 | 0.466 | 0.00 | 0.48 |
| 37（第②步落） | 37.691 | 37.69 | (−0.084, +0.162) | 0.00 | 2.33 | 同上 | 0.00 | 112.0 | 0.466 | 0.00 | 0.51 |
| 44 | 44.571 | 44.57 | (−0.182, +0.206) | 0.00 | 2.33 | 同上 | 0.00 | 112.1 | 0.466 | 0.00 | 0.56 |
| 47（第③步落） | 47.520 | 47.52 | (−0.227, +0.221) | 0.00 | 2.33 | 同上 | 0.00 | 112.1 | 0.466 | 0.00 | 0.57 |
| **49（到位）** | **49.486** | **49.49** | (−0.256, +0.230) | 0.00 | 2.33 | 同上 | 0.00 | 112.2 | 0.466 | 0.00 | 0.58 |
| 61（保持末） | 49.486 | 49.49 | (−0.256, +0.230) | 0.00 | 2.33 | 同上 | 0.00 | 112.2 | 0.466 | 0.00 | 0.58 |

**首帧是严格中立姿势**（零通道、腿残差 0、目视 = 静止的 5.91°）：这不是装饰——导出侧 E5 量的是
"导出那一刻的姿势有没有污染 Rest Pose"，首帧带姿势就会把**骨架身份**写歪（管线 §四·3 的硬约定）。

**U4 复核（口径 §6.2 世界轴映射的**第二个动作**复核）**：左腕 `x = +0.7133`（**左手侧 = +X** ✓）·
右腕 `x = −0.7133`（**右手侧 = −X** ✓）· 左趾指向与 **−Y 夹角 0.000°**（**前方 = −Y** ✓）·
头骨上轴与 **+Z 夹角 5.91°**（**上方 = +Z** ✓；5.91° 是头骨自身的静止后仰，`head_up_deg` 那个量
存在的理由就是它）。⚠️ 本节的口径限制 ①（"轴与前后左右的绑定只在**不转身**时成立"）在本件里
**逐帧适用**：转身 49.49° 的那些帧里，"前 / 左"是**角色体侧系**的词，道具量一律在**门局部系**里说。

### 7.2 `F4a`（**持续**型）：帧 13–61 共 **49 帧**、三条接触对逐帧

| 接触对 | 判据形态 | 49 帧内的读数 | 最坏 | 容差 |
|---|---|---|---|---|
| 虎口 ↔ 近侧竖棱（自由边） | 点↔线 | **49 帧全部 0.00 mm** | 0.00 | 2.8 |
| 拇指 ↔ 近侧面 | 点↔面 | **49 帧全部 2.33 mm** | 2.33 | 2.8 |
| 食/中/无名/小指 ↔ 远侧面 | 点↔面（有限面片/体） | **49 帧全部 −1.37 / +0.69 / −1.49 / +0.90 mm** | 1.49 | 2.8 |

⇒ 整段弧（29.486°）上**松一帧都没有**；且三条读数与卡 1 的**椅背基准格逐值相同**
（口径 §13.6："引用卡 1 侧握基准格"）——**V-f 要治的"门在转、手不动"在判据面上没有发生**。

### 7.3 时序（零阈值）：声明帧 vs 产物极值帧

- **判据量**：`mixamorig:LeftHand` 绕**铰链轴**的水平方位角（`atan2(Δy, Δx)`，铰链 = 门铰链）。
  选它的理由：门在转则它单调涨、**门在转手不动则它是平的** ⇒ 极值帧立刻偏离声明帧（**非退化**）。
- **取样**：口径 §十 的机制是"41 点均匀采样 + 整帧细化"；本件**逐帧全取**（61 帧），
  等价于该机制的极限情形，且**不留插值盲区**。
- 曲线末段（度）：帧 45 → **50.7098** · 46 → 51.6926 · 47 → 52.6755 · 48 → 53.6583 ·
  **49 → 54.6412** · 50–61 → 54.6412（平台）
- **极值帧 = 49（取首个极大） == 声明到位帧 49** ✅

### 7.4 接地与支撑相位（`soleMargin = 2.8 mm`，来源 = `FootGroundingIK.cs` 的 bind pose 实测）

| 帧区间 | 支撑集合 | 左足底最低 | 右足底最低 |
|---|---|---|---|
| 1–21 | 双支撑 | +0.01 mm | +0.01 mm |
| 22–26 | **仅左**（右脚摆动） | +0.01 | **+25.01** |
| 27–31 | 双支撑 | +0.01 | +0.01 |
| 32–36 | **仅右**（左脚摆动） | **+25.01** | +0.01 |
| 37–41 | 双支撑 | +0.01 | +0.01 |
| 42–46 | **仅左**（右脚摆动） | +0.01 | **+25.01** |
| 47–61 | 双支撑（保持） | +0.01 | +0.01 |

**无支撑帧：0 个**（全程至少一只脚在地上）· **换脚帧：22 / 27 / 32 / 37 / 42 / 47** ·
**支撑脚陷入：0 处**（最深 0.00 mm）· **摆动脚窗口内最低 +25.01 mm**（三段相同，明确离地）·
**腿 IK 跟踪残差最坏 0.00 mm**（踝都到了目标 ⇒ 落点是标称落点）。
⚠️ **层**：本表是**骨点层**（M2 的取点口径）；#165 的件同帧并列了**蒙皮层**（本件实测静息两层层差
**0.33 mm**），逐帧两种读数的完整并列见 §九。

### 7.5 穿模（能播类三项之一；动作库规格 §四·戊·5 判据 4）

| 类 | 阈值 | 61 帧最坏 | 违规 |
|---|---|---|---|
| 躯干 / 上臂 / 大腿（8 根） | 零穿透（≥ 0） | **+155.18 mm**（@帧 34 `RightUpLeg.tail`） | 0 |
| 手 / 前臂 / **五指 20 根**（允许接触） | 骨心进构件内部 ≤ 2 cm（≥ −20） | **−19.24 mm**（@帧 13 `LeftHandRing4.tail`） | 0 |
| 非接触部位（肩 / 小腿 / 足 / 趾 / 颈 / 头） | 最小间隙 ≥ 1 cm | **+43.33 mm**（@帧 49 `RightToeBase.tail`） | 0 |

判据量 = 骨段（head / mid / tail 三点采样）↔ **门板有限盒**的带符号间隙。
⚠️ **层差如实登记**（口径 §十 已写明）：判据量的是**骨段**，owner 看的是**看得见的蒙皮** ⇒
扩到蒙皮层要逐顶点求交，**本轮不做**（与 #165 的层差登记同族）。

⚠️⚠️ **本表最该被读到的一格**：`手/前臂/五指` 那一类**只差 0.76 mm 就红**——
最坏处是「**帧 13 `LeftHandRing4.tail`**」（无名指末节骨心落在门板盒内 **19.24 mm**，允许 20 mm）。
成因**不是姿势错**：掐棱握式就是"四指绕过远侧面、拇指压在近侧面"，**手指的骨心必然落在 40 mm
厚的板体内**（同一帧 Ring 的**蒙皮**读数 = **−1.49 mm**，正贴在外）。⇒ 这条判据的 2 cm 余量
**恰好是它能过的原因**；换更厚的板 / 更严的阈值就会立刻红（`F6 = [10, 66.2] mm` 那条改族阈值
量的是别的东西，不要混）。**登记为已知边界**，见 §十二。

## 八、导出侧机械门禁 E0–E6（`code/tools/export_xbot_action.py`）

| 门禁 | 读数 | 阈值 | 结果 |
|---|---|---|---|
| E0 源动作非退化 | 最大位移 **797.705 mm**（`mixamorig:LeftHand`；Hips 360.07 · 双脚 261.0 / 277.1） | ≥ 20 mm | ✅ |
| E1 烘焙保真 | **0.001 mm**（最坏 `RightHand.head@f1`）· 烘焙后 674 条曲线 · 剥掉控制骨曲线 **42** 条 · 解约束 **14** 个 | ≤ 0.5 mm | ✅ |
| E2 骨集 | 骨 **65** · 变形骨 **65** · **`CTRL_` 外泄 0** · 多余骨 0 | 65 / 0 | ✅ |
| E3 根骨 | `['mixamorig:Hips']` **单根** | 单一根骨 | ✅ |
| E4 animation-only | 网格 **0** | 0 | ✅ |
| E5 Rest Pose | 端点最大差 **0.122 mm**（矩阵元素 1.22e−4） | ≤ 0.5 mm | ✅ |
| E6 回导保真 | **0.0015 mm**（最坏 `LeftHand.head@f1`）· 回导动作 `Armature\|PushDoorEdgeFull` 659 条曲线 · 帧 1–61 | ≤ 0.5 mm | ✅ |
| 产物 | `PushDoorEdgeFull.fbx` **672284 字节** —— ⚠️ **不入库**（口径 §13.6 完成定义只要求"导出侧门禁全绿"；与 #164 同形态：生成物将来要用再单开） | — | — |

⇒ 口径 §13.6 的完成定义后半句（"防的正是**能看却不能导**那四类假完成"：控制骨泄漏 / 多一层
Leaf Bone / Rest Pose 漂移 / Root 结构分叉）**四类逐条被机械排掉**。

## 九、接触相位（#165 的件给出的表；**定义与容差都不是本件定的**）

`xbot_contact_phase.py --blend … --action PushDoorEdgeFull --name doorpush`：

| 项 | 读数 |
|---|---|
| 左脚支撑区间 | `[1,21]` `[27,41]` `[47,61]`（接触帧 **51/61** · 最低 **+0.01 mm** @帧 5） |
| 右脚支撑区间 | `[1,31]` `[37,61]`（接触帧 **56/61** · 最低 **+0.01 mm** @帧 1） |
| 双支撑 | `[1,21]` `[27,31]` `[37,41]` `[47,61]` |
| 无支撑 | **未出现** |
| 换脚帧 | **22 · 27 · 32 · 37 · 42 · 47** |
| 蒙皮层对照 | 静息两层层差 **0.33 mm**（骨点 +0.01 / 蒙皮 −0.32）——与 #165 的"静息时两层只差 0.33 mm"**逐值一致** |
| 读数指纹 | `sha256 = 6bff7718a9e78603721f17479de8591a2ac4175f548543bb7ca40825aad19036` |

⇒ **宿主自读（§7.4）与独立件逐值一致**（换脚帧、区间、最低点全同）——两处**各算一份**却给出同一个答案，
这是"这个量已经被 ≥3 处要求"（#165 的建件理由）之后**第一次有真实消费方**。

## 十、问题差分（`new_finding = after − mapped(before) − expected_delta`）

| 项 | 结果 |
|---|---|
| 允许变化（issue 的预期差分） | `code/tools/author_xbot_chair_grab.py` ✅ · `design/engineering/evidence/door-push-2026-09-15.md` ✅（本文件）· `design/slices/CP-01-ward-1f/slice.md` ✅ |
| 实际 diff | **四个文件**：上面三个 + `design/engineering/evidence/README.md`（**证据索引**多一行）。⚠️ **如实登记第 4 个文件**：README 的「已有证据」表是**证据目录的索引**，#165 / #167 落地时都登记了自己那一行（先例）；不登记就让新证据从索引里消失 ⇒ 按先例补一行，**理由与越界事实都写在这里**，不静默 |
| 原判 | ~~**只改这三个文件**~~（`git status` 见 §十一）；宿主 3 的代码**逐字节未动**（`git diff` 里没有任何一行落在 `main_door()` 内） |
| 预期不变 | `data/hand_grip_cards.json` ✅ · `validate_grip_cards.py` ✅ · `code/unity/**` ✅ · `data/action_set.json` ✅ · `回合战斗流程.md` §10.13 ✅ · `xbot_contact_phase.py` ✅ · `blender_action_compat.py` ✅ |
| `new_finding` | **空**（未发现任何"改前稳定失败、改后仍在"的既有问题；三处新登记都是**本件新测的读数**，不是既有缺陷） |

## 十一、回滚演练（预测 → 实测）

issue 的「回滚」节预测：`git revert` 本条提交组 ⇒ 宿主表回到既有四个（`chair`/`card1`/`door`/`restate`）·
`run_all_checks` 仍 15/15 · 门边静帧能力与 #164 的读数不受影响。

**实测**：在临时 worktree 里对**本条提交组**做 `git revert`，再跑全部适用门禁与 `--task` 宿主表核对。
读数与结论见 §十三（回滚演练实做记录）。

## 十二、未闭合（诚实清单）

| # | 未闭合 | 类型 | 影响 / 去处 |
|---|---|---|---|
| 1 | **owner 目视未做** | 需 owner | 本件验收标准第 8 条："**不以机械全绿代替**"。**8 张关键帧静帧**（`stills/`：帧 1 / 13 / 19 / 22 / 32 / 42 / 49 / 61）+ 一条 61 帧动作在 `.scratch/doorpush/` |
| 2 | `重心` / `支撑多边形` 的**判据部分**仍未建 | 待建 | 本件只报读数（Hips 水平投影 + 到最近支撑接触点的距离，35.3 → 178.5 mm），**不建凸包与余量判据**——那是 #167 登记的后续件 |
| 3 | **改族边（卡 1 → 卡 4）仍未验** | 待建 | 卡 4 不在场（口径 §13.9）。本件只测出"**没有触发改族**" |
| 4 | 宿主 3 的两处病（转身写在 `z`、腿脚没打键）**仍在** | 待处置 | 本件按预期差分**不改它**；但它是 owner 那句"像是有病"的根因 ⇒ 建议另开一件（或在本件收尾后处置） |
| 5 | `solve_gaze`（牛顿法）**转身 ≥ 45° 发散** | 已绕开 | 实测 45° → after **45.431°**（8 次迭代）；新宿主改用**扫描版** `solve_gaze_scan`。⚠️ 旧函数仍在库里、宿主 3 仍在用（它转身写错轴，所以没暴露） |
| 6 | 肘平面扫描的**7 帧预热漂移** | 已登记 | 帧 13–19 输入**完全相同**，肘角却 116.0 → 111.8（每帧 −0.7°）后稳定；两遍运行**逐值相同**（可复现，非随机）。**不影响任何判据**（三条接触对逐帧 0.00/2.33/±1.5） |
| 7 | **数源对不上**：胶囊半径 0.3117 vs 蒙皮高度 1.809 | 待核 | 见 §四 的登记（按 1.809 推 ⇒ `通过开度` 56.9°） |
| 8 | 「推到底」实际上是**被门推着退** | 如实登记 | 门朝角色这侧开 ⇒ 自由边扫过来，角色**后退三步**。这不是"推开门走过去"。机械判据全部只认"门到 `通过开度` + 手在整段弧上扣住" ⇒ 判据面成立；**观感上是否像"推"由 owner 目视裁**（目视材料见 §一末行：8 张关键帧静帧；这一条请在目视时点名回答） |
| 9 | 摆动腿抬脚高度 0.05 m、三段窗口的帧号 | 运动学取值 | 不是判据阈值（判据只有"离地 > soleMargin"与"到位帧 == 极值帧"），但它们是本件**选的**运动参数，owner 可改 |
| 10 | **穿模判据最坏只差 0.76 mm 就红** | 已知边界 | 最坏处 = **帧 13 `LeftHandRing4.tail`**（骨心进板体 **19.24 mm** / 允许 20 mm）。成因是**握式本身**（掐棱 = 四指绕过远侧面）而非姿势错：同帧该指**蒙皮**读数 = **−1.49 mm**。⚠️ 这条判据量的是**骨段**、且它的 2 cm 余量**恰好**是掐棱握式能过的原因 ⇒ 换更厚的板或更严的阈值会立刻红。**登记，不改阈值**（阈值来源 = 动作库规格 §四·戊·5 判据 4，不是本件拍的） |

## 十三、回滚演练实做记录

形态与 [#167](https://github.com/verystrongdog/game/issues/167) 的先例一致：**临时 worktree + 逐条
`git revert` + 全门禁重跑**（不做 `reset --hard`）。

```bash
git worktree add --detach .scratch/rollback-drill-166 HEAD
cd .scratch/rollback-drill-166
git revert --no-edit 5b632ca      # 证据 + 切片回填
git revert --no-edit e9145f5      # 新宿主 + 库层扩展
```

| 核对项 | issue 的预测 | **实测** |
|---|---|---|
| `--task` 宿主表 | 回到既有四个（`chair`/`card1`/`door`/`restate`） | ✅ 逐字核对：`choices: "chair", "card1", "door", "restate"`；`doorpush` 全文件命中 **0** 处；`door_pass_open_deg` 命中 **0** 处 |
| `run_all_checks.py` | 仍 15/15 | ✅ **16/16 退出码 0**（⚠️ 预测里的 15 是 #165 落地**之前**的计数；基线现在是 16——如实更正） |
| 三个设计校验器 | 0 失败 | ✅ `validate_cross_refs` **1981 refs / 0 死链 / 0 段引用警告** · `validate_trash_isolation` **✅ 全部通过** · `validate_params` **49 passed / 0 failed**（7 条既有 WARN） |
| 宿主 3 与 #164 的读数 | 不受影响（新旧宿主分家，旧宿主一字不改） | ✅ **逐字节核**：回退后 `code/tools/author_xbot_chair_grab.py` 的 `sha256 = 7712bd0f…c6cb01` **==** base `3f47a6c` 的同名文件；`design/slices/CP-01-ward-1f/slice.md` 的 `sha256 = 32fa137a…05f81c` **==** base 同名文件 |
| 资产与数据 | 无资产入库、无数据迁移、无运行时消费方 | ✅ 本件**未产生任何入库资产**（FBX 落在 `.scratch/`）；`data/` 与 `code/unity/` 零改动 |

⇒ **回滚面 = 三个文件**（1 个代码 + 2 个文档），且回退后**上一稳定状态逐字节恢复**。

---

## 十四、第 2 轮：owner 2026-09-16 目视**不通过**（四条指名）→ 逐条改完

> owner 原话（逐字）：「你这个动作**只能算得上拉门**，而且 **Armature 里面重复骨骼的问题又犯了**，
> **右手机械的平举**，**门和椅子重叠在了一起**，总的来说**不像是人的动作更像是机器人**，
> 但是值得表扬的是这个人物模型**确实完成了和门交互的动作**，我原本以为会更糟」。
>
> ⚠️ 这一轮**再次**是「机械判据全绿、owner 目视不通过」：第 1 轮的三类判据（`F4a` / 时序 / 接地）
> 与导出 E0–E6 **一项都没红**，而四条指名**一条都不是判据面能报出来的**——
> 这正是口径 §十 与 §4.3 要治的病，也是「**判据全绿不等于能看**」的第二次实测。

### 14.1 四条指名 → 逐条改法 → 改后读数

| # | owner 的话 | 接成什么（度量对象） | 根因（实测） | 改法 | 改后读数 |
|---|---|---|---|---|---|
| ① | 「只能算得上**拉门**」 | 门**朝哪一侧半掩**（`DOOR_PUSH_START_DEG` 的符号） | 半掩在**角色这侧**（+20°）⇒ 自由边是**朝角色扫过来**的，角色只能一边扣住一边**后退** ⇒ 观感是拉 | 半掩改到**远侧**（**−20°**），终态 **−49.486°**；身体从"后退"改成**向前迈步推、最后穿过门洞** | 骨盆 (0.401,−0.563) → (0.322,−1.017)；行程 **1.144 m**；**6 步**；门开角 −20° → −49.486° |
| ② | 「**Armature 里面重复骨骼**的问题又犯了」 | 骨架的**骨名集合** | **实测没有重复骨**：`Armature` = **78 骨 = 13 `CTRL_` + 65 `mixamorig:`**，无同名、无 `.001` 后缀（母版与产物**各查一遍**）⇒ 真正的观感来源是 **(a) `Beta_Joints`（骨骼可视化网格）与皮肤网格叠着画**、**(b) 13 根控制骨与它驱动的变形骨完全重合地画在一起** | 预览场景里**隐藏** `Beta_Joints` 与 13 根 `CTRL_` 骨（**只动显示**，姿势与导出都不受影响——导出仍 **E0–E6 全绿**） | 隐藏网格 **1** 个 · 隐藏控制骨 **13** 根 |
| ③ | 「**右手机械的平举**」 | 副手（右手）**有没有姿势** | 静止姿势是 **T-pose**；本件第 1 轮**只给左手解了姿势、右手一个字没动** ⇒ 它就那么**水平伸着** | 新增**副手解**：腕落到体侧（指尖朝下、掌心内向）、按 0.35 松握，起手段随 `t` 从 T-pose 过渡 | 肘 **159.0°**（自然微屈）· 腕 z **0.830 m**（低于肩 **0.55 m**）· 体侧 **0.15 m**· 随推段微摆到 **174°** |
| ④ | 「**门和椅子重叠**在了一起」 | 场景里的**残留代理** | 场景里还留着 **#163 的椅子代理**（`REF_Back` / `REF_Seat` / `REF_Leg0..3`）——**证据里早有登记**"母版残留 `REF_*`"；而门板绕铰链扫过的扇区**正好覆盖**它的位置 ⇒ 预览里门穿椅子 | 预处理里**删掉** `REF_Chair` / `REF_Board` 两个集合（它们**不进 FBX、不参与任何判据**——判据目标一律由 `door_layout_*` 现算） | 删掉 **6 个对象 / 2 个集合** |
| ⑤ | 「不像是人的动作**更像是机器人**」 | 全身（**没有单一量**） | 第 1 轮：右手 T-pose + 脊柱**笔直**地沿弧平移 + 步子齐整 | ① 副手有解（见 ③）② 真的**走路**：6 步、左右交替、双支撑换脚 **12 处**、摆动脚离地 17.1–50.0 mm ③ 加**躯干前倾**（`Spine`/`Spine1`/`Spine2` 各 4°，**轴向有来源**：管线 §2.1.4 实测局部 X = 前倾）④ 首帧仍**严格中立** | 前倾 0 → **12.0°**（帧 21 → 51）· 肩前移 **27.8 mm** · 目视水平最坏 **0.58°** |

### 14.2 第 2 轮的三类判据 + 导出（**全部重跑**）

| 判据 | 第 2 轮读数 | 状态 |
|---|---|---|
| `F4a` 持续（帧 16–61，46 帧逐帧） | 虎口 **0.00** · 拇指 **2.33** · 四指 **−1.37 / +0.69 / −1.49 / +0.90** mm 逐帧不变；最坏 **2.33 / 2.8 mm** | ✅ |
| 时序（零阈值） | **声明到位帧 51 == 产物极值帧 51**（44.3302°）——⚠️ 判据量改成**方位角幅值**：门朝远侧开后方位角是**负**的（−14.8° → −49.5°），取有符号值时极值帧会落到起手帧 16（本件实测到过、当场报红） | ✅ |
| 接地 / 支撑相位 | **无支撑帧 0** · 换脚帧 **3·4·7·9·11·13·15·23·28·36·40·48**（12 处）· 支撑脚最低 **+0.01 mm**（0 处陷入）· 摆动脚窗口内最低 **+17.11 mm** · 腿 IK 跟踪最坏 **0.00 mm** | ✅ |
| 支撑相位（#165 的件独立复算） | 左 `[1,2] [4,10] [13,27] [36,61]` · 右 `[1,6] [9,14] [23,39] [48,61]` · 双支撑 **7 段** · 无支撑**未出现** · 指纹 `sha256 = 508bac38…c63f` | ✅ 与宿主自读**逐值一致** |
| 穿模（判据 4） | 躯干/上臂/大腿 **+160.86 mm** @帧 21 · 手/前臂/五指 **−19.24 mm** @帧 16 · 非接触部位 **+116.61 mm** ⇒ **违规 0 处** | ✅ |
| 导出 E0–E6 | 源位移 **1574.549 mm**（阈值 20）· 烘焙 **0.0011 mm** · 回导 **0.0018 mm** · 骨 **65** · `CTRL_` **0** · 单根 `mixamorig:Hips` · 网格 **0** | ✅（**注意**：控制骨被隐藏之后导出**仍然全绿**——隐藏是显示开关，不影响 bake/导出） |
| 迈步（自由度表第 3 行） | 步数 **不写死**：`N = ceil(1.144 / 0.20) = 6`，窗口按**身体路径弧长**均分（每步 0.1906 m），摆动占每桶 70%；实测步长 0.237 / 0.451 / 0.426 / 0.328 / 0.388 / 0.361 m | ✅ |

### 14.3 第 2 轮的解空间并列（口径 §4.2 硬规矩 5）

| 解 | 读数 |
|---|---|
| **先迈左脚（采用）** | 穿模·非接触部位最坏 **+116.61 mm** · 步长 0.237/0.451/0.426/0.328/0.388/0.361 · 其余判据与另一解逐值相同 |
| 先迈右脚 | 最坏 **+116.61 mm** · 步长 0.229/0.474/0.402/0.352/0.341/0.426 · 同样全绿 |
| 身体净距 b（−49.486° 处） | 0.33（胶囊余量 **−0.002** 不可行）· **0.35（+0.018）** · **0.38（+0.048，采用）** · **0.42（+0.088）** · **0.46（+0.128）**；肩→腕 0.427 → 0.533 m · 肘 99.0° → 143.1° |
| 弧上扫描（保持扣住可达的最大角） | **−20° → −88° 全程可解**（残差 0.00 · 肩→腕 0.465 → 0.469 m）⇒ **改族未触发** |

### 14.4 第 2 轮仍未闭合

| # | 项 | 说明 |
|---|---|---|
| 1 | **owner 目视（第 2 轮）** | ⏳ **未做**。静帧 9 张（`.scratch/doorpush/stills/`：帧 1/9/16/21/28/36/44/51/61）+ `push_clip.blend`（动作 `PushDoorEdgeFull`）已就绪 |
| 2 | 「**更像人**」这一条**没有机械判据** | 第 2 轮只做了四件能指名的：副手有解 · 真走路 · 躯干前倾 · 清残留件。**次要运动**（手臂随步摆动、脊柱的逐段相位差、起推的发力加速/到位减速）**都没有做** —— 它们**没有可引用的来源**，也没有判据能判，只能由 owner 点名后再做 |
| 3 | 门**穿过角色原先站的位置**这一几何事实 | 远侧半掩后，自由边不再扫过角色（第 1 轮它扫过、实测 θ≈52.6° 时**正落在原点**）——第 2 轮这条**自动消失**，登记为已消解 |

### 14.5 第 2 轮的逐帧表（抽帧；全 61 帧在 `push_report_r2f.json` 的 `frames`）

| 帧 | 门开角° | 转身° | 骨盆 (x,y) m | 前倾° | 虎口 mm | 拇指 mm | 四指 mm | 残差 mm | 抓握肘° | 副手肘° | 腿残差 mm | 目视° |
|---:|---:|---:|---|---:|---:|---:|---|---:|---:|---:|---:|---:|
| 1（中立） | −20.000 | 0.00 | (−0.000, −0.015) | 0.0 | — | — | — | — | — | — | 0.00 | 5.91 |
| 9（走近） | −20.000 | −10.67 | (+0.214, −0.308) | 0.0 | — | — | — | — | — | 88.9 | 0.00 | 2.70 |
| **16（扣住）** | −20.000 | −20.00 | (+0.401, −0.563) | 0.0 | 0.00 | 2.33 | [−1.37, 0.69, −1.49, 0.90] | 0.00 | 111.8 | 159.0 | 0.00 | 0.29 |
| 21（开始推） | −20.000 | −20.00 | (+0.401, −0.563) | 0.0 | 0.00 | 2.33 | 同上 | 0.00 | 111.8 | 159.0 | 0.00 | 0.29 |
| 28 | −26.880 | −26.88 | (+0.403, −0.672) | 2.8 | 0.00 | 2.33 | 同上 | 0.00 | 109.6 | 161.9 | 0.00 | 0.36 |
| 36 | −34.743 | −34.74 | (+0.390, −0.795) | 6.0 | 0.00 | 2.33 | 同上 | 0.00 | 107.1 | 165.4 | 0.00 | 0.48 |
| 44 | −42.606 | −42.61 | (+0.361, −0.915) | 9.2 | 0.00 | 2.33 | 同上 | 0.00 | 104.6 | 169.4 | 0.00 | 0.51 |
| **51（到位）** | **−49.486** | −49.49 | (+0.322, −1.017) | **12.0** | 0.00 | 2.33 | 同上 | 0.00 | 102.5 | 174.0 | 0.00 | 0.58 |
| 61（保持末） | −49.486 | −49.49 | (+0.322, −1.017) | 12.0 | 0.00 | 2.33 | 同上 | 0.00 | 102.5 | 174.0 | 0.00 | 0.58 |

**打键通道 58 个**（第 1 轮 35 → 58）：骨盆 + **双侧**手臂/腿脚 12 根 + 颈/头 + **三段脊柱** +
**双侧**手指 40 根。⚠️ 第 1 轮漏腿脚、这一轮**差点再漏一次脊柱**——两处都是「解了姿势但没进打键表」，
产物里就会退回静止姿势（腿脚那次实测成"脚陷地 60 mm 而 M2 读成接触"）。

---

## 十五、第 3 轮：「Armature 里多余骨骼」到底在哪（owner 第 **三** 次指出）

> owner 原话：「**Armature 中有多余骨骼的问题一直在重复的犯，包括这一次，那些多余骨骼十分显眼的
> 飘在人体模型外面**，更让我生气的是你竟然声明没有问题，这个问题之前还犯过！所以问题到底出在哪里？」

### 15.1 先认两个错（都不是"没看见"，是**判据用错**与**重犯已登记的错**）

| # | 我的错 | 事实 |
|---|---|---|
| ① | 上一轮我用「**骨名有没有重复**」回答「**骨骼看着多余**」 | 骨名确实不重复（78 = 13 + 65，无 `.001`）——但**这是另一个问题**。真正的量是「**这根骨有没有蒙皮、骨尖在不在皮肤里面**」：按这个量一查，**13 根骨的骨尖伸到皮肤外**（详见 15.3） |
| ② | 我把 `Beta_Joints` **隐藏**掉当作修复 | [#164 的证据 §十二](hand-grip-cards-2026-09-15.md) 早就写明：「`Beta_Joints` 是**人体网格**（10514 顶点）不是骨骼显示件——**隐藏它会把腰部蒙皮一起去掉**（实测踩到一次）」。**我重犯了这个已经登记的错**，本轮**已回改**（`Beta_Joints` 保持可见） |

### 15.2 根因（三条，前两条**本仓早就写着**）

| # | 根因 | 出处（本仓已有） | 为什么"每次都犯" |
|---|---|---|---|
| **甲** | **母版生成器把 `CTRL`(13) 与 `MIXAMORIG`(65) 两个骨集合都设成可见**（`V9_display` 契约）⇒ 13 根控制骨与它驱动的 65 根变形骨**逐根完全重合地画在一起** ⇒ 看着就是"**多出一整套骨骼**" | 处置口径写在 [#164 证据 §十二](hand-grip-cards-2026-09-15.md)：「目视时会看成『多套骨骼重叠』⇒ **预览脚本统一收敛为只显示 `MIXAMORIG` 一套**」；`author_xbot_chair_grab.py` 的宿主 1 里还**打印过提示**（"母版里 CTRL/MIXAMORIG 两个骨骼集合默认可见 ⇒ **78 根骨的八面体会挡在人身前**；建议在 Outliner 里把这两个集合的眼睛关掉"） | **口径写了、提示打了，但从来没有任何一个宿主真去关它**——每次都把"要 owner 自己去点"当成了处置 |
| **乙** | **13 根"骨尖标记"（零蒙皮权重的叶骨）骨尖伸到皮肤外面**：`HeadTop_End` **+241.85 mm**（头顶上方）· `LeftToe_End`/`RightToe_End` **+93.50 mm**（脚尖前方）· 10 根手指 `…4` **+15.70 ~ +31.66 mm** | [管线 §2.1.4](../../presentation/Blender%E5%8A%A8%E4%BD%9C%E5%88%B6%E4%BD%9C%E7%AE%A1%E7%BA%BF.md) 注 1：「`HeadTop_End` 与 `Toe_End` 是"**骨尖标记**"，不是关节——它们是**叶骨**（无子骨）、且**没有顶点权重**」 | 它们**不能删**（导出契约要恰好 65 根变形骨）⇒ 只能在预览里**藏**——而没有任何宿主藏过 |
| **丙** | **验证回路里没有任何通道能看见骨架** | 本件实测：回显卡量的是几何；静帧走**渲染器**，而 **Blender 的渲染根本不画骨架**（这 9 张静帧里一根骨头都没有） | ⇒ 「骨头飘在模型外面」在我这一侧**从来没有读数**，判据面永远绿 |

### 15.3 新判据：把"骨头飘在模型外面"变成读数

`visible_bone_outliers(arm)` —— **可见骨的 head/tail 必须落在皮肤包围盒内**；
"伸出"与"贴面"的分界**沿用同一个既有常量** `CONTACT_TOL_MM = 2.8 mm`（口径 §8.2 的"多近算贴住表面"），
**不新拍阈值**。实测两类之间有一个**数量级**的天然间隙：正经关节的骨尖最多越界 **2.13 mm**（指尖/趾尖），
而标记骨越界 **15.70 ~ 241.85 mm**。

| 时刻 | 可见骨 | 读数 | 结论 |
|---|---|---|---|
| **收敛前（母版原样）** | 78 根（`CTRL` + `MIXAMORIG` 都可见） | **9 根伸出**：`HeadTop_End` **+241.85** · `LeftToe_End`/`RightToe_End` **+93.50** · 手指 `…4` ×6 **+15.70 ~ +31.66** · `Head` **+21.48**（**已知例外**，见下） | ❌（这正是 owner 看到的那 9 根） |
| **收敛后（本宿主）** | **52 根**（只 `MIXAMORIG`；13 根标记骨 + 13 根控制骨已隐藏） | **0 根非例外越界** | ✅ |

⚠️ **判据的两条已知边界（如实登记）**：
① 包围盒只抓"整体伸出去"的那类；**盒内的空隙**（腋下、胯间）抓不到。第一版试过**射线奇偶**与
**法向侧判定**，**在 X Bot 这套非闭合网格上都不成立**（实测：髋骨点被两种方法都判成"体外"，
`Hips`/`Spine` 等 62 根被误报）⇒ 最终改用包围盒。
② `mixamorig:Head` 的**骨尖**比皮肤最高顶点高 **21.48 mm**——它是**正经关节**（523 个顶点带权重），
骨尖只是画在颅顶标记处 ⇒ 登记为**已知例外**（`DOOR_PUSH_BONE_OUTLIER_EXCEPTIONS`，且**每次运行重新实测**，
数值漂了会看得见）。

### 15.4 修法与逐条读数

| 项 | 改法 | 读数 |
|---|---|---|
| 甲（多出一套骨） | 预览里**只让 `MIXAMORIG` 可见**（`arm.data.collections[...].is_visible`）——**按 #164 已写的口径执行** | `CTRL` 集合 `is_visible: True → False`；可见骨 **78 → 52** |
| 乙（骨尖标记） | **13 根零权重叶骨**（**推导**得到，不写死清单：`无子骨 且 零蒙皮权重 且 不在控制集合`）移进隐藏集合 `HIDDEN_MARKERS` 并 `bone.hide=True`；⚠️ **不删**（导出契约 65 根） | 藏起 13 根；`HeadTop_End` / `Toe_End` ×2 / 手指 `…4` ×10 |
| 丙（没有观测通道） | 新增判据 `visible_bone_outliers` 进报告与 `problems` | 收敛前 **9 根伸出**（会报红）→ 收敛后 **0 根** ✅ |
| ② 我的误修 | **回改**：`Beta_Joints` **不藏**（它是人体网格，藏了会掉腰部蒙皮） | `未藏的网格: ['Beta_Joints']` |
| 导出侧 | 骨集合可见性与 `bone.hide` **只影响显示**——重跑核 | **E0–E6 全绿**：骨 **65** · `CTRL_` **0** · 单根 · 网格 **0** · 烘焙 **0.0011 mm** · 回导 **0.0018 mm**（隐藏后导出**实测**不受影响，不是推断） |

### 15.5 这次为什么"必须由你来说三遍"——结构性的那一层

本仓的门禁（`run_all_checks` 16 个校验器）**全部看文件内容**，没有一个能看"渲染出来的样子"；
而**唯一能生成的图像**（Workbench 静帧）**不画骨架**。⇒ 只要问题只存在于"骨架怎么画"这一层，
**任何数量的机械判据都不会红**。这次的处置把这一层补上一格（骨头 ↔ 皮肤包围盒），
但**仍然到不了"看起来像不像人"**——那一格**永远归 owner 目视**（口径 §十）。

### 15.6 owner 目视确认（2026-09-16）

> owner 原话：「**可以了，现在没问题了**」。

⇒ 第 3 轮的两件事**都由 owner 目视确认通过**：① 预览里不再有多余骨骼（执行面读数：可见骨 **52**、
非例外越界 **0**）② 上一轮被我误藏的 `Beta_Joints`（腰部蒙皮）**没有坏**（已回改）。

### 15.7 #166 的验收标准逐条结账（**改后**）

| # | 验收标准 | 结论 | 证据 |
|---|---|---|---|
| 1 | 改前只到 28° 静帧、无时序与接地读数、未跑导出 → 改后逐帧可读的完整推门段 | ✅ | §十四.5（61 帧逐帧表）· `push_report_r2f.json` |
| 2 | 终态 = `通过开度`（由体型实测推出）· **不得出现无来源的角度常量** | ✅ | §四（`asin(0.6234/0.82) = 49.486°`）· §十四.1① |
| 3 | 改族判定：实测「保持扣住可达的最大角」并登记结论 | ✅ 未触发（弧上扫描 **−20° → −88° 全程可解**，残差 0.00） | §十四.3 |
| 4 | `F4a` = `持续`：三条接触对**逐帧**在容差内，松一帧即红 | ✅ 帧 16–61 逐帧 0.00 / 2.33 / ±1.5（最坏 **2.33 / 2.8 mm**） | §十四.2 |
| 5 | 时序判据（零阈值）：**声明到位帧 == 产物极值帧** | ✅ **51 == 51**（判据量 = 手骨绕铰链轴方位角**幅值**，§十四.2 记了取错符号时的报红） | §十四.2 |
| 6 | 接地与支撑相位：沿用 `soleMargin = 2.8 mm`；相位与换脚帧由 #165 的件给出并逐帧报出 | ✅ 无支撑帧 **0** · 换脚帧 **12 处** · 独立件复算**逐值一致** | §十四.2 · §九 |
| 7 | 导出侧 **E0–E6 全绿** | ✅ 全绿（隐藏骨集合之后**重跑实测**，不是推断） | §八 · §十五.4 |
| 8 | **owner 目视**：能连着播"看着不出戏" | ✅ **通过**（2026-09-16「可以了，现在没问题了」）⚠️ **但经过 3 轮返工**——见下 | §十四.1 · §十五.6 |

**⚠️ 第 8 条必须带这句登记**：口径 §13.8 的判据 2 是「owner 目视 **≤2 轮/件**」，而本件走了 **3 轮**
（第 1 轮四条指名 → 第 2 轮改完但你发现"多余骨骼"仍在 → 第 3 轮才通过）。⇒
**`动作描述口径` 的 `Health` 因此仍不判 `PASS`**（口径 §十 的 **V-c「≤1 轮返工」未达标**），
哪怕机械面（V-a/V-b/V-f）全绿、导出全绿、owner 最终接受。另外 **V-e（预览版 ↔ 权威版对拍）本件未做**。
⇒ 这两条如实留在 §十二 与切片里，**等 owner 说改**再改。
