# P4a 证据 — 环境锁定与最小 CI

> 按 [WORKFLOW.md §五/§六](../../../WORKFLOW.md) 的必填字段记录。

## 绑定

| 字段 | 值 |
|---|---|
| base SHA | `b67d7a3` |
| head SHA | `b67d7a3`（本证据随该提交入库） |
| 阶段 | P4a（owner 方案 §9.1） |
| 执行者 | DSH agent（本机） |

## 工具版本（实测）

| 工具 | 版本 | 命令 |
|---|---|---|
| .NET SDK | 10.0.400 | `dotnet --version` |
| .NET runtime | 10.0.11（**无 net8.0**） | `dotnet --list-runtimes` |
| 工程目标框架 | net8.0 | `*.csproj` |
| Python | 3.10.12 | `python3 --version` |
| PyYAML | 6.0.3 | `python3 -c "import yaml;print(yaml.__version__)"` |
| numpy / scipy / numba | 2.2.6 / 1.15.3 / 0.67.0 | — |
| Unity Editor | 6000.5.2f1（**NOT_AVAILABLE**） | `ProjectSettings/ProjectVersion.txt` |

## 执行的命令与退出码

| # | 命令 | 退出码 | 结论 |
|---|---|---|---|
| 1 | CI YAML 解析（PyYAML safe_load） | 0 | 3 个 job 合法 |
| 2 | 9 个校验器循环（与 CI 同构） | 0 | 9/9 ✅ |
| 3 | `dotnet restore --packages .ci-probe-pkgs`（**干净包目录**） | 0 | NuGet.config 生效，不依赖开发机缓存 |
| 4 | `dotnet build -c Release --no-restore` | 0 | 4 warnings / **0 errors** |
| 5 | `dotnet test -c Release --no-build --logger trx` | 0 | **353 passed / 0 failed** |
| 6 | 产出 `artifacts/engine.trx` | — | 490 KB，已 gitignore |

## 问题差分

```
new_finding = after − mapped(before) − expected_delta = ∅
```

- **before**：本阶段前，9 校验器 exit=0、dotnet test 353/0（Phase 5 记录，可复现）
- **expected_delta**（阶段开始前登记）：
  1. 新增锁文件（`global.json` / `NuGet.config` / 两份 requirements.txt）
  2. 新增 CI workflow
  3. 新增本文档与证据
  4. `.gitignore` 增加 CI 产物条目
  → **预期不改变任何校验器结果与测试结果**
- **after**：见上表第 2、5 行——与 before 一致 ✅
- **new_finding：空**

## 工作树

检查后 `git status` 干净；探针目录（`.ci-probe-pkgs`、`artifacts`）已删除；两者与 `*.trx` 已加入 `.gitignore`。

## 回滚演练

未执行 `git revert` 演练（本阶段为**纯新增文件** + 一行 `.gitignore`，且已单独提交）。
回滚方式：`git revert <P4a-commit>` 删除新增锁文件与 workflow；无其他文件依赖它们。

> ⚠️ **此项未做实测**，按判据「回滚演练恢复上一稳定状态」尚不满足。登记为 P4a 的未闭合项。

## 未验证项（不得当作通过）

1. **CI 未在 GitHub 真实运行**——本机只逐条模拟命令与退出码。首次 push 后的 CI 结果才是真证据
2. **Unity 门禁未执行**（`NOT_AVAILABLE`）→ 阻塞 P4b / P4d / P5，不阻塞 P4a / P4c / P7
3. **Blender 工具未纳入 CI 验证**
4. **无 `packages.lock.json`**（NuGet 传递依赖锁定）——见 [构建与测试 §五](../../engineering/build-and-test.md)

## 消费闭合

按 [WORKFLOW.md §四](../../../WORKFLOW.md)，门禁闭合 ≠ 消费闭合。
本阶段产物的消费者是 **P4b / P4c / P4d**——它们尚未开始，故**消费闭合待后续阶段确认**。

---
*创建: 2026-09-12 | 更新: 2026-09-12*
*关联: [构建与测试](../build-and-test.md), [WORKFLOW.md](../../../WORKFLOW.md)*
