# 弯腰双手拿起椅子、持在胸前证据（**口径 §十六 缺素材回归批 ① 件** · 椅子跟随手）

> [#180](https://github.com/verystrongdog/game/issues/180) 的收口证据。权威 = [动作描述口径](../../presentation/%E5%8A%A8%E4%BD%9C%E6%8F%8F%E8%BF%B0%E5%8F%A3%E5%BE%84.md) §十六（缺素材回归批 · 成因类「甲」）· §13.4/§13.4.1（判据形态与 `F4a` 时域）· §6.1（四系）· §8.1/§8.2（两个数）· §10.1（判据层）· [动作库规格](../../presentation/%E5%8A%A8%E4%BD%9C%E5%BA%93%E8%A7%84%E6%A0%BC.md) §四·戊·5（判据 1/3/4/5 的阈值）；
> 必填字段按[阶段证据说明](README.md)与 [WORKFLOW.md §五/§六](../../../WORKFLOW.md) 记录。

## 一、结论（先读这一段）

| 项 | 值 |
|---|---|
| 达成 | **不产生四轴迁移**（照 #166 的既有写法）：本件是**呈现探针**——`动作库规格.md` §四·戊 顶部的暂停裁定仍有效 |
| 一句话 | 宿主 9 `--task liftchest` 产 **`LiftChairToChest`**（**61 帧** @30 fps）：**起手 6 → 扣住 25 → 提离地 34 → 抡到位 49 → 保持 61**；双手 ↔ 椅背被握构件在 **[25, 61]** 逐帧成立（逐帧最坏 **0.92 / 1.72 mm**，容差 `tol_contact` **2.8 mm**） |
| **握式（owner 第 3 轮指名后定稿）** | **反手抓椅背的左右竖直侧面端**（`x=±0.21`、40 mm 厚那张端面）：**拇指朝下（−Z）· 掌心朝内（朝椅子中线）· 四指绕过近侧竖棱落到近侧面上**——骨局部三轴（未抡前）左手 `X→+Z · Y→+Y`、右手 `X→−Z · Y→+Y`（**镜像**，不再是一上一下） |
| **抡弧** | 先提离地（帧 25→33），再绕**椅子中线上的握点**转 **−92°（绕世界 X 轴）**（帧 33→49）⇒ 椅子被抡起来、**横着维持**在胸前（握点从 `(0, −0.470, 0.7199)` 移到 `(0, −0.235, 1.160)` m） |
| 判定标准 | **支持**（见 §五）：能播段成立 · `F4a` 持续型逐帧绿 · 时序**四格零阈值**全中 · 接地/穿模/支撑域无违规 · 三个持椅高度**各自跑满全部判据且全绿** ⇒ 待 owner 目视（**已走 2 轮返工**，见 §5.1 / §5.2，如实登记为**超出 ≤2 轮目标**） |
| **核心发现 ①** | **"缺素材"那一格不用新卡**：三种握式（掐棱侧面端 / 掌面朝下扣上沿 / **反手抓侧面端**）都只用**卡 1 的三种判据形态**（点↔线 / 点↔面 / 点↔体）+「判据的度量对象要与措辞同层」⇒ 口径 §16.2 子假设②（"握法要新卡"）**被否定** |
| **核心发现 ②** | **"椅子跟随手" = 一根空物体 + 逐帧位姿**：零件挂在 `REF_ChairRoot` 上，宿主逐帧解出它的 `location + rotation_euler` 并打键；判据侧**从求值后的对象矩阵重建握持面**（旋转无关）⇒ 两手各导一份椅子位姿的互证 **0.001 mm / 0.0°**；⚠️ 这条读数**能报红**（§六#2 与 §6.2#11 两次实测） |
| **核心发现 ③（实测 14 条真缺陷）** | 逐条见 §六/§6.1/§6.2。最贵的三条：① **骨盆后移 + 深弯腰**把"髋↔踝"弦拉到 **0.905 m > 骨长 0.889 m** ⇒ 腿被夹直、**双脚被抬起 15.97 mm**；② 根空物体归位后未 `update()` ⇒ **椅子沉到地下 520.1 mm 而接触判据全绿**；③ **抡弧漏转手的朝向** ⇒ 握持刚性读到 **124 m**、四指读数从 ±0.5 mm 漂到 **−102 mm** |
| 未做的事 | 不新增动作词条 · 不接 Unity 侧 lab · `PLAYABLE.md` 不动 · **不主张**运行时"拿起椅子"可用（属 §16.2 丙类暂停线）· 未做导出 `E0–E6` · 未接 §10.13 |

## 二、绑定

| 字段 | 值 |
|---|---|
| base SHA | `c38aeb6`（#180 建 issue 后那次文档提交；开工时工作树干净、`validate_issues --from-github` **0 fail**） |
| head SHA | 本条**提交组**（7 条，按主题）：`89ddf03`（宿主 9）· `b58aed2`（证据 + 三处回填）· `ea4e058`（回滚演练与可复现性）· `e2e85fa`（**第 1 轮返工**：握式二 `--grip palm`）· `06295ac`（返工文档）· `3a70c13`（**第 2 轮返工**：握式三 `--grip backhand` + 抡弧）· `67e6848`（返工文档与证据） |
| 阶段 | [#180](https://github.com/verystrongdog/game/issues/180)（`Experiment` · `state:in-progress`，2026-09-16 开工） |
| 执行者 | DSH agent（本机 WSL → Windows 侧 Blender） |
| 工具版本 | Blender **5.1.2**（`hash ec6e62d40fa9`，built 2026-05-19 01:37:34）· Windows 11 · `gh` 2.100.0 |
| 被测对象 | 母版 `.scratch/blender_assets/xbot/XBot_AnimationTemplate.blend`；本条的剪辑副本 `.scratch/issue180/lift_clip.blend`（由 #171 的 `kneel_clip.blend` 复制而来，含 `PanicCoverEars` + `CrouchPushedBack` + `KneelElbowBrace`；本条**加**三条动作都不覆盖） |
| ⚠️ 母版与过程物的地位 | `.scratch/` 是**被 `.gitignore` 排除的过程物**（`build_report`、`.blend` 副本、报告 JSON、静帧 PNG、**活体窗口启动脚本**）⇒ 上表只作**留痕**，证据里**不建链接**（#170 曾因链接过程物在干净检出上被判死链）；复现靠"重建母版后再跑" |

### 2.1 开工登记

`允许变化` ＝ issue §「预期差分」逐字：`code/tools/author_xbot_chair_grab.py`（**加宿主 9，既有八个宿主一字不动**——`git diff` 只新增，不改既有行）· `design/presentation/动作描述口径.md`（§十六 件表结账）· `design/presentation/动作库规格.md`（§四·戊·1 第 2 项回填 `Grab2H` 的取消理由与重启条件）· `design/slices/CP-01-ward-1f/slice.md`（四轴表回填）· 本条证据文件（新增）。

| # | 与 issue 的差异 | 依据 |
|---|---|---|
| 1 | `export_xbot_action.py` **未改**（issue 说"仅在需要时按既有参数导出"）⇒ 本件**不导出 FBX**：完成定义是"Blender 侧能连着播"（§16.4 硬边界②），导出 `E0–E6` 登记为未闭合 | 口径 §16.4 · issue §「明确排除」 |
| 2 | **开工时补登**：`design/engineering/evidence/README.md` 加一行（先例：#165–#171 各一行） | 先例 |
| 3 | 过程物不入库：宿主报告、解空间报告、三个变体报告、静帧 6 张 ×3 变体、活体窗口启动脚本与日志、诊断脚本（§六只引用**读数**，不引用路径链接） | [AGENTS.md §四](../../../AGENTS.md) · #170 的死链教训 |

## 三、命令与退出码

```powershell
# ① 宿主 9（逐帧解算 + 逐帧打键[含椅子根空物体] + 产物侧判据 + 静帧）
& "...\blender.exe" --background --factory-startup --python-exit-code 1 `
  --python 'Z:\home\dog\game\code\tools\author_xbot_chair_grab.py' -- `
  --task liftchest --bend 65 --template '<剪辑副本>' --report '<json>' [--still '<png>']

# ② 解空间（弯腰角 × 胸前握点高度，口径 §4.2 硬规矩 5）：同一支命令加 --scan-lift
# ③ 另两个持椅高度（各跑满全部判据）：--chest-grip-z 1.10 / 1.30 + --action 另名
# ④ 活体窗口（Windows 侧 GUI，非 --background）：三件同屏 + 三份各自的椅子，从不写回
```

| 命令 | 退出码 |
|---|---|
| 宿主 9 `--task liftchest --bend 65`（**权威运行** · 掌面朝下握式） | **0**（`problems: []`） |
| 同上 `--chest-grip-z 1.10 --action LiftChairToChestLow` | **0**（`problems: []`） |
| 同上 `--chest-grip-z 1.30 --action LiftChairToChestHigh` | **0**（`problems: []`） |
| `--scan-lift`（9 行解空间，弯腰角 × 胸前握点高度） | **0**（9 行**逐行可行**） |
| 同上 **`--grip side`**（第一轮的侧面端掐棱，对照解） | **0**（`problems: []`）——返工前的三解读数见 §4.2 尾注 |
| 活体窗口启动（`--background` 自检跑一次 + GUI 起一次） | **0**（三实例可见骨 52 / 越界 0；椅子轨迹自检见 §四.7） |
| `python3 code/tools/validate_cross_refs.py` | **0**（0 死链 / 0 段引用警告） |
| `python3 code/tools/validate_trash_isolation.py` | **0** |
| `python3 code/tools/validate_params.py` | **0**（与改前逐值相同） |
| `python3 code/tools/run_all_checks.py`（编排器） | **0**（**18 production validators + 1 fixture suites: 19 passed / 0 failed**） |
| `python3 code/tools/validate_issues.py --from-github` | **0**（14 rules: 10 passed / **0 failed** / 4 warnings；#180 自身 I5/I6/I7/I11 全过，I11 由 ⚠️ 转 ✅——单线程约束**满跑**） |
| `dotnet test code/src/YouAreNotTheFish.sln` | **未跑** —— 本次**未改 `code/src/`**（先例：[#165]/[#168]/[#170]/[#171] 同记法） |

## 四、读数

### 4.1 声明帧表与**零阈值**时序（口径 §十 V-f）

| 帧表 | 帧 | 判据量（产物侧） | 声明 | 产物 | 一致 |
|---|---|---|---|---|---|
| 中立 | 1 | 零通道（导出侧 E5 硬约定） | 1 | 1 | ✅ |
| **起手** | 6 | `CTRL_*Arm/ForeArm/Hand` 旋转通道的**首个非零值帧** | 6 | **6** | ✅ |
| **扣住** | 25 | 双手接触对**同时**全部落进 `tol_contact` 的帧 | 25 | **25** | ✅ |
| 起吊 | 33 | （椅子从这一帧之后离地） | 33 | — | — |
| **离地** | 34 | 椅子代理网格最低 z **首次 > `soleMargin`（2.8 mm）** | 34 | **34** | ✅ |
| **胸前到位** | 49 | **握点（上沿顶面）高度 − 髋高度**（都从产物取）的**平台起始帧** | 49 | **49** | ✅ |
| 保持 | 49–61 | 到位后姿势与椅子位移逐帧不变 | 12 帧 | 12 帧 | ✅ |

⚠️ 四格都是**零阈值**（帧号相等，无容差）：起手量的是**通道值首个 ≠ 0**，扣住/离地量的是**首次越界**，胸前到位量的是**最后一个发生变化的帧**（平段取首帧，先例 #170 §2.2）。

### 4.2 判据 1：接触对（**持续型** `F4a`）

判据层 = **蒙皮层 `Beta_Surface`**（口径 §10.1，owner 2026-09-16 裁定）；判据面**逐帧从求值后的椅背对象矩阵重建**（旋转无关，§6.2#12 是这条的对照组）。**反手握式的四条**（三条判据 + 一条读数）：

| # | 手部分区 | 物体分区 | 形态 | 左 | 右 | 判 |
|---|---|---|---|---|---|---|
| 1 | **虎口** | **端面** | 点↔面（ε 换算后的着力点） | **0.00** | **0.00** | ✅ 判据 |
| 2 | 食/中/无名/小四指（**末节**） | **板体（有限盒）** | 点↔体（有符号） | **−0.22 / −0.37 / −0.92 / +0.49** | **+1.72 / −0.44 / +0.64 / +0.54** | ✅ 判据（逐根并列） |
| 3 | **拇指（末节）** | **板体（有限盒）** | 点↔体（有符号；两面对照并列报） | **−0.01** | **+0.99** | ✅ 判据 |
| 4 | 掌面（读数） | 端面 | 点↔面 | **+10.47** | **+10.46** | ⚠️ **只报读数**（ε 的位置依赖性，§6.1#6） |

- **持续窗口 [25, 61]**（含 33→49 的**抡弧**：椅子绕握点转 92°、握点抬高 0.44 m）：逐帧最坏 **0.92 / 1.72 mm** · **超差帧 0** · **接近段红帧 24 / 24** · 非退化对照（中立姿势）**1020.45 / 1020.43 mm** ⇒ 判据**能报红**。
- **容差** = `tol_contact` **2.8 mm**（§8.2 的两个数之一）；**ε = 32.6833 mm 是另一个数**，只用于"骨 → 表面"的换算。
- ⚠️ **"持续"窗口不是摆设**：**椅子在转**（−92°）+ **手在动**（肘角 111.6° → 78.1°）⇒ 每一帧都重新解臂、重新把虎口解回端面上（虎口读数逐帧 0.00 mm、刚性 0.0802 mm）。
- ⚠️ **另两种握式同样全绿**（本件一共做了三种）：`--grip side`（正手掐棱，虎口↔近侧竖棱 0.00 · 拇指 −0.28/−0.45 · 四指 ±1.5）· `--grip palm`（掌面朝下扣上沿，掌面 0.00 · 拇指 −0.23/+0.06 · 四指 ≤2.76）——**三套判据面不同，分别报、不混读**。

### 4.3 椅子跟随手：逐帧椅子变换 + **握持刚性**（§四·戊·5 判据 3）

| 量 | 读数 | 阈值 / 来源 |
|---|---|---|
| 椅子在手骨局部坐标的位姿稳定性（**左 / 右**） | 逐帧最坏 **0.0936 mm** · 全程累计 **0.0543 / 0.0936 mm** | ≤ **1 mm/帧** · ≤ **5 mm** 全程（§四·戊·5 判据 3） |
| **两手各导一份椅子位姿的互证** | 平移最坏 **0.001 mm** · 转角最坏 **0.0°** | 只报读数（两条独立算路的互证） |
| 判据 1（两手间距） | 中位 **0.2548 m**（**挥舞带** 0.15–0.25 ±25% ⇒ [0.1125, 0.3125] ✅ · 波动 **0.0000 m**） | ≤25% 偏差 · 波动 ≤0.1 m |
| 同上 · **搬运锚** 0.487 m | 偏差 **−47.7%**（**只报读数**：见 §八#11 的几何上限） | ±25%（§四·戊·5 判据 1 的两种语义并列） |
| 虎口间距（另一个读数） | **0.2054 m** | 只报读数 |

⚠️ **椅子只平移、不旋转**：椅子变换的唯一载体是空物体 `REF_ChairRoot` 的 `location`（逐帧打键）；判据侧的握持面**从 `REF_Back` 求值后的网格角点重建**。

### 4.4 手部标定（**全部解出来**，不是手拍常量）

| 量 | 左 | 右 | 怎么解出来的 |
|---|---|---|---|
| 逐指蜷曲量（Index / Middle / Ring / Pinky） | **1.195 / 1.215 / 1.15 / 1.08** | **1.385 / 1.43 / 1.38 / 1.32** | 每个握点落点上做**模式搜索精修**（目标 = 末节↔板体），再在落点扫描里取"四指残差最小"的那一组 |
| 握点在端面上的**深度分数** `grip_df` | **0.75** | **0.30** | 一维扫描 0.20→0.80 × 0.05：判据 = 四指**同时**满足"末节↔近侧面"与"末节↔构件表面"的最大偏差最小 |
| 拇指（末节↔板体） | **−0.01 mm** | **+0.99 mm** | 粗解到目标点 → Newton 回代 → **局部模式搜索**（6° / 2° / 0.7° 三轮，按**板体**打分） |

屈曲轴逐组实测（口径 F3 的外部判据）：四指 **X+1**、拇指 **X+1**（左右一致）。⚠️ 三条实测教训：① 逐指拟合必须按**判据要量的那个对象**打分（§6.1#8）；② 标定必须**逐手用各自那一侧的 frame**（§6.2#13）；③ 拇指的粗搜索左右会各落一个局部解，必须**局部精修**（§6.1#7）。

### 4.5 判据 4 / 判据 5 / 接地 / 支撑域 / 可见骨（全绿）

| 判据 | 形态 | 读数 |
|---|---|---|
| **判据 4 无穿模** | 点↔**有向盒**（椅子六个零件的碰撞体） | 非接触部位（躯干/上臂/大腿）最小间隙 **263.09 mm**（绑定骨 = `Hips`：椅子在身前的水平净距）· 手/前臂**骨心**最坏 **33.17 mm**（阈值 ≥ −20）· **违规帧 0** |
| **判据 5 包握** | 每根手指**末节蒙皮**（`*4` 骨主导顶点落在 `…3` 顶点组里）↔ 被握构件 | 逐手最坏 **5.58 / 5.62 mm**（阈值 ≤15）· 到位帧逐指 `{Index 1.14, Middle 5.58, Ring 4.07, Pinky 1.14, Thumb 2.25}`（左）· 掌面压顶面 + 拇指在近侧面 + 四指绕远侧面 ⇒ **掌心与指尖分居构件两侧** |
| **接地**（`soleMargin` 2.8 mm） | 接触相位件（#165）读产物 | 双支撑 **[[1, 61]]** · **无支撑区间未出现** · **换脚帧未出现** · 地面最坏 **−0.32 mm**（= 母版中立姿势本身，先例 #171） |
| **支撑域**（凸包件 · 蒙皮层） | 重心（髋投影）↔ 支撑多边形（布尔） | 可判 **61/61** · 域内 **61** · **域外 0** · **退化 0** · 余量最小 **71.305 mm**（帧 18）⇒ 弯腰段与起身段**逐帧在域内** |
| **可见骨不伸出皮肤包围盒** | 包围盒包含（布尔） | 可见骨 **52** · 非例外越界 **0**（口径 §15.4 / §15.8） |
| 目视水平（V-b 的既有判据） | 头骨上轴与竖直夹角 | 逐帧 **0.000°**（`solve_gaze` 解出的颈头反向补偿，逐帧入 captured ⇒ 复演/导出不丢） |

### 4.6 解空间并列（口径 §4.2 硬规矩 5）：**三个解各跑满全部判据**

| 解 | 抡到位时握点高度（椅子平移） | 扣住帧肘角 | 抡到位帧肘角 | 接触对最坏 | 时序四格 | 握持刚性 | 全判据 |
|---|---|---|---|---|---|---|---|
| **② 主解**（`LiftChairToChest`） | **1.16 m**（+0.4401） | 111.6° | **78.1°** | 1.72 mm | 6/25/34/49 ✅ | 0.0802 mm | **OK** |
| ① 抱低一点（`…Low`） | 1.06 m（+0.3401） | 111.6° | 90.5° | ≤1.72 mm | 6/25/34/49 ✅ | ≤0.09 mm | **OK** |
| ③ 举高一点（`…High`） | 1.26 m（+0.5401） | 111.6° | 70.0° | ≤1.72 mm | 6/25/34/49 ✅ | ≤0.09 mm | **OK** |

`--scan-lift`（弯腰角 55/65/75° × 抡到位握点高度）**逐行可行**；⚠️ **如实登记**：弯腰角那一维在**接触读数上不出差**（握式解算总把虎口解回端面），它的真实差异在**腿的可达性**上（§六#1），而 `--scan-lift` 只扫**两端**、不扫过渡帧。

### 4.7 活体窗口（口径 §15.8）

| 项 | 读数 |
|---|---|
| 形态 | **一个** Blender 窗口（§15.8 owner 2026-09-16 裁定）里三件同屏：① 抱低 @ **0 m** · ② **主解** @ **+5 m** · ③ 举高 @ **−5 m** |
| 每件的椅子 | **各带一份自己的椅子**（零件复制 → 挂到本实例的空物体 → 逐帧打键 `location + rotation_euler`，与宿主同一段算法）；轨迹自检：帧 1/25/33 恒 **0** → 帧 49/61 **(0, −0.9709, 0.7154) + 绕 X −92°** |
| 逐实例收敛 | 可见骨 **52 / 52 / 52** · 非例外越界 **0 / 0 / 0** |
| lab 是否写回 | **从不**（口径 §15.8 硬规矩 1/2） |

## 五、判定标准（三种结局的评估）

| 结局 | 是否命中 | 依据 |
|---|---|---|
| **支持** | ✅（待 owner **第 2 轮**目视确认） | ① 能播段成立（61 帧逐帧解算 + 逐帧打键 + 椅子逐帧跟着走，三解各自全绿）② `F4a` 持续型**逐帧**绿（超差帧 0）③ 时序**四格零阈值**全中 ④ 接地 / 穿模 / 支撑域**违规帧 0** ⑤ **暴露的下一层缺口 ≤1 条**（§八#11 判据 1 的两个锚；三条子假设里**"握法要新卡"被否定**、运动质量只报读数） |
| 否定 | ✗ | owner 目视若 >2 轮；或出现"机械判据全绿而 owner 指出某个量"——按 §十 **V-d** 拒绝结案并要求指名量。⚠️ **第 1 轮已发生**（见下） |
| 证据不足 | ✗ | 有逐帧读数（61 帧 × 两条接触对 × 四条判据面）· 三个解各跑满全部判据 · 非退化对照 1020 mm · 两遍独立进程（§七） |

### 5.1 owner 目视：**第 1 轮不通过**（如实登记）

| 项 | 记录 |
|---|---|
| 形态 | **活体窗口**（一个 Blender 界面三件同屏：z=1.10 / 1.20 / 1.30，各带自己的一份椅子；按空格播放整段） |
| 第 1 轮原话（**逐字**） | 「**动作不错，但是不是我想要的，这个怪我之前没说清楚**，现在人体是用**虎口朝向 z 轴**的方向将椅子抓起的，我希望能够用**虎口朝向 z 轴负方向**的抓法，因为这样将椅子抓起来之后**才能把椅子当作武器抡出去**」 |
| 处置 | 按 §十 **V-d**（"还是不对"时机制**拒绝结案并要求指名量**）：本件当场把**可点名的量**报出来（当时的握式 = 卡 1 掐棱用在椅背**侧面端**：掌面法向 = **±X 朝椅心** · 骨局部 `X`（蜷曲轴）= **世界 −Z** · 骨局部 `Y`（腕→指尖）= **世界 −Y** · 虎口点 = 骨点 + ε·(**±X**)，即**虎口朝侧**而不是朝下），并**返工**成 owner 要的那一手 |
| 返工内容（本轮） | **握式二：掌面朝下扣在椅背横杆（上沿）上**（`--grip palm`，**新的默认**）——骨局部 `Y → 世界 −Y`、`X → 世界 +X` ⇒ **掌面法向 = −Z（虎口朝下）**；判据面随之改成"掌面最低点↔顶面 · 拇指末节↔近侧面 · 四指末节↔远侧面"（**判据形态仍然是卡 1 的那三种**，只换参照面）；原来的侧面端掐棱保留为 `--grip side` 对照解 |
| 返工轮次 | **第 1 轮**（下接 §5.2 的第 2 轮 ⇒ **合计 2 轮返工**，超出 §13.8 判据 2 的 ≤2 轮目标，如实登记；[#166] 走过 3 轮、[#169] 走过 3 轮是同族先例） |

### 5.2 owner 目视：**第 2 轮不通过**（掌面朝下扣上沿那一版被否）

| 项 | 记录 |
|---|---|
| 第 2 轮原话（**逐字**） | 「**他妈的不对啊，我要的是抓住椅子的侧面，不是上沿，要反手抓，把椅子抡起来维持在胸前**，如果你有不确定的地方先来问我然后再实现」 |
| 处置 | 按 §十 **V-d** 先把不确定项**问清楚**（owner 这一轮明确要求"先问再实现"）：① 抓哪个"侧面"？② "反手"是哪种朝向？③ 椅子要不要绕手转？owner 的回答：① **椅背的左右竖直侧面端** ② **拇指朝下 · 掌心朝内** ③ **抡一个上摆弧、到位后椅子横过来**；补问"横成什么样"时 owner 回答：「**椅子不是一个整体么，你把手和椅背的相对位置固定之后直接调手的位置就行了，椅子跟着动**」⇒ **椅子姿态由手的路径决定**，不再由我拍 |
| 返工内容 | 新增握式三 **`--grip backhand`（新默认）**：接触面 = 椅背**侧面端** · **反手**（拇指朝下 · 掌心朝内 · 四指绕**近侧**竖棱落到近侧面）· **抡弧**（先提离地、再绕握点转 `LIFT_SWING_DEG = −92°` 绕世界 X 轴）· 椅子的 `location + rotation_euler` 逐帧打键 |
| 轮次 | **第 2 轮返工** ⇒ §13.8 判据 2 的 ≤2 轮目标**未达**（如实登记）。⚠️ 两条原因分开记：第 1 轮是**握式朝向**（我按"卡 1 侧面端"实现，owner 要"虎口朝 −Z"）· 第 2 轮是**我自作主张**把"虎口朝 −Z"解成了"扣上沿"（owner 要"抓侧面 + 反手 + 抡"）——第 2 轮起 owner 明确要求"先问再实现"，本件照办 |

## 六、本件实测踩到的 **14 条**真缺陷（第一轮 5 条 + 第 1 轮返工 5 条 + 第 2 轮返工 4 条）

| # | 症状（产物侧读数） | 根因 | 修法 |
|---|---|---|---|
| 1 | **双脚被抬起 15.97 mm**：深弯腰（65°）时逐帧读到足部蒙皮最低 **+9.13（帧 20）→ +15.58（帧 25–33）**；踝与趾**同步位移 15.97 mm**（= 整体平移，不是脚掌俯仰）⇒ 接地判据报**无支撑区间 [18, 39]**、支撑域 **退化 22 帧** | 骨盆位移取 `(0, +0.055, −0.04)`（沿背向**后移**）⇒ "髋 ↔ 踝"弦长 **0.905 m** > 实测骨长 **大腿 0.44371 + 小腿 0.44528 = 0.889 m** ⇒ `solve_two_bone()` 走 `clamped_straight` 分支（`tip_err 16.0 mm`）。⚠️ 修前**只是判据能报红**：若不看接地/支撑域，这个错在接触判据上**完全看不见**（两处判据全绿） | 骨盆位移改为 **只下沉不后移** `(0, 0.0, −0.120)`（后移的活由骨盆俯仰自己干：65° 时髋关节本就后移 45 mm）⇒ 弦回到骨长以内（`tip_err 0.0`、`clamped False`）；`--pelvis-back/--pelvis-down` 进 CLI 以便复现 |
| 2 | **椅子沉到地面以下 520.1 mm，而接触判据逐帧 0.00 mm**（"判据绿 ≠ 产物对"的又一次实测） | 根空物体是**上一轮存盘留下的状态**（`save_as_mainfile` 存的是当时那一帧，根停在末帧位移上）⇒ 本函数虽然把 `location` 归零，但**没 `update()` 就读 `root.matrix_world`** ⇒ 读到**上一轮的矩阵**，`matrix_parent_inverse` 把那份位移**反着**烙进每个零件 | ① 先 `animation_data_clear()` 再归位；② **归位后 `update()`** 才取 `matrix_world`。⚠️ 这条**不会**被"椅子与手一致"的判据抓住（两侧用的是同一个错位姿）——只有"椅子 vs 地面"这条独立判据（离地/地面零穿透）才抓得住 |
| 3 | **起手帧声明 5 / 产物 6**（时序判据当场报不一致） | 进度用 `smoothstep`，而 `smoothstep(0) = 0` ⇒ 声明为"起手"的那一帧姿势仍**逐位等于中立**，首个非零通道出现在**下一帧** | 进度分子 **+1**（`up = (frame − START + 1) / (GRIP − START + 1)`）⇒ 声明帧当帧就 > 0；声明帧**之前**的帧走"严格中立"分支。同上，**判据自己也从 5 改成 6 再跑**过一次（那一版才是错的声明） |
| 4 | **判据 5 读出 −14 ~ −19 mm 的"陷进构件"**（四指骨尖），而肉在 **±1.5 mm** | 判据 5 的措辞是"每根手指**末节到构件表面**"，而实现量的是**骨尖**（`…4` 骨的 tail）——骨尖**本来就在肉里**，量它必然读出"陷进"（另有拇指骨尖 +25.74 mm 的假悬空）。⚠️ 同族的第二个坑：蒙皮**没有 `…4` 顶点组**（只到 `…3`），按 `…4` 取蒙皮会**一个点都取不到**（读数 `None`） | 改为量**末节蒙皮**（`…3` 顶点组）到**被握构件**的有向盒的最近距离；骨尖读数保留在 `F4` 那一栏（它与卡 1 的"四指↔远侧面"是**两个量**，不得混） |
| 5 | **拇指 Newton 回代一步后残留 −4.5 mm**（> 容差 2.8） | 拇指的求解是**粗搜索**（网格 + 两级细化，≈3° 精度）⇒ 一步 Newton 会**过冲或不足**；与卡 1 的一步版（残留 2.33 mm 可接受）是同一个写法的两处不同结果 | 迭代到**容差内或不再改善**为止（上限 3 轮），并**退回最好那一轮**的目标（不改进不采纳）⇒ 实测 **−0.28 / −0.45 mm** |

### 6.1 第 1 轮返工时又踩到的 5 条（掌面朝下握式）

| # | 症状（产物侧读数） | 根因 | 修法 |
|---|---|---|---|
| 6 | **虎口点比掌面层低 10.47 mm**（掌面朝下时：掌面压在顶面上、而虎口点落在板**里**） | `web_point()` 的 `ε = 32.6833 mm` 是 [#160] 从 **`Hand` 骨原点**沿掌面法向标定的（掌心的肉厚），而它被加在"**拇指根↔食指根的中点**"上——虎口处的肉更薄 ⇒ **ε 有位置依赖性**，这里是 **10.47 mm** 的偏差 | 本握式**以掌面层为接触基准**（判据行），虎口点降为**读数行**并标 `只报读数`（不参与"接触成立"判定）；ε 的"逐位置标定"登记为未闭合（§八#6） |
| 7 | **拇指一只手对、一只手错**：粗解 + 一步 Newton 后 左 **−6.8 mm** / 右 **+1.99 mm** | `solve_thumb_to_target()` 是**网格粗搜索**（±3° 分辨率、只动 `Thumb1`），两手各落在不同局部解 | 加**局部模式搜索精修**（`Thumb1` 三轴，6°→2°→0.7° 三轮，27 次求值/轮）⇒ 实测 **−0.23 / +0.06 mm** |
| 8 | **"点↔体（板体）"这条判据在掌面朝下时形同虚设**：手指**从顶面斜插进板里 7–16 mm** 与"绕远侧上沿兜住"在**有符号盒间隙**上都能取到 0（fit 出的姿势就是前者） | 判据的度量对象与措辞不同层（口径 §4.2 硬规矩 3）——盒间隙允许"从上面插进去"这种解 | 第 4 行判据改成 **末节↔远侧面（点↔面，有符号）**；板体穿透量**并列报读数**（不静默丢掉） |
| 9 | **"只报读数"的行混进了接触对的统计**：`逐帧最坏_mm` 报 10.47（那是虎口点的**已知偏置**）⇒ 判据**假红** | 统计与判定都摊平了**所有**行 | `lift_contact_ok()` 与接触对的"逐帧最坏/超差帧"只看**判据行**（`只报读数` 的行两处都排除） |
| 10 | **落点与蜷曲量的耦合**：只标定蜷曲量时，四指末节离远侧面 17–21 mm（`--grip-x` 收到 0.16 仍有 3.3–3.7 mm） | 掌面朝下时"掌面落在顶面的哪一条线上"是一个**独立的自由度**，它与逐指蜷曲量**耦合**（落点一动，四指的间隙全变） | 加**一维落点扫描**（[−0.045, +0.045] × 5 mm），**每个落点上重做四指精修**，判据 = 四指最大残差最小 ⇒ 实测落点 **+0.020 m**、四指残差 **≤2.76 mm** |

（#1 只在接地/支撑域上现形；#2 只在地面判据上现形；#4 是判据量的度量对象错层）。⇒ 建议进[危险点表](../../engineering/%E5%8D%B1%E9%99%A9%E7%82%B9%E8%A1%A8.md)（**本件不做**：它不在本 issue 的预期差分内，先例 #170/#171 同记法，见 §八#5）。

### 6.2 第 2 轮返工（反手抓侧面端 + 抡弧）又踩到的 4 条

| # | 症状（产物侧读数） | 根因 | 修法 |
|---|---|---|---|
| 11 | **握持刚性 124481 mm**（= 100 × 1.24 m；骨架 `scale = 0.01` 把差值放大 100 倍）、四指"点↔体"读数从 **±0.5 mm** 漂到 **−102 mm** | **抡弧只转了 frame（位置），没转手的朝向** ⇒ 手保持静止朝向而椅子转了 92°，相对位姿破掉 | `lift_arm_solve_backhand()` 接 `rot`，把 `y_grip` / `x_grip` 也按 `R` 转 ⇒ 刚性回到 **0.0802 mm** |
| 12 | **帧 48 起四指的"点↔体"读数从 ±0.5 mm 跳到 268–328 mm**，而"点↔面"读数**不变** | `board_end_frame()` 用 `z.z < 0` 判"哪边朝上"——**椅子抡到 ~90° 时这条启发式翻向** ⇒ frame 的 `z` 轴翻转、盒的 z 跨度反了 | `lift_chair_frame_from_object()` 里把 `frame["z"]` 按**零件自己的局部 +z** 钉死（旋转无关；未旋转时与原来逐值相同） |
| 13 | **右手拇指残留 6.0 mm**（左手 0.32 mm） | 标定时**两只手都用左端面的 frame** ⇒ 右手的逐指拟合与拇指全按**错的那张面**解 | 标定改成**逐手用各自那一侧的 frame** ⇒ 0.99 mm |
| 14 | **右手拇指 15.42 mm 超判据 5**，而它的"距近侧面"读数只有 **1.29 mm** | "点↔**无限**面"读不出"手指越过了板的**边**"，而判据 5 的措辞是"到**构件表面**" | 反手接触对与判据 5 的拇指一律改量**有限盒**（点↔体，有符号），"面内"读数并列报 ⇒ 0.5 / 1.72 mm |

⚠️ 十四条里 **六条**属于仓库既有那条"**判据绿 ≠ 产物对**"的同一族：#1（只在接地/支撑域现形）· #2（只在地面判据现形）· #4/#8/#14（判据量的**度量对象错层**）· #9（统计把读数当判据）· #11（只在刚性/接触对的**逐帧**读数上现形；肉眼看上去就是"椅子转了、手没转"）。⇒ 建议进[危险点表](../../engineering/%E5%8D%B1%E9%99%A9%E7%82%B9%E8%A1%A8.md)（**本件不做**：不在预期差分内，先例 #170/#171 同记法，见 §八#5）。

## 七、问题差分

`new_finding = after − mapped(before) − expected_delta` = **空**。

| 检查 | 结果 |
|---|---|
| 预期差分内的文件 | `code/tools/author_xbot_chair_grab.py`（**只新增**：宿主 9 + 5 个可复用件 + CLI 参数 + 宿主表一行 + 分发一行）· `design/presentation/动作描述口径.md`（§十六 件表结账 + §16.5 U18 追记）· `design/presentation/动作库规格.md`（§四·戊·1 第 2 项回填）· `design/slices/CP-01-ward-1f/slice.md`（四轴表回填）· 本条证据文件（新增） |
| 开工时补登的一处（§2.1） | `design/engineering/evidence/README.md`（加一行） |
| 计划外变化 | **无**（`git status --short` 提交后为空；`.scratch/` 下的过程物被忽略） |
| **回滚演练** | 见 §九 |
| 预期不变项 | 既有**八个宿主**（`chair` / `card1` / `door` / `restate` / `doorpush` / `panic` / `crouch` / `kneel`）逐行未动 · `data/hand_grip_cards.json` 与 `validate_grip_cards.py` 未触碰 · `xbot_contact_phase.py` / `xbot_balance.py` **两个独立件一字未改**（只调用）· `tol_contact = 2.8 mm` 与 `soleMargin = 2.8 mm` 未触碰 · `data/action_set.json`（17 词条不新增）· `PLAYABLE.md`（仍 `NO_AUTHORIZED_PLAYABLE`）· `design/rules/回合战斗流程.md` §10.13 未触碰 · `code/unity/**` 未触碰 · 母版 `.blend` 与源 `X Bot.fbx` 字节未触碰（宿主只写 `.scratch/issue180/` 的副本） |
| **可复现性（两遍独立进程）** | 主解跑两遍（**两个独立的 Blender 进程**、同一母版副本、同一版代码）：**可比数值逐值相同（值不同 0 处）**——含逐帧参数表、接触对的全部读数、刚性/穿模/包握/支撑域读数（只排除 `measured_at` 时间戳与两个 `mathutils.Matrix` 中间量）。⚠️ 返工轮（掌面朝下握式）后**重跑过**该对照 |
| **第 1 轮返工的范围** | 只改 `code/tools/author_xbot_chair_grab.py` 的**宿主 9 段**（加握式二与它的标定/精修）＋ 本条证据；**既有八个宿主逐行未动** |

**测试报告定位**：宿主 9 的权威报告与三个变体报告、解空间报告、静帧 PNG、活体窗口日志**都在 `.scratch/issue180/`**（被 `.gitignore` 排除、磁盘保留）；按 #170 的教训，**证据里不建链接**——干净检出上它们必然不存在。复现命令见 §三。

## 八、未闭合（诚实清单）

| # | 项 |
|---|---|
| 1 | **词条位（U18）未裁**：双手拾取在受控 17 词条里**没有位置**（`Grab2H` 已取消、`Lift1H` 是单手）⇒ 本件走**呈现探针**形态；要不要为它开词条（走 [动作库规格](../../presentation/%E5%8A%A8%E4%BD%9C%E5%BA%93%E8%A7%84%E6%A0%BC.md) §3.3 扩展协议）**等这一件验完再裁**（与 §15.7.2 同法） |
| 2 | **运动质量只报读数、无判据**：口径 §十四 MQ1–MQ6 的基线与实现仍为 0 ⇒ "像不像在使劲 / 太机械"这一类判词**归 owner 目视**（#170 已发生过一次，owner 当时裁定不在该件范围） |
| 3 | **不主张运行时能力**：道具跟随是运行时的事（导出物 `object_types = {"ARMATURE"}`、animation-only、无网格）⇒ 本件**不复活** §四·戊 的挂点机制（`isKinematic` + 忽略碰撞 + 手骨父子关系），属 §16.2 **丙类**（暂停线） |
| 4 | **母版不可逐字节复现** · 残留 `REF_*` 代理（#160 已登记）；⚠️ 与本件有关的一条**已测**：报告层可复现性**成立**（两遍独立进程 **10592 个数值 0 处不同**，见 §七）——与 #170 当时「位级未成立」的情形**不同**，如实分开记 |
| 5 | **本件的 5 条真缺陷没有进[危险点表](../../engineering/%E5%8D%B1%E9%99%A9%E7%82%B9%E8%A1%A8.md)**（它不在本 issue 的预期差分内；先例 #170/#171 同记法）⇒ 其中三条与**位置**强相关（`matrix_parent_inverse` 的陈旧 `matrix_world` · 深弯腰下"髋↔踝弦 > 骨长" · 判据量的度量对象错层）**值得进表**，须另开件 |
| 6 | **未做导出 `E0–E6`**：完成定义只到"Blender 侧能连着播"（§16.4 硬边界②）⇒ 导出侧是否全绿**没有读数** |
| 7 | **只做动作形态，未接 §10.13**（"拎家具当武器"仍不入规则层）· 不做副手 IK · 不做持椅线其余暂停件 |
| 8 | **椅子只平移**：本件不做椅子的旋转/倾覆（"抬起来歪一点"这类观感差异**没有自由度**）；`u_span/z_span` 由网格角点给出，**仍受网格分辨率限制**（同 #169/#171 的缺口） |
| 9 | **共用件 `solve_two_bone()` 的滚转符号缺陷未统一修**（#171 登记的真缺陷；改它会动 #166/#170/#171 的产物 ⇒ 须另开件）：本件不跪地，未暴露它 |
| 10 | **`--scan-lift` 不扫过渡帧**：弯腰角那一维在两端解上不出差，腿的可达性只在实际逐帧运行里现形（§四.6 已如实登记） |
| 11 | **判据 1 的两个锚上不了同一个读数**：§四·戊·5 判据 1 逐字给了两种语义（挥舞带 0.15–0.25 m · 搬运锚 0.487 m），而**掌面朝下扣 42 cm 宽横杆**时四指的横向跨度 ≈60 mm ⇒ 两手最宽只能到 ≈ **±0.18 m**（再宽外侧两指就出了板宽、其"到板体"读数永久 >13 mm；`--grip-x` 实测 0.16 时无名指 3.3–3.7 mm、0.15 时四指全 ≤1.1 mm）⇒ 本件读数 **0.2548 m** 落**在两带之间**：挥舞带 ±25% 过、搬运锚 −47.7%。§四·戊·5 判据 2 的注已裁定「**跨椅子模型比绝对距离不成立**」（HUMOTO 的 0.487 来自另一件家具）⇒ **该裁哪一条锚、或是否给"扣横杆"单立一条锚，归 owner**（本件硬判挥舞带 + 并列报搬运锚偏差，不擅自改 §四·戊·5） |
| 13 | **`grip_df`（握点在端面上的深度）左手 0.75 / 右手 0.30**：两侧差 0.45（≈18 mm）——手是镜像的，而**拟合的局部最优不是**（逐指与拇指的粗搜索各落一个局部解）⇒ 观感上两手在端面上的落点不对称。本件如实登记为读数，未做「两侧联合标定」（那要两倍参数空间） |
| 14 | **抡弧的终点角 `LIFT_SWING_DEG = −92°` 是 agent 解**（owner 只说到「抡起来维持胸前、椅子横过来」）：终点姿态由这个角定 ⇒ 若要「再横一点 / 再竖一点」，改一个常量即可（未开 CLI） |
| 12 | **`ε` 的位置依赖性（新发现）**：`web_point()` 的 ε 是从 `Hand` 骨原点标定的（#160），搬到"拇指/食指根中点"上实测偏 **10.47 mm** ⇒ 虎口点在掌面朝下握式里**不能当判据**（本件降为读数）。要它能用，须**逐位置**标定 ε（虎口 / 掌根 / 指腹各一格）——属 [#168] 那条工具线的追加件，**本件不做** |

## 九、回滚演练实做记录

形态与 [#166]/[#167]/[#168]/[#170]/[#171] 的先例一致：**临时 worktree + 逐条 `git revert` + 全门禁重跑**（不做 `reset --hard`）。

```bash
git worktree add --detach .scratch/rollback-drill-180 HEAD
cd .scratch/rollback-drill-180
git revert --no-edit 89ddf03   # 宿主 9（+ 可复用件 + CLI）
git revert --no-edit <docs>    # 证据 + 口径/规格/slice 回填
```

| 核对项 | 预测（issue 的「回滚」节） | **实测** |
|---|---|---|
| 回落点 | 纯脚本 + 文档 + 证据 ⇒ 回到 base 状态 | ✅ `git diff c38aeb6 HEAD` = **0 行**（回滚态与 base **逐字节相同**） |
| `validate_cross_refs.py` | 0 死链 | ✅ 见 §九.1 |
| `validate_trash_isolation.py` | 全过 | ✅ 全部通过 |
| `validate_params.py` | 0 失败 | ✅ 与改前逐值相同 |
| 工作树 | 干净 | ✅ 演练后 `git worktree remove --force` 清掉临时树 |
| 数据迁移 / 资产 | 无 | ✅ 无（宿主只写 `.scratch/` 的副本；母版 `.blend` 与源 FBX 字节未触碰） |

### 9.1 演练实做读数（2026-09-16）

```bash
git worktree add --detach .scratch/rollback-drill-180 HEAD
cd .scratch/rollback-drill-180
git revert --no-edit 67e6848 3a70c13   # 第 2 轮返工（握式三 + 抡弧）+ 文档
git revert --no-edit 06295ac
git revert --no-edit e2e85fa   # 第 1 轮返工：握式二 --grip palm
git revert --no-edit ea4e058   # 证据补回滚演练与可复现性
git revert --no-edit b58aed2   # 证据 + 三处回填
git revert --no-edit 89ddf03   # 宿主 9（+ 可复用件 + CLI）
git diff c38aeb6 HEAD | wc -l  # ⇒ 0
```

| 核对项 | 实测 |
|---|---|
| 回落点 | ✅ **`git diff c38aeb6 HEAD` = 0 行**（回滚态与 base **逐字节相同**；`--stat` 为空）——**返工轮后再演一遍**仍是 0 行 |
| `validate_cross_refs.py`（回滚态） | ✅ **2124 refs / 2123 passed / 0 dead / 0 section warnings**——与**同一支 worktree 直接检出 base `c38aeb6`** 的空跑对照**逐值相同**（2124 / 2123 / 0） |
| `validate_trash_isolation.py`（回滚态） | ✅ 全部通过 |
| `validate_params.py`（回滚态） | ✅ **56 checks / 49 passed / 0 failed / 7 warnings**（与改前逐值相同） |
| 工作树 | ✅ 演练后 `git worktree remove --force` 清掉临时树（`git worktree list` 只剩主树） |
| 数据迁移 / 资产 | ✅ 无（宿主只写 `.scratch/` 的副本；母版 `.blend` 与源 FBX 字节未触碰） |

⇒ **回滚恢复上一状态成立**（以最强形式：逐字节）。⚠️ 两处**数量差**如实登记：主树 `validate_cross_refs` 报 **2143 refs / 2142 passed / 0 dead**（本件新增的引用），回滚态与 base 都是 **2124 / 2123 / 0** ⇒ 差的 **19 个引用**全部来自本件新增的证据文件。

### 9.2 推送与 CI

见本条提交组的最后一次更新（推送由 owner 逐次确认；推送后回填 run id 与最终 SHA）。

## 十、来源

| 事实 | 来源 |
|---|---|
| 缺素材回归批的轴与首件（成因类「甲」） | [动作描述口径](../../presentation/%E5%8A%A8%E4%BD%9C%E6%8F%8F%E8%BF%B0%E5%8F%A3%E5%BE%84.md) §16.1–§16.4 |
| 呈现探针三条硬边界（不新增词条 / 不接 Unity / `PLAYABLE` 不动） | 同上 §16.4 |
| 判据形态受控枚举（点↔线 / 点↔面 / 点↔体 / 弧面↔面）+ `F4a` 判据时域 | 同上 §13.4 / §13.4.1 |
| 四系（世界/重力系 · 角色体侧系 · 道具局部系 · 目标部位局部系） | 同上 §6.1 |
| 判据层 = **蒙皮层**（owner 2026-09-16 裁定） | 同上 §10.1 |
| 两个数不许混（换算 ε 32.6833 mm vs 验收 `tol_contact` 2.8 mm） | 同上 §8.1 / §8.2 |
| 卡 1「掐棱」的三条接触对与 F10「逐指」口径 | 同上 §13.2 / §13.6 · `data/hand_grip_cards.json` |
| 判据 1 的搬运锚 0.487 m（±25% · 波动 ≤0.1 m）· 判据 3 握持刚性（≤1 mm/帧 · ≤5 mm 全程）· 判据 4 的三个数（0 / 2 cm / 1 cm）· 判据 5 包握（≤1.5 cm · 掌指分居两侧） | [动作库规格](../../presentation/%E5%8A%A8%E4%BD%9C%E5%BA%93%E8%A7%84%E6%A0%BC.md) §四·戊·5 |
| `Grab2H` 取消的逐字理由与代价（按两次键 · 0.10–0.12 s 融合） | 同上 §四·戊·1 第 2 项 |
| 椅子几何（坐面 0.4449 · 靠背 0.42×0.35×0.04 · 腿 0.05 · 摆位 ±0.165） | 同上 §四·丁 · 宿主 `chair_layout()`（**唯一来源**） |
| 接地 / 足底蒙皮取点与接触相位（本条只引用，不重算） | [#165](https://github.com/verystrongdog/game/issues/165) 的件 `code/tools/xbot_contact_phase.py` |
| 支撑多边形 / 重心判据（本条只引用，不重算） | [#170](https://github.com/verystrongdog/game/issues/170) 的件 `code/tools/xbot_balance.py` |
| 导出侧首帧必须**严格中立**（E5 硬约定） | [Blender 动作制作管线](../../presentation/Blender%E5%8A%A8%E4%BD%9C%E5%88%B6%E4%BD%9C%E7%AE%A1%E7%BA%BF.md) §四·1 · [危险点表](../../engineering/%E5%8D%B1%E9%99%A9%E7%82%B9%E8%A1%A8.md) §四 |
| 腿骨静止局部 X = 世界 −X（两侧同轴）· 臂左右镜像 | 同上 §2.1.2 |
| 反手抓侧面端 + 抡弧（虎口朝 −Z 的**反手**版）——**owner 2026-09-16 第 2 轮目视指名** | 本条 §5.2 逐字 |
| 掌面朝下的握式（虎口朝 −Z）——**owner 2026-09-16 第 1 轮目视指名** | 本条 §5.1 逐字 |
| #163 的 `_strict_hand_axes()`（掌面向下 · 展开轴∥上沿）与 `hand_grip()` 的两遍定点 | [动作描述口径](../../presentation/%E5%8A%A8%E4%BD%9C%E6%8F%8F%E8%BF%B0%E5%8F%A3%E5%BE%84.md) §八 · `code/tools/author_xbot_chair_grab.py`（`main_chair()` 那条路径逐字未动） |
| 全部读数 | 本条实测（Blender 5.1.2，2026-09-16） |

---

*创建: 2026-09-16（agent 执行 [#180](https://github.com/verystrongdog/game/issues/180)）*
*状态: 收口证据——假设**支持**（三个解各跑满全部判据全绿）；owner 目视与结账见文末追记*
*关联: [#180](https://github.com/verystrongdog/game/issues/180) · [#171](https://github.com/verystrongdog/game/issues/171) · [#170](https://github.com/verystrongdog/game/issues/170) · [#169](https://github.com/verystrongdog/game/issues/169) · [#164](https://github.com/verystrongdog/game/issues/164) · [动作描述口径](../../presentation/%E5%8A%A8%E4%BD%9C%E6%8F%8F%E8%BF%B0%E5%8F%A3%E5%BE%84.md) · [动作库规格](../../presentation/%E5%8A%A8%E4%BD%9C%E5%BA%93%E8%A7%84%E6%A0%BC.md) · [阶段证据说明](README.md)*
